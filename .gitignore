# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# 로컬 API 키
.env

server/results.json

server/bench-*.jsonl
server/llm-results.json
# run-llm.mjs --group <그룹> 의 결과 — 전체 기준선과 같은 생성물이다
server/llm-results-*.json

# 로컬 도구 상태
.context/
.claude/launch.json

# 디렉터리든 심링크든 — 워크트리 에이전트가 심링크를 커밋한 사고(2026-09-11)
node_modules
.superpowers/
.claude/worktrees/

# wrangler 로컬 시크릿 — 절대 커밋하지 않는다
server/.dev.vars
# wrangler dev 로컬 상태(KV 에뮬레이션 등)
server/.wrangler/
/.wrangler/
build-sim/
