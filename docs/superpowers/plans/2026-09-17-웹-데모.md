# 웹 데모 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Etavia의 핵심 한 줄기(A1→A2→A3→A5→A6)를 로그인 없이 체험할 수 있는 공개 웹 URL을 만들고, 심사 기간 4주 동안 죽지 않게 한다.

**Architecture:** 새 앱을 만들지 않는다. 같은 코드를 `react-native-web`으로 두 번째 타깃에 내보내고, 웹에서 죽는 네이티브 모듈은 Metro가 자동 우선하는 `.web.ts` 플랫폼 파일로 대체한다. 가용성의 급소는 화면이 아니라 **폴백**이다 — `/route`와 장소 검색이 실패하면 지금은 계획 전체가 죽으므로, 목 공급자로 강등되는 층을 먼저 깐다. Kakao 키는 웹 번들에서 빼고 Workers `/places` 프록시 뒤로 옮긴다.

**Tech Stack:** Expo SDK 57 · React Native 0.86 · react-native-web 0.21 · TypeScript 6 · Cloudflare Workers + Pages · node:test (`tsx --test`)

**Spec:** `docs/superpowers/specs/2026-09-17-웹-데모-design.md`

## Global Constraints

- **마감 2026-09-20 23:59:59.** 9/20은 수정일이 아니라 제출 확인일이다.
- **2026-09-21 ~ 10-17 동안 링크가 살아 있어야 한다.** 접속 불가 시 심사 대상에서 제외된다.
- **로그인·보호 설정 없이** 시크릿 모드에서 열려야 한다.
- Expo는 버전 문서를 그대로 따른다: https://docs.expo.dev/versions/v57.0.0/
- 테스트: `npm test` (= `tsx --test 'src/**/*.test.ts' 'server/src/*.test.ts' 'scripts/*.test.mjs'`). 단일 파일은 `npx tsx --test <path>`.
- **`node_modules`를 절대 스테이징하지 않는다.** 이 저장소는 워크트리이고, 과거에 심링크가 커밋돼 메인 `node_modules`가 지워진 사고가 있었다.
- **LLM은 '무엇을'만 뽑는다.** 소요시간·거리·도착 가능 여부를 만들게 하지 않는다. 이 계획은 그 원칙을 건드리지 않는다.
- 웹 shim은 **기존 `.ts` 파일을 수정하지 않는다.** 네이티브 동작이 바뀌면 안 된다. 예외는 명시된 두 곳(`tracker.tsx` 가드, `places.ts` 폴백 배선)뿐이다.

## 실행 순서

| 단계 | Task | 날짜 |
|---|---|---|
| A. 폴백 | 1, 2 | 9/17 |
| B. 웹 shim·UI | 3, 4, 5, 6 | 9/17 |
| C. 첫 배포 (Kakao 키 없이) | 7 | 9/17 |
| D. `/places` 프록시 | 8, 9 | 9/18 |
| E. 완주 검증 | 10 | 9/18~19 |

**C가 D보다 먼저인 것이 중요하다.** 첫 배포는 Kakao 키를 아예 안 싣고 나가므로 장소 검색이 목 카탈로그로 돈다(Task 2의 폴백 덕분에 화면은 정상이다). 키가 공개 번들에 노출되는 순간이 **한 번도 없다.** Task 9가 프록시로 실검색을 켠다.

## File Structure

| 파일 | 책임 |
|---|---|
| `src/lib/routePlan/fallbackProvider.ts` | **신규.** RouteProvider 하나를 감싸 실패 시 추정 공급자로 강등 |
| `src/lib/placesFallback.ts` | **신규.** PlaceSearchProvider를 감싸 실패 시 목 카탈로그로 강등 |
| `src/lib/prefs.web.ts` | **신규.** localStorage 기반 설정 |
| `src/lib/trackLog.web.ts` | **신규.** 전 export no-op |
| `src/lib/backgroundLocation.web.ts` | **신규.** 추적은 no-op, `toFix`는 살린다 |
| `src/notifications.web.ts` | **신규.** 전 export no-op |
| `src/NotificationBridge.tsx` / `.web.tsx` | **신규.** `App.tsx`에 있던 브리지를 빼내 플랫폼 분기 |
| `src/lib/currentPlace.web.ts` | **신규.** 브라우저 geolocation + 데모 출발지 폴백 |
| `src/components/TabBar.web.tsx` | **신규.** `null` 반환 |
| `src/webFrame.css` | **신규.** 데스크톱 전용 폰 프레임 |
| `server/src/placesSchema.ts` | **신규.** `/places` 요청 검증 |
| `server/src/places.ts` | **신규.** Kakao Local 호출 + 캐시 |
| `src/lib/routePlan/fallbackProvider.test.ts` · `src/lib/placesFallback.test.ts` · `server/src/placesSchema.test.ts` · `server/src/places.test.ts` | 위 네 개의 테스트 |
| `src/state/planFlowProvider.tsx:112` | 수정. 자동차 공급자를 폴백으로 감싼다 |
| `src/lib/places.ts:287-296` | 수정. `getProvider`가 폴백 래퍼를 통과시킨다 |
| `src/state/tracker.tsx:390-408` | 수정. 웹에서 `live` 자동 시작 차단 |
| `App.tsx:10,16,68-97,117` | 수정. 알림 직접 import 제거, 브리지 분리 |
| `server/src/index.ts:126,131` | 수정. `/places` 라우트 등록 |
| `server/src/guard.ts:31,35,56` | 수정. `/places` 상한 |
| `server/wrangler.toml:8` | 수정. Pages 오리진 추가 |

---

## Task 1: 자동차 라우팅 폴백

지금 `/route`가 실패하면 `runPlan.ts:89-93`이 계획 전체를 `FAIL(direct)`로 끝낸다. 일일 상한·502·타임아웃 어느 것이든 데모가 죽는다. 대중교통 공급자(`transitProvider.ts:146-148`)가 이미 `estimate` + `onFallback` 패턴을 쓰고 있으므로 그 모양을 그대로 따른다.

**Files:**
- Create: `src/lib/routePlan/fallbackProvider.ts`
- Test: `src/lib/routePlan/fallbackProvider.test.ts`
- Modify: `src/state/planFlowProvider.tsx:105-116`

**Interfaces:**
- Consumes: `RouteProvider`, `RouteResult`, `LatLng`, `Mode` (`src/lib/routePlan/types.ts`)
- Produces: `fallbackRouteProvider(opts: FallbackProviderOptions): RouteProvider` where `FallbackProviderOptions = { primary: RouteProvider; estimate: RouteProvider; onFallback?: (err: unknown) => void }`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/lib/routePlan/fallbackProvider.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { fallbackRouteProvider } from './fallbackProvider.ts';
import { mockRouteProvider } from './mockProvider.ts';
import type { LatLng, RouteProvider } from './types.ts';

const A: LatLng = { latitude: 37.5271, longitude: 126.9327 };
const B: LatLng = { latitude: 37.5148, longitude: 126.8563 };

const throwing = (err: unknown): RouteProvider => ({
  async route() {
    throw err;
  },
});

test('기본은 primary 를 그대로 쓴다 — 성공하면 추정으로 안 내려간다', async () => {
  const estimate = mockRouteProvider();
  const p = fallbackRouteProvider({
    primary: { async route() { return { durationMin: 42, distanceKm: 9, polyline: [A, B], sections: [{ durationMin: 42, distanceKm: 9 }], source: 'provider' }; } },
    estimate,
  });
  const r = await p.route([A, B], 600, 'car');
  assert.equal(r.durationMin, 42);
  assert.equal(r.source, 'provider');
  assert.equal(estimate.calls, 0);
});

test('primary 가 던지면 추정으로 강등하고 source 는 estimate', async () => {
  const estimate = mockRouteProvider();
  const p = fallbackRouteProvider({ primary: throwing(new Error('server route 429')), estimate });
  const r = await p.route([A, B], 600, 'car');
  assert.equal(r.source, 'estimate');
  assert.equal(estimate.calls, 1);
  assert.ok(r.durationMin > 0);
  assert.equal(r.sections.length, 1);
});

test('429 만이 아니라 모든 실패에서 강등한다 — 502·타임아웃·잘못된 모양', async () => {
  for (const err of [new Error('server route 502'), new Error('server route: bad shape'), new DOMException('aborted', 'AbortError')]) {
    const estimate = mockRouteProvider();
    const p = fallbackRouteProvider({ primary: throwing(err), estimate });
    const r = await p.route([A, B], 600, 'car');
    assert.equal(r.source, 'estimate', `${err} 에서 강등하지 않았다`);
  }
});

test('강등 사유를 onFallback 으로 넘긴다 — 화면이 아니라 로그로 간다', async () => {
  const seen: unknown[] = [];
  const p = fallbackRouteProvider({
    primary: throwing(new Error('boom')),
    estimate: mockRouteProvider(),
    onFallback: e => seen.push(e),
  });
  await p.route([A, B], 600, 'car');
  assert.equal(seen.length, 1);
  assert.match(String(seen[0]), /boom/);
});

test('경유지가 있어도 sections 개수가 맞는다', async () => {
  const p = fallbackRouteProvider({ primary: throwing(new Error('x')), estimate: mockRouteProvider() });
  const mid: LatLng = { latitude: 37.52, longitude: 126.90 };
  const r = await p.route([A, mid, B], 600, 'car');
  assert.equal(r.sections.length, 2);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `npx tsx --test src/lib/routePlan/fallbackProvider.test.ts`
Expected: FAIL — `Cannot find module './fallbackProvider.ts'`

- [ ] **Step 3: 최소 구현을 쓴다**

`src/lib/routePlan/fallbackProvider.ts`:

```ts
/**
 * RouteProvider 하나를 감싸 실패 시 추정 공급자로 강등한다.
 *
 * 왜 필요한가: `/route` 가 던지면 `runPlan` 이 계획 전체를 FAIL(direct) 로 끝낸다.
 * 공개 데모에서는 일일 상한·502·타임아웃 중 하나만 걸려도 심사자가 오류 화면을 본다.
 * 상한은 피해를 묶는 장치여야지 가용성 차단 스위치여선 안 된다.
 *
 * 429 만 잡지 않는다 — 4주 동안 더 자주 오는 건 502 와 타임아웃이다.
 * 강등 사유는 onFallback 으로만 나간다. 이 모듈은 순수해야 한다(node 테스트가 닿는다)
 * — trackLog 는 expo-file-system 을 물기 때문에 여기서 부르면 안 된다.
 *
 * transitProvider 의 estimate/onFallback 과 같은 계약이다.
 */
import type { LatLng, Mode, RouteProvider, RouteResult } from './types';

export type FallbackProviderOptions = {
  primary: RouteProvider;
  /** 강등 대상. source 를 'estimate' 로 내는 공급자여야 한다 */
  estimate: RouteProvider;
  onFallback?: (err: unknown) => void;
};

export function fallbackRouteProvider(opts: FallbackProviderOptions): RouteProvider {
  return {
    async route(points: LatLng[], departAtMin: number, mode: Mode): Promise<RouteResult> {
      try {
        return await opts.primary.route(points, departAtMin, mode);
      } catch (e) {
        opts.onFallback?.(e);
        return opts.estimate.route(points, departAtMin, mode);
      }
    },
  };
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `npx tsx --test src/lib/routePlan/fallbackProvider.test.ts`
Expected: PASS — 5 tests

- [ ] **Step 5: `planFlowProvider`에 배선한다**

`src/state/planFlowProvider.tsx` 상단 import에 추가:

```ts
import { fallbackRouteProvider } from '../lib/routePlan/fallbackProvider';
```

`pickProvider()`의 `provider:` 줄(112행)을 이렇게 바꾼다:

```ts
      provider: hybridProvider(
        fallbackRouteProvider({
          primary: serverRouteProvider({ baseUrl, appToken, deviceId }),
          estimate: mock,
          // 화면엔 A5 판정 카드의 '추정' 문구로만 보인다 — 왜 내려갔는지는 로그에만
          onFallback: e =>
            logTrack({ k: 'net', ep: '/route', ms: 0, ok: false, d: { mode: 'car', fallback: true, err: String(e).slice(0, 120) } }),
        }),
        transit,
        mock,
      ),
```

- [ ] **Step 6: 전체 테스트를 돌린다**

Run: `npm test`
Expected: PASS — 기존 테스트가 하나도 깨지지 않아야 한다

- [ ] **Step 7: 커밋**

```bash
git add src/lib/routePlan/fallbackProvider.ts src/lib/routePlan/fallbackProvider.test.ts src/state/planFlowProvider.tsx
git commit -m "feat(web): /route 가 죽으면 추정으로 내려간다 — 상한이 가용성 차단 스위치였다"
```

---

## Task 2: 장소 검색 폴백 (웹에서만)

`places.ts:159`의 `kakaoFetch`가 던지면 `runPlan`이 `FAIL(search)`로 끝난다.

**네이티브에는 이 폴백을 켜지 않는다.** 부산 사용자가 "올리브영"을 검색했는데 목 카탈로그의 여의도 매장이 나오면 그게 더 나쁜 거짓말이다. 네이티브는 지금처럼 실패 배너(`DestinationSheet`의 `setFailed`)를 유지한다. 데모는 준비된 지역에서만 돌므로 목이 정직하다.

**Files:**
- Create: `src/lib/placesFallback.ts`
- Test: `src/lib/placesFallback.test.ts`
- Modify: `src/lib/places.ts` (import 추가 + `getProvider` 287-296행)

**Interfaces:**
- Consumes: `PlaceSearchProvider`, `Place` (`src/lib/places.ts`)
- Produces: `withMockFallback(primary: PlaceSearchProvider, fallback: PlaceSearchProvider, opts?: { enabled?: boolean; onFallback?: (err: unknown) => void }): PlaceSearchProvider`

> 타입을 `places.ts`에서 가져오면 순환 참조가 된다(`places.ts`가 이 모듈을 쓴다). 그래서 이 모듈은 **구조적 타입을 자기 안에 선언**한다 — `places.ts`의 `PlaceSearchProvider`가 그대로 대입된다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/lib/placesFallback.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { withMockFallback, type SearchProviderLike } from './placesFallback.ts';

const place = (name: string) => ({ id: `id-${name}`, name, address: '서울 영등포구', coord: { latitude: 37.5271, longitude: 126.9327 } });

const ok: SearchProviderLike = { key: 'kakao', async search(q) { return [place(q)]; } };
const boom: SearchProviderLike = { key: 'kakao', async search() { throw new Error('kakao keyword 429'); } };
const mock: SearchProviderLike = { key: 'mock', async search() { return [place('목-올리브영')]; } };

test('성공하면 primary 결과를 그대로 준다', async () => {
  const p = withMockFallback(ok, mock, { enabled: true });
  assert.deepEqual((await p.search('올리브영', null)).map(r => r.name), ['올리브영']);
});

test('enabled 면 실패 시 목으로 내려간다 — 빈 배열이 아니다', async () => {
  const p = withMockFallback(boom, mock, { enabled: true });
  const r = await p.search('올리브영', null);
  assert.equal(r.length, 1);
  assert.equal(r[0].name, '목-올리브영');
});

test('enabled 가 false 면 그대로 던진다 — 네이티브는 실패 배너를 띄워야 한다', async () => {
  const p = withMockFallback(boom, mock, { enabled: false });
  await assert.rejects(() => p.search('올리브영', null), /429/);
});

test('key 는 primary 것을 유지한다 — 어느 공급자를 쓰려 했는지가 로그에 남아야 한다', () => {
  assert.equal(withMockFallback(boom, mock, { enabled: true }).key, 'kakao');
});

test('primary 가 이미 목이면 감싸지 않는다', () => {
  assert.equal(withMockFallback(mock, mock, { enabled: true }), mock);
});

test('강등 사유를 onFallback 으로 넘긴다', async () => {
  const seen: unknown[] = [];
  await withMockFallback(boom, mock, { enabled: true, onFallback: e => seen.push(e) }).search('x', null);
  assert.equal(seen.length, 1);
  assert.match(String(seen[0]), /429/);
});

test('near·radiusM 인자를 목에 그대로 넘긴다', async () => {
  let got: unknown[] = [];
  const spy: SearchProviderLike = { key: 'mock', async search(...args) { got = args; return []; } };
  const near = { latitude: 37.5, longitude: 127 };
  await withMockFallback(boom, spy, { enabled: true }).search('빵집', near, 1500);
  assert.deepEqual(got, ['빵집', near, 1500]);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `npx tsx --test src/lib/placesFallback.test.ts`
Expected: FAIL — `Cannot find module './placesFallback.ts'`

- [ ] **Step 3: 최소 구현을 쓴다**

`src/lib/placesFallback.ts`:

```ts
/**
 * 장소 검색 실패를 목 카탈로그로 받아낸다. 웹 데모에서만 켠다.
 *
 * 지금은 kakaoFetch 가 던지면 runPlan 이 FAIL(search) 로 계획 전체를 끝낸다.
 * 공개 링크에서는 그게 곧 '데모가 죽었다'가 된다.
 *
 * **네이티브에서는 끈다.** 부산에서 '올리브영'을 찾았는데 목 카탈로그의 여의도 매장이
 * 나오면 조용한 거짓말이 된다. 실패 배너가 정직하다. 데모는 준비된 지역에서만 돌므로
 * 목이 답이 될 수 있다.
 *
 * 타입을 places.ts 에서 가져오면 순환 참조가 되므로 구조적 타입을 여기 둔다.
 */
export type PlaceLike = {
  id: string;
  name: string;
  address: string;
  coord: { latitude: number; longitude: number };
};

export type SearchProviderLike = {
  readonly key: string;
  search(query: string, near: { latitude: number; longitude: number } | null, radiusM?: number): Promise<PlaceLike[]>;
};

export type FallbackOptions = {
  /** 웹에서만 true. 네이티브는 기존 동작(던지기)을 유지한다 */
  enabled?: boolean;
  onFallback?: (err: unknown) => void;
};

export function withMockFallback<P extends SearchProviderLike>(
  primary: P,
  fallback: SearchProviderLike,
  opts: FallbackOptions = {},
): P | SearchProviderLike {
  if (!opts.enabled) return primary;
  if (primary.key === fallback.key) return primary;
  return {
    key: primary.key,
    async search(query, near, radiusM) {
      try {
        return await primary.search(query, near, radiusM);
      } catch (e) {
        opts.onFallback?.(e);
        return fallback.search(query, near, radiusM);
      }
    },
  };
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `npx tsx --test src/lib/placesFallback.test.ts`
Expected: PASS — 7 tests

- [ ] **Step 5: `places.ts`에 배선한다**

`src/lib/places.ts` 상단 import에 두 줄 추가:

```ts
import { Platform } from 'react-native';
import { withMockFallback } from './placesFallback';
```

`getProvider`(287행)의 본문을 바꾼다. **`mockProvider`는 감싸지 않는다** — 이미 목이다.

```ts
export function getProvider(near: LatLng | null): PlaceSearchProvider {
  const wanted = regionProviderKey(near);
  const pick = (): PlaceSearchProvider => {
    if (wanted === 'kakao' && kakaoRestKey) return kakaoProvider;
    if (wanted === 'google' && googlePlacesKey) return googleProvider;
    // 원하는 쪽 키가 없으면 있는 쪽이라도 쓴다 (해외에서 구글 키가 없을 때 등)
    if (kakaoRestKey) return kakaoProvider;
    if (googlePlacesKey) return googleProvider;
    return mockProvider;
  };
  /* 웹 데모에서만 목으로 받아낸다. 네이티브는 실패 배너가 정직하다 — placesFallback.ts 주석 참고.
     DestinationSheet 도 planSearchFn 도 이 함수를 지나므로 배선은 여기 한 곳이면 된다. */
  return withMockFallback(pick(), mockProvider, { enabled: Platform.OS === 'web' }) as PlaceSearchProvider;
}
```

- [ ] **Step 6: 강등을 화면에 말한다 — 플래그**

목으로 내려갔는데 화면이 아무 말도 안 하면 심사자는 그 결과를 실제 검색이라고 읽는다. 스펙 §1-3의 요구다.

`src/lib/places.ts`의 `getProvider` **위**에 작은 스토어를 둔다:

```ts
/**
 * 이번 세션에서 장소 검색이 한 번이라도 목으로 내려갔나.
 *
 * 한 번 서면 안 내린다 — 깜빡이는 배너는 읽히지 않는다. 뜻은 '이 화면의 결과 중
 * 일부는 예시 데이터일 수 있다'이고, 그게 사용자가 알아야 할 전부다.
 */
let searchDegraded = false;
export const isSearchDegraded = () => searchDegraded;
```

`getProvider`의 `withMockFallback` 호출에 `onFallback`을 붙인다:

```ts
  return withMockFallback(pick(), mockProvider, {
    enabled: Platform.OS === 'web',
    onFallback: () => {
      searchDegraded = true;
    },
  }) as PlaceSearchProvider;
```

- [ ] **Step 7: 강등을 화면에 말한다 — 문구**

`src/sheets/DestinationSheet.tsx`를 고친다.

import에 추가:

```ts
import { getProvider, isSearchDegraded } from '../lib/places';
```

> 이미 `getProvider`를 import하고 있으므로 그 줄에 `isSearchDegraded`만 더한다.

46행 `const [failed, setFailed] = useState(false);` 아래에 추가:

```ts
  const [degraded, setDegraded] = useState(false);
```

검색 성공 `.then` 블록 안, `setSearching(false);` 바로 위에 추가:

```ts
          setDegraded(isSearchDegraded());
```

363행 캡션의 삼항 사슬에 한 단계를 끼운다. `failed` 다음, `here.status === 'denied'` 앞이다 — 실패가 강등보다 급하고, 강등은 권한 안내보다 급하다:

```tsx
          {failed
            ? '검색에 실패했어요 · 연결 상태나 API 키를 확인해 주세요'
            : degraded
              ? '지금은 예시 장소 데이터로 보여드리고 있어요'
              : here.status === 'denied'
                ? '위치 권한이 없어 거리는 표시되지 않아요'
                : '경로를 계산하려면 목록에서 골라야 해요 · 가까운 순으로 보여드려요'}
```

> **범위 판단**: 이 문구는 검색 시트에만 단다. A5 판정 카드는 시간의 출처(`timingSource`)를 말하는 자리라 라우팅 강등이 이미 거기서 드러난다. 검색 강등을 A5까지 끌고 가려면 `Slot`에 필드를 더해 플래너를 관통해야 하는데, 3일 일정에서 그만한 값은 아니다.

- [ ] **Step 8: 전체 테스트를 돌린다**

Run: `npx tsc --noEmit && npm test`
Expected: 둘 다 성공

- [ ] **Step 9: 커밋**

```bash
git add src/lib/placesFallback.ts src/lib/placesFallback.test.ts src/lib/places.ts src/sheets/DestinationSheet.tsx
git commit -m "feat(web): 장소 검색이 죽으면 목 카탈로그로 받아내고, 그렇다고 말한다"
```

---

## Task 3: 웹 shim — 설정과 추적 로그

`expo-file-system`은 웹에서 지원되지 않는다. 지금은 던지지 않고 경고만 내지만(`expo-file-system is not supported on web` ×2), 설정이 조용히 안 저장된다 — A6 스포트라이트 가이드가 새로고침마다 다시 뜬다.

**Files:**
- Create: `src/lib/prefs.web.ts`, `src/lib/trackLog.web.ts`
- Modify: 없음

**Interfaces:**
- Consumes: `Prefs`, `DEFAULT_PREFS`, `parsePrefs`, `serializePrefs` (`src/lib/prefsFormat.ts`), `TrackEvent` (`src/lib/trackLog.ts`)
- Produces: `prefs.web.ts`가 `getPref`·`setPref`를, `trackLog.web.ts`가 `newRunId`·`currentRunId`·`logTrack`·`flushTrackLog`·`listTrackLogs`·`exportTrackLogs`·`clearTrackLogs`를 원본과 같은 시그니처로 내보낸다

- [ ] **Step 1: `prefs.web.ts`를 쓴다**

원본 `src/lib/prefs.ts`와 export가 정확히 같아야 한다. `localStorage`는 시크릿 모드·차단 설정에서 **접근만 해도 던진다** — 읽기·쓰기 양쪽에 try/catch가 필요하다.

```ts
/**
 * 웹 설정 — localStorage. expo-file-system 은 웹에서 지원되지 않는다.
 *
 * 네이티브와 같은 약속을 지킨다: **절대 던지지 않는다.** 설정은 앱이 도는 데 필수가
 * 아니다. 시크릿 모드·사이트 데이터 차단에서는 localStorage 에 **접근하는 것만으로도**
 * 던지므로 읽기·쓰기·파싱 셋 다 감싼다. 심사자가 시크릿 모드로 들어온다.
 */
import { DEFAULT_PREFS, parsePrefs, serializePrefs, type Prefs } from './prefsFormat';

const KEY = 'etavia.prefs';

function load(): Prefs {
  try {
    const raw = globalThis.localStorage?.getItem(KEY);
    return raw ? parsePrefs(raw) : { ...DEFAULT_PREFS };
  } catch {
    return { ...DEFAULT_PREFS };
  }
}

let cache: Prefs = load();

export function getPref<K extends keyof Prefs>(key: K): Prefs[K] {
  return cache[key];
}

export function setPref<K extends keyof Prefs>(key: K, value: Prefs[K]): void {
  if (cache[key] === value) return;
  cache = { ...cache, [key]: value };
  try {
    globalThis.localStorage?.setItem(KEY, serializePrefs(cache));
  } catch {
    // 못 써도 이번 세션은 메모리 값으로 동작한다
  }
}
```

- [ ] **Step 2: `trackLog.web.ts`를 쓴다**

원본의 7개 export를 모두 덮어야 한다. 하나라도 빠지면 `planFlowProvider.tsx:19`의 import가 런타임에 `undefined`가 된다 — **TypeScript는 이걸 못 잡는다**(확장자 없는 import는 `.ts` 계약을 본다).

```ts
/**
 * 웹 추적 로그 — 전부 no-op. expo-file-system·expo-sharing 은 웹에서 못 쓴다.
 *
 * 원본 src/lib/trackLog.ts 의 export 를 **하나도 빠짐없이** 덮어야 한다.
 * 빠지면 부르는 쪽에서 undefined 가 되는데, 확장자 없는 import 라 tsc 는 .ts 계약을
 * 보고 통과시킨다. 최종 검사는 실제 웹 export 와 브라우저 완주다.
 */
import type { TrackEvent } from './trackLog';

let runId: string | null = null;

export function newRunId(): string {
  runId = `web-${Date.now().toString(36)}`;
  return runId;
}

export function currentRunId(): string | null {
  return runId;
}

export function logTrack(_e: TrackEvent): void {}

export function flushTrackLog(): void {}

export async function listTrackLogs(): Promise<{ name: string; bytes: number }[]> {
  return [];
}

export async function exportTrackLogs(): Promise<'shared' | 'empty' | 'unavailable'> {
  return 'unavailable';
}

export async function clearTrackLogs(): Promise<void> {}
```

- [ ] **Step 3: 원본과 export 목록이 같은지 확인한다**

Run:

```bash
diff <(grep -o 'export \(async \)\?function [A-Za-z]*' src/lib/trackLog.ts | awk '{print $NF}' | sort) \
     <(grep -o 'export \(async \)\?function [A-Za-z]*' src/lib/trackLog.web.ts | awk '{print $NF}' | sort)
```

Expected: 출력 없음 (차이 없음). 차이가 나오면 빠진 export를 채운다.

- [ ] **Step 4: 타입 검사와 웹 빌드**

Run: `npx tsc --noEmit && npx expo export --platform web --output-dir /tmp/etavia-web-check`
Expected: 둘 다 성공. `expo-file-system is not supported on web` 경고가 **사라져야 한다**

- [ ] **Step 5: 커밋**

```bash
git add src/lib/prefs.web.ts src/lib/trackLog.web.ts
git commit -m "feat(web): 설정은 localStorage 로, 추적 로그는 no-op 으로"
```

---

## Task 4: 웹 shim — 알림

`notifications.ts:13`이 **모듈 최상단에서** `Notifications.setNotificationHandler(...)`를 부른다. `App.tsx:16`의 `import './src/notifications'`가 그걸 실행한다. 그리고 `App.tsx:10`이 `expo-notifications`를 **직접** import해 `NotificationBridge`에서 리스너를 단다 — `notifications.web.ts`만으로는 이 경로가 안 막힌다.

**Files:**
- Create: `src/notifications.web.ts`, `src/NotificationBridge.tsx`, `src/NotificationBridge.web.tsx`
- Modify: `App.tsx`

**Interfaces:**
- Consumes: `usePlan` (`src/state/plan.tsx`), `RootStackParamList` (`App.tsx`), `ACTION_OPEN_LEG`·`ACTION_OPEN_TASKS` (`src/notifications.ts`)
- Produces: `NotificationBridge({ navigationRef }: { navigationRef: NavigationContainerRef<RootStackParamList> }): JSX.Element | null`

- [ ] **Step 1: `notifications.web.ts`를 쓴다**

원본의 export 12개(상수 5 + 함수 7)를 모두 덮는다.

```ts
/** 웹 알림 — 전부 no-op. 데모는 핵심 한 줄기(A1→A6)만 보여주므로 알림 경로가 없다. */

export const CONGESTION_CATEGORY = 'congestion-report';
export const ARRIVAL_CATEGORY = 'stop-arrival';
export const NEXT_LEG_CATEGORY = 'next-leg';
export const ACTION_OPEN_TASKS = 'open-tasks';
export const ACTION_OPEN_LEG = 'open-leg';

export function ensureNotificationsReady(): Promise<boolean> {
  return Promise.resolve(false);
}

export async function scheduleDepartureReminder(): Promise<string | null> {
  return null;
}

export async function notifyArrival(): Promise<void> {}
export async function notifyDestinationArrival(): Promise<void> {}
export async function notifyNextLeg(): Promise<void> {}
export async function cancelScheduled(): Promise<void> {}
export async function notifyDeadlineRisk(): Promise<void> {}
export async function scheduleThanksNotification(): Promise<void> {}
```

- [ ] **Step 2: 원본과 export 목록이 같은지 확인한다**

Run:

```bash
diff <(grep -o 'export \(async function\|function\|const\) [A-Za-z_]*' src/notifications.ts | awk '{print $NF}' | sort) \
     <(grep -o 'export \(async function\|function\|const\) [A-Za-z_]*' src/notifications.web.ts | awk '{print $NF}' | sort)
```

Expected: 출력 없음

- [ ] **Step 3: 브리지를 `App.tsx`에서 빼낸다**

`src/NotificationBridge.tsx` — `App.tsx:68-97`을 그대로 옮기되 `navigationRef`를 prop으로 받는다:

```tsx
/**
 * 알림 응답 → 화면 전환. App.tsx 에 있던 것을 플랫폼 분기가 가능하게 빼냈다.
 *
 * 웹에는 .web.tsx 가 대신 들어간다 — App.tsx 가 expo-notifications 를 직접 import 하면
 * notifications.web.ts 를 만들어도 그 경로가 안 막힌다.
 */
import React from 'react';
import * as Notifications from 'expo-notifications';
import type { NavigationContainerRef } from '@react-navigation/native';
import { usePlan } from './state/plan';
import { ACTION_OPEN_LEG, ACTION_OPEN_TASKS } from './notifications';
import type { RootStackParamList } from '../App';

export function NotificationBridge({ navigationRef }: { navigationRef: NavigationContainerRef<RootStackParamList> }) {
  const { setCongestionReport } = usePlan();
  React.useEffect(() => {
    const sub = Notifications.addNotificationResponseReceivedListener(response => {
      const action = response.actionIdentifier;
      if (['low', 'mid', 'high', 'veryhigh'].includes(action)) {
        setCongestionReport(action);
        return;
      }
      /*
        도착·출발 알림에서 들어온 경우 — 진행중으로 보내되 필요한 시트까지 열어준다.
        알림을 눌렀는데 홈이 뜨면 다시 두세 번 눌러야 한다.
      */
      const data = (response.notification.request.content.data ?? {}) as {
        screen?: string;
        stopId?: string;
      };
      if (data.screen !== 'today' || !navigationRef.isReady()) return;
      if (action === ACTION_OPEN_LEG) {
        navigationRef.navigate('Today', { sheet: 'mapapp' });
      } else if (action === ACTION_OPEN_TASKS && data.stopId) {
        navigationRef.navigate('Today', { sheet: 'task', stopId: data.stopId });
      } else {
        navigationRef.navigate('Today', data.stopId ? { sheet: 'task', stopId: data.stopId } : {});
      }
    });
    return () => sub.remove();
  }, [setCongestionReport, navigationRef]);
  return null;
}
```

- [ ] **Step 4: 웹 브리지를 쓴다**

`src/NotificationBridge.web.tsx`:

```tsx
/** 웹에는 알림 경로가 없다. expo-notifications 를 import 하지 않는 것이 이 파일의 요점이다. */
export function NotificationBridge(_props: { navigationRef: unknown }) {
  return null;
}
```

- [ ] **Step 5: `App.tsx`를 고친다**

세 곳이다.

1. 10행 `import * as Notifications from 'expo-notifications';` **삭제**
2. 14행 `import { ACTION_OPEN_LEG, ACTION_OPEN_TASKS } from './src/notifications';` **삭제** (브리지로 옮겨갔다)
3. 68-97행 `function NotificationBridge() { ... }` **삭제**하고, 대신 import 블록에 추가:

```ts
import { NotificationBridge } from './src/NotificationBridge';
```

4. 117행 `<NotificationBridge />` → `<NotificationBridge navigationRef={navigationRef} />`

16행 `import './src/notifications';`는 **남긴다** — 웹에서는 `notifications.web.ts`가 대신 읽히고, 그 파일에는 최상단 부작용이 없다.

- [ ] **Step 6: 타입 검사와 웹 빌드**

Run: `npx tsc --noEmit && npx expo export --platform web --output-dir /tmp/etavia-web-check`
Expected: 둘 다 성공. `[expo-notifications] Listening to push token changes...` 경고가 **사라져야 한다**

- [ ] **Step 7: 커밋**

```bash
git add src/notifications.web.ts src/NotificationBridge.tsx src/NotificationBridge.web.tsx App.tsx
git commit -m "feat(web): 알림 경로를 웹에서 끊는다 — App.tsx 직접 import 가 남아 있었다"
```

---

## Task 5: 웹 위치

두 가지를 동시에 푼다.

1. `currentPlace.ts`가 `Location.reverseGeocodeAsync`를 부르는데 **웹에서 지원되지 않는다.** 그리고 권한을 거부하면 `status: 'denied'`로 A1이 막힌다.
2. A6 진입이 `confirmPlan()`을 부르고(`TimelineScreen.tsx:101`) `tracker.tsx:390-408`이 `live` 추적을 자동으로 켠다. 심사자가 A6에 들어가는 순간 위치 권한 팝업이 **또** 뜬다.

**Files:**
- Create: `src/lib/currentPlace.web.ts`
- Modify: `src/state/tracker.tsx` (import 1줄 + 393행 가드)

**Interfaces:**
- Consumes: `LatLng` (`src/data/mockData.ts`)
- Produces: `currentPlace.web.ts`가 `CurrentPlaceStatus`·`CurrentPlace`·`formatAddress`·`formatShortAddress`·`refreshCurrentPlace`·`refreshIfStale`·`useCurrentPlace`를 원본과 같은 시그니처로 내보낸다

- [ ] **Step 1: 원본의 export 목록을 확인한다**

Run: `grep -n '^export' src/lib/currentPlace.ts`
Expected: `CurrentPlaceStatus`, `CurrentPlace`, `formatAddress`, `formatShortAddress`, `refreshCurrentPlace`, `refreshIfStale`, `useCurrentPlace` — 이 목록이 다음 단계의 계약이다

- [ ] **Step 2: `currentPlace.web.ts`를 쓴다**

```ts
/**
 * 웹 현재 위치 — 브라우저 geolocation. 역지오코딩은 없다.
 *
 * expo-location 의 reverseGeocodeAsync 는 iOS 내장 지오코더를 쓰므로 웹에서 지원되지 않는다.
 * 주소는 사람이 읽으라고 만드는 것이라 없어도 좌표 기반 동작은 그대로다.
 *
 * 권한을 거부하거나 콜백이 안 오면 **데모 출발지**로 간다. 심사자가 데스크톱에서
 * 권한 팝업을 거부할 확률은 낮지 않고, 거기서 A1 이 막히면 데모가 시작도 못 한다.
 * timeout 을 명시하지 않으면 콜백이 영영 안 와서 좌표가 null 로 남는다.
 *
 * 폴백으로 갔으면 화면이 그렇게 말한다 — 실제 위치처럼 보이면 오작동으로 읽힌다.
 */
import { useEffect, useSyncExternalStore } from 'react';
import { LatLng } from '../data/mockData';

export type CurrentPlaceStatus = 'idle' | 'loading' | 'ready' | 'denied' | 'error';

export type CurrentPlace = {
  status: CurrentPlaceStatus;
  coord: LatLng | null;
  address: string | null;
  shortAddress: string | null;
  area: string | null;
};

/** 여의나루역 — mockData.ts 의 HOME. 목 데이터셋이 여의도↔목동 축으로 짜여 있다 */
const DEMO_ORIGIN: LatLng = { latitude: 37.5271, longitude: 126.9327 };
const DEMO_PLACE: CurrentPlace = {
  status: 'ready',
  coord: DEMO_ORIGIN,
  address: '서울 영등포구 여의동로 (데모 출발지)',
  shortAddress: '데모 출발지 · 여의나루역',
  area: '영등포구',
};

/* 원본과 export 목록을 맞추기 위한 자리. 웹에는 지오코딩 결과가 없다.
   원본은 LocationGeocodedAddress 를 받지만 currentPlace.ts 밖에서 부르는 곳이
   하나도 없으므로(확인함) 인자를 받지 않아도 깨지지 않는다. */
export function formatAddress(): string {
  return '';
}

export function formatShortAddress(): string | null {
  return null;
}

let snapshot: CurrentPlace = { status: 'idle', coord: null, address: null, shortAddress: null, area: null };
const listeners = new Set<() => void>();

function set(next: Partial<CurrentPlace>) {
  snapshot = { ...snapshot, ...next };
  listeners.forEach(l => l());
}

let inflight: Promise<void> | null = null;
let fetchedAt = 0;
const STALE_MS = 60_000;
/** 5초. runPlan 의 전체 예산이 12초라 여기서 오래 끌면 계획이 타임아웃으로 죽는다 */
const GEO_TIMEOUT_MS = 5000;

export function refreshCurrentPlace(): Promise<void> {
  if (inflight) return inflight;
  inflight = (async () => {
    set({ status: 'loading' });
    try {
      const coord = await new Promise<LatLng>((resolve, reject) => {
        if (!globalThis.navigator?.geolocation) return reject(new Error('geolocation 없음'));
        globalThis.navigator.geolocation.getCurrentPosition(
          pos => resolve({ latitude: pos.coords.latitude, longitude: pos.coords.longitude }),
          err => reject(err),
          { enableHighAccuracy: false, timeout: GEO_TIMEOUT_MS, maximumAge: 60_000 },
        );
      });
      fetchedAt = Date.now();
      set({ status: 'ready', coord, address: null, shortAddress: '내 위치', area: null });
    } catch {
      // 거부·타임아웃·미지원 — 전부 데모 출발지로. 여기서 막히면 데모가 시작도 못 한다
      fetchedAt = Date.now();
      set(DEMO_PLACE);
    } finally {
      inflight = null;
    }
  })();
  return inflight;
}

export function refreshIfStale(): void {
  if (Date.now() - fetchedAt > STALE_MS) void refreshCurrentPlace();
}

const subscribe = (listener: () => void) => {
  listeners.add(listener);
  return () => {
    listeners.delete(listener);
  };
};

export function useCurrentPlace(): CurrentPlace {
  const value = useSyncExternalStore(subscribe, () => snapshot);
  useEffect(() => {
    if (snapshot.status === 'idle') void refreshCurrentPlace();
  }, []);
  return value;
}
```

- [ ] **Step 3: export 목록이 같은지 확인한다**

Run:

```bash
diff <(grep -o '^export \(async \)\?\(function\|type\|const\) [A-Za-z]*' src/lib/currentPlace.ts | awk '{print $NF}' | sort) \
     <(grep -o '^export \(async \)\?\(function\|type\|const\) [A-Za-z]*' src/lib/currentPlace.web.ts | awk '{print $NF}' | sort)
```

Expected: 출력 없음

- [ ] **Step 4: 웹에서 `live` 추적을 막는다**

`src/state/tracker.tsx` 17행의 React import 줄 **아래**에 추가:

```ts
import { Platform } from 'react-native';
```

393행 `if (!state.planConfirmed || autoStartedRef.current) return;`를 이렇게 바꾼다:

```ts
    /* 웹 데모에는 지오펜스가 없다. 여기를 막지 않으면 A6 에 들어가는 순간
       위치 권한 팝업이 또 뜬다 — A1 에서 이미 한 번 물었다. */
    if (Platform.OS === 'web') return;
    if (!state.planConfirmed || autoStartedRef.current) return;
```

- [ ] **Step 5: `backgroundLocation.web.ts`를 쓴다**

`toFix`는 **no-op으로 만들면 안 된다** — `tracker.tsx:455,466`이 전경 GPS 변환에 쓴다. 추적만 끈다. 그리고 원본 32행의 `TaskManager.defineTask`는 모듈이 읽히는 순간 실행되는 부작용이라, 이 shim이 그걸 없앤다.

```ts
/**
 * 웹 배경 위치 — 추적은 no-op, 변환은 살린다.
 *
 * 통째로 no-op 하면 안 된다: toFix 는 tracker.tsx 가 **전경** GPS 에도 쓴다.
 * 그리고 원본은 모듈 최상단에서 TaskManager.defineTask 를 부른다 — 웹에서는
 * 그 부작용 자체가 없어야 한다.
 */
import type { Fix } from './arrival';

export const LOCATION_TASK = 'etavia-location-updates';

export const getLastBackgroundPosition = (): Fix | null => null;

export function subscribeBackgroundLocation(_listener: (fix: Fix) => void) {
  return () => {};
}

export type BackgroundStartResult = 'started' | 'denied' | 'unavailable';

export async function startBackgroundLocation(): Promise<BackgroundStartResult> {
  return 'unavailable';
}

export async function stopBackgroundLocation(): Promise<void> {}

/** 네이티브와 같은 순수 변환. iOS 는 속도를 모르면 -1 을 준다 */
export function toFix(loc: { coords: { latitude: number; longitude: number; accuracy?: number | null; speed?: number | null } }): Fix {
  const { latitude, longitude, accuracy, speed } = loc.coords;
  return {
    latitude,
    longitude,
    accuracyM: accuracy ?? null,
    speedMps: speed == null || speed < 0 ? null : speed,
  };
}
```

- [ ] **Step 6: 타입 검사, 테스트, 웹 빌드**

Run: `npx tsc --noEmit && npm test && npx expo export --platform web --output-dir /tmp/etavia-web-check`
Expected: 셋 다 성공

- [ ] **Step 7: 커밋**

```bash
git add src/lib/currentPlace.web.ts src/lib/backgroundLocation.web.ts src/state/tracker.tsx
git commit -m "feat(web): 위치는 브라우저에서 한 번만 묻는다 — 거부하면 여의나루역"
```

---

## Task 6: 웹 UI — 탭바와 폰 프레임

`TabBar`는 9개 화면에서 쓰인다(`HomeScreen.tsx:261` 등). `TabBar.web.tsx`가 `null`을 반환하면 아홉 군데를 안 고쳐도 된다. 진행중·주변·기록으로 가는 입구가 사라지므로 미완성 화면에 심사자가 빠지지 않는다.

**Files:**
- Create: `src/components/TabBar.web.tsx`, `src/webFrame.css`
- Modify: `index.ts`

**Interfaces:**
- Produces: `TabBar(): null`

- [ ] **Step 1: `TabBar.web.tsx`를 쓴다**

```tsx
/**
 * 웹 데모에는 탭바를 안 단다.
 *
 * 데모 범위는 핵심 한 줄기(A1→A2→A3→A5→A6)다. 탭바를 남기면 진행중·주변·기록으로
 * 갈 수 있는데(TabBar.tsx:80 이 실제로 이동한다) 그 화면들은 데모 범위 밖이다.
 * 눌렀더니 미완성 화면이 나오면 나머지 화면도 못 믿게 된다.
 *
 * 부르는 쪽 9곳(HomeScreen·PlanScreen·OptionsScreen·TimelineScreen …)은 손대지 않는다.
 */
export function TabBar() {
  return null;
}
```

- [ ] **Step 2: 폰 프레임 CSS를 쓴다**

`src/webFrame.css` — **데스크톱에만 적용한다.** 모바일 브라우저에 420px 고정 높이를 씌우면 주소창·소프트 키보드 때문에 Sheet 입력이 잘린다.

```css
/* 웹 데모 프레임. 데스크톱 폭에서 화면이 가로로 늘어지는 것만 막는다.
   모바일 브라우저는 건드리지 않는다 — 고정 높이를 씌우면 주소창과
   소프트 키보드 때문에 하단 입력창이 잘린다. */

html, body, #root {
  height: 100%;
  margin: 0;
  background: #f2f3f5;
}

@media (min-width: 768px) {
  body {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  #root {
    width: 420px;
    height: min(900px, 96vh);
    overflow: hidden;
    border-radius: 28px;
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.18);
    background: #fff;
  }
}
```

- [ ] **Step 3: `index.ts`에서 CSS를 읽는다**

`index.ts` 전체를 이걸로 바꾼다:

```ts
import { registerRootComponent } from 'expo';

import App from './App';

/* 웹 전용 프레임. document 가 있을 때만 읽는다 — 네이티브 번들에는 .css 로더가 없다 */
if (typeof document !== 'undefined') require('./src/webFrame.css');

// registerRootComponent calls AppRegistry.registerComponent('main', () => App);
// It also ensures that whether you load the app in Expo Go or in a native build,
// the environment is set up appropriately
registerRootComponent(App);
```

- [ ] **Step 4: 웹 빌드 후 데스크톱·모바일 폭에서 눈으로 본다**

```bash
npx expo export --platform web --output-dir /tmp/etavia-web-check
npx serve /tmp/etavia-web-check -l 8080   # 또는 python3 -m http.server 8080
```

Chrome에서 `http://localhost:8080` 확인:
- 데스크톱 폭: 420px 프레임이 가운데. 탭바 없음
- DevTools 기기 모드(iPhone): 전체 폭, 프레임 없음, 하단 입력창이 안 잘림

- [ ] **Step 5: 커밋**

```bash
git add src/components/TabBar.web.tsx src/webFrame.css index.ts
git commit -m "feat(web): 탭바를 빼고 데스크톱에만 폰 프레임을 씌운다"
```

---

## Task 7: 첫 배포 — Kakao 키 없이

**이 시점에 배포하는 이유**: 배포 배선(env 누락·CORS origin 오타)은 로컬에서 안 드러난다. 발견이 하루 늦어지면 검증할 시간이 사라진다. 그리고 Kakao 키를 아직 안 실으므로 **키가 공개 번들에 노출되는 순간이 없다** — 장소 검색은 Task 2의 폴백 덕에 목 카탈로그로 돈다.

**Files:**
- Modify: `server/wrangler.toml:8`

- [ ] **Step 1: 웹 빌드를 만든다**

`wrangler pages deploy`는 이미 만들어진 산출물을 올리므로 빌드는 **로컬 Node**로 돈다. TestFlight 워크플로와 같은 24로 맞춘다.

```bash
node --version    # v24.x 여야 한다
npx expo export --platform web --output-dir dist
```

`SERVER_URL`·`APP_TOKEN`은 `.env`에서 `app.config.js:11`이 주입한다. `KAKAO_REST_KEY`·`GOOGLE_PLACES_KEY`는 **빈 값이어야 한다.**

- [ ] **Step 2: 키가 번들에 안 들어갔는지 확인한다**

Run:

```bash
grep -o 'KakaoAK [A-Za-z0-9]\{8\}' dist/_expo/static/js/web/*.js || echo "카카오 키 없음 — OK"
grep -c 'X-Goog-Api-Key' dist/_expo/static/js/web/*.js
```

Expected: 첫 줄은 `카카오 키 없음 — OK`. 두 번째는 헤더 **이름**만 잡히므로 0이 아니어도 되지만, 실제 키 값이 없어야 한다. 의심되면 `grep -o 'AIza[A-Za-z0-9_-]\{35\}' dist/_expo/static/js/web/*.js`로 확인하고 결과가 없어야 한다.

- [ ] **Step 3: Cloudflare Pages에 배포한다**

```bash
npx wrangler pages deploy dist --project-name etavia-demo
```

출력된 production URL을 기록한다.

- [ ] **Step 4: `ALLOWED_ORIGINS`에 그 오리진을 추가한다**

`server/wrangler.toml:8`:

```toml
ALLOWED_ORIGINS = "http://localhost:8080,http://localhost:8081,https://etavia-demo.pages.dev"
```

> 와일드카드로 열지 않는다(파일 상단 주석의 약속). 프리뷰 배포 도메인도 넣지 않는다 — 제출 URL은 production 하나다.

- [ ] **Step 5: Workers를 재배포한다**

```bash
cd server && npx wrangler deploy && cd ..
```

- [ ] **Step 6: CORS가 실제로 먹는지 확인한다**

Worker 주소는 `.env`의 `SERVER_URL`이다 (`app.config.js:14`가 그 값을 앱에 주입한다). 셸에 올려 쓴다:

```bash
WORKER=$(grep -E '^SERVER_URL=' .env | cut -d= -f2- | tr -d '"'"'"' ')
echo "$WORKER"   # 비어 있으면 여기서 멈춘다 — 아래가 전부 무의미해진다

curl -s -o /dev/null -D - -X OPTIONS "$WORKER/route" \
  -H "Origin: https://etavia-demo.pages.dev" \
  -H "Access-Control-Request-Method: POST" | grep -i 'HTTP/\|access-control'

curl -s -o /dev/null -D - -X OPTIONS "$WORKER/route" \
  -H "Origin: https://evil.test" \
  -H "Access-Control-Request-Method: POST" | grep -ci 'access-control'
```

Expected: 첫 번째는 `204` + `access-control-allow-origin` 헤더. 두 번째는 `0`

- [ ] **Step 7: 시크릿 모드로 접속한다**

Chrome 시크릿 창에서 production URL을 연다. A1이 뜨고, 위치 팝업을 **거부**해도 출발지가 `데모 출발지 · 여의나루역`으로 채워져야 한다.

- [ ] **Step 8: 커밋**

```bash
git add server/wrangler.toml
git commit -m "chore(deploy): 웹 데모 오리진을 CORS 허용 목록에 넣는다"
```

---

## Task 8: Workers `/places` 프록시 (서버)

`server/src/kakao.ts`는 **모빌리티 자동차 길찾기 전용**이고 Local 장소 검색이 없다. 키도 `KAKAO_MOBILITY_KEY`와 별개다.

**설계 판단 — 얇은 프록시로 간다.** 검색어 정규화(`planSearch`)와 업종 필터(`keepPlace`)는 `src/lib/placeQuery.ts`의 순수 함수이고 이미 테스트가 있다. 그걸 서버로 옮기면 3일 안에 테스트까지 다시 짜야 한다. 그래서 **서버는 키를 들고 Kakao 두 엔드포인트를 그대로 중계**하고, 정규화·필터는 클라이언트에 남긴다. 목표(키를 번들에서 빼고 `guard` 상한 안으로 넣기)는 그대로 달성된다.

**Files:**
- Create: `server/src/placesSchema.ts`, `server/src/placesSchema.test.ts`, `server/src/places.ts`, `server/src/places.test.ts`
- Modify: `server/src/index.ts:19-42`(Env), `:126`(known), `:131`(분기), `server/src/guard.ts:31,35,56`

**Interfaces:**
- Produces:
  - `parsePlacesRequest(raw: unknown): PlacesRequest | null` where `PlacesRequest = { kind: 'keyword' | 'address'; query: string; x?: number; y?: number; radius?: number; size: number; categoryCode?: string; sortByDistance: boolean }`
  - `handlePlaces(body: unknown, env: Env, deps: { fetch: typeof fetch }): Promise<Response>`
  - `placesCacheKey(req: PlacesRequest): string`

- [ ] **Step 1: 스키마 테스트를 쓴다**

`server/src/placesSchema.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { parsePlacesRequest } from './placesSchema';

test('정상 — keyword, 좌표·반지름 포함', () => {
  const r = parsePlacesRequest({ kind: 'keyword', query: '올리브영', x: 126.9327, y: 37.5271, radius: 1500, size: 15 });
  assert.deepEqual(r, { kind: 'keyword', query: '올리브영', x: 126.9327, y: 37.5271, radius: 1500, size: 15, categoryCode: undefined, sortByDistance: true });
});

test('좌표가 없으면 거리순 정렬을 끈다', () => {
  const r = parsePlacesRequest({ kind: 'address', query: '여의도동 12', size: 5 });
  assert.equal(r?.sortByDistance, false);
  assert.equal(r?.x, undefined);
});

test('kind 가 둘 중 하나가 아니면 거절', () => {
  assert.equal(parsePlacesRequest({ kind: 'category', query: 'x', size: 5 }), null);
});

test('빈 검색어·40자 초과는 거절', () => {
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: '   ', size: 5 }), null);
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: 'ㄱ'.repeat(41), size: 5 }), null);
});

test('한국 밖 좌표는 거절 — 이 프록시는 카카오 로컬 전용이다', () => {
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: 'x', x: 139.7, y: 35.6, size: 5 }), null);
});

test('size 는 1~15 로 자른다', () => {
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: 'x', size: 99 })?.size, 15);
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: 'x', size: 0 })?.size, 1);
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: 'x' })?.size, 15);
});

test('radius 는 카카오 상한 20km 로 자른다', () => {
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: 'x', x: 126.9, y: 37.5, radius: 99999, size: 5 })?.radius, 20000);
});

test('categoryCode 는 대문자·숫자 3자리만 받는다', () => {
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: 'x', size: 5, categoryCode: 'PK6' })?.categoryCode, 'PK6');
  assert.equal(parsePlacesRequest({ kind: 'keyword', query: 'x', size: 5, categoryCode: '../../etc' }), null);
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `npx tsx --test server/src/placesSchema.test.ts`
Expected: FAIL — `Cannot find module './placesSchema'`

- [ ] **Step 3: 스키마를 구현한다**

`server/src/placesSchema.ts`:

```ts
/**
 * /places 요청 검증. 앱이 보내는 값도 믿지 않는다 — 인젝션 방어선은 프롬프트가 아니라 스키마다.
 *
 * 이 프록시는 카카오 로컬 전용이라 좌표를 한국 bbox 로 가둔다.
 * 검색어 정규화(planSearch)와 업종 필터(keepPlace)는 클라이언트에 남는다 —
 * src/lib/placeQuery.ts 의 순수 함수이고 이미 테스트가 있다.
 */
export type PlacesRequest = {
  kind: 'keyword' | 'address';
  query: string;
  x?: number;
  y?: number;
  radius?: number;
  size: number;
  categoryCode?: string;
  sortByDistance: boolean;
};

/** src/lib/places.ts 의 KR_BBOX 와 같은 범위 */
const KR_BBOX = { minLat: 33.0, maxLat: 38.7, minLng: 124.5, maxLng: 132.0 };
const MAX_QUERY = 40;
const MAX_SIZE = 15;
/** 카카오 로컬 radius 상한 */
const MAX_RADIUS = 20000;

const clamp = (n: number, lo: number, hi: number) => Math.min(hi, Math.max(lo, n));

export function parsePlacesRequest(raw: unknown): PlacesRequest | null {
  if (!raw || typeof raw !== 'object') return null;
  const r = raw as Record<string, unknown>;

  if (r.kind !== 'keyword' && r.kind !== 'address') return null;

  if (typeof r.query !== 'string') return null;
  const query = r.query.trim();
  if (!query || query.length > MAX_QUERY) return null;

  let x: number | undefined;
  let y: number | undefined;
  if (r.x !== undefined || r.y !== undefined) {
    const nx = Number(r.x);
    const ny = Number(r.y);
    if (!Number.isFinite(nx) || !Number.isFinite(ny)) return null;
    if (ny < KR_BBOX.minLat || ny > KR_BBOX.maxLat || nx < KR_BBOX.minLng || nx > KR_BBOX.maxLng) return null;
    x = nx;
    y = ny;
  }

  let radius: number | undefined;
  if (r.radius !== undefined) {
    const n = Number(r.radius);
    if (!Number.isFinite(n) || n <= 0) return null;
    radius = Math.round(clamp(n, 1, MAX_RADIUS));
  }

  const size = r.size === undefined ? MAX_SIZE : Math.round(clamp(Number(r.size) || 1, 1, MAX_SIZE));

  let categoryCode: string | undefined;
  if (r.categoryCode !== undefined) {
    if (typeof r.categoryCode !== 'string' || !/^[A-Z]{2}\d$/.test(r.categoryCode)) return null;
    categoryCode = r.categoryCode;
  }

  return { kind: r.kind, query, x, y, radius, size, categoryCode, sortByDistance: x !== undefined };
}
```

- [ ] **Step 4: 스키마 테스트가 통과하는지 확인한다**

Run: `npx tsx --test server/src/placesSchema.test.ts`
Expected: PASS — 8 tests

- [ ] **Step 5: 핸들러 테스트를 쓴다**

`server/src/places.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { handlePlaces, kakaoLocalUrl, placesCacheKey } from './places';
import type { PlacesRequest } from './placesSchema';

const req = (over: Partial<PlacesRequest> = {}): PlacesRequest => ({
  kind: 'keyword', query: '올리브영', x: 126.9327, y: 37.5271, radius: 1500, size: 15,
  categoryCode: undefined, sortByDistance: true, ...over,
});

const kv = () => {
  const store = new Map<string, string>();
  return {
    store,
    async get(k: string) { return store.get(k) ?? null; },
    async put(k: string, v: string) { store.set(k, v); },
  };
};

const env = (over: Record<string, unknown> = {}) => ({ KAKAO_LOCAL_KEY: 'test-key', CACHE: kv(), RATE: kv(), ...over }) as never;

test('URL 조립 — 좌표가 있으면 거리순, radius 를 싣는다', () => {
  const u = new URL(kakaoLocalUrl(req()));
  assert.equal(u.pathname, '/v2/local/search/keyword.json');
  assert.equal(u.searchParams.get('query'), '올리브영');
  assert.equal(u.searchParams.get('sort'), 'distance');
  assert.equal(u.searchParams.get('radius'), '1500');
  assert.equal(u.searchParams.get('size'), '15');
});

test('좌표가 없으면 sort·radius 를 안 싣는다', () => {
  const u = new URL(kakaoLocalUrl(req({ x: undefined, y: undefined, radius: undefined, sortByDistance: false })));
  assert.equal(u.searchParams.get('sort'), null);
  assert.equal(u.searchParams.get('radius'), null);
});

test('address 는 다른 엔드포인트', () => {
  assert.match(kakaoLocalUrl(req({ kind: 'address' })), /\/v2\/local\/search\/address\.json/);
});

test('성공 응답을 documents 로 넘기고 캐시에 넣는다', async () => {
  const e = env();
  const fetchFn = async () => new Response(JSON.stringify({ documents: [{ id: '1', place_name: '올리브영 여의도IFC점' }] }), { status: 200 });
  const res = await handlePlaces(req(), e, { fetch: fetchFn as typeof fetch });
  assert.equal(res.status, 200);
  assert.equal((await res.json() as { documents: unknown[] }).documents.length, 1);
  assert.equal(e.CACHE.store.size, 1);
});

test('캐시에 있으면 바깥을 안 부른다', async () => {
  const e = env();
  e.CACHE.store.set(placesCacheKey(req()), JSON.stringify({ documents: [{ id: 'cached' }] }));
  let called = 0;
  const fetchFn = async () => { called++; return new Response('{}', { status: 200 }); };
  const res = await handlePlaces(req(), e, { fetch: fetchFn as typeof fetch });
  assert.equal(called, 0);
  assert.equal((await res.json() as { documents: { id: string }[] }).documents[0].id, 'cached');
});

test('카카오가 실패하면 502 — 실패는 캐시하지 않는다', async () => {
  const e = env();
  const fetchFn = async () => new Response('nope', { status: 500 });
  const res = await handlePlaces(req(), e, { fetch: fetchFn as typeof fetch });
  assert.equal(res.status, 502);
  assert.equal(e.CACHE.store.size, 0);
});

test('키가 없으면 501 — 키 없음과 상류 장애를 섞지 않는다', async () => {
  const res = await handlePlaces(req(), env({ KAKAO_LOCAL_KEY: '' }), { fetch: (async () => new Response('{}')) as typeof fetch });
  assert.equal(res.status, 501);
});

test('캐시 키는 좌표를 격자로 깎는다 — 11m 차이로 캐시가 갈리면 안 된다', () => {
  assert.equal(placesCacheKey(req({ y: 37.52710 })), placesCacheKey(req({ y: 37.52712 })));
  assert.notEqual(placesCacheKey(req({ query: '올리브영' })), placesCacheKey(req({ query: '파리바게뜨' })));
});
```

- [ ] **Step 6: 핸들러 테스트가 실패하는지 확인한다**

Run: `npx tsx --test server/src/places.test.ts`
Expected: FAIL — `Cannot find module './places'`

- [ ] **Step 7: 핸들러를 구현한다**

`server/src/places.ts`:

```ts
/**
 * 카카오 로컬 장소 검색 프록시 — 키를 가리고 바깥 응답을 중계한다.
 *
 * 왜 필요한가: src/lib/places.ts 가 dapi.kakao.com 을 클라이언트에서 직접 불렀다.
 * guard 를 안 거치고, 카카오 REST 키는 도메인 제한이 안 걸린다 — 공개 웹 번들에
 * 박히면 누구나 무제한으로 쓴다. 키 회전은 해결책이 아니다(새 키도 똑같이 박힌다).
 *
 * 얇게 간다. 검색어 정규화(planSearch)·업종 필터(keepPlace)는 클라이언트에 남는다 —
 * 순수 함수이고 테스트가 이미 있다. 여기서는 키·상한·캐시만 맡는다.
 *
 * KAKAO_LOCAL_KEY 는 developers.kakao.com 키다. KAKAO_MOBILITY_KEY(길찾기)와 다르다.
 */
import type { PlacesRequest } from './placesSchema';

export const KAKAO_LOCAL_BASE = 'https://dapi.kakao.com';

/** 장소 검색은 길찾기보다 훨씬 덜 변한다. 하루면 충분하다 */
export const PLACES_TTL_S = 24 * 60 * 60;

export function kakaoLocalUrl(req: PlacesRequest, base = KAKAO_LOCAL_BASE): string {
  const params = new URLSearchParams({ query: req.query, size: String(req.size) });
  if (req.categoryCode) params.set('category_group_code', req.categoryCode);
  if (req.x !== undefined && req.y !== undefined) {
    params.set('x', String(req.x));
    params.set('y', String(req.y));
    if (req.sortByDistance) params.set('sort', 'distance');
    if (req.radius !== undefined) params.set('radius', String(req.radius));
  }
  return `${base}/v2/local/search/${req.kind}.json?${params}`;
}

/** 좌표는 4자리(약 11m)로 깎는다. routeCacheKey 와 같은 규칙 */
export function placesCacheKey(req: PlacesRequest): string {
  const g = (n: number | undefined) => (n === undefined ? '-' : n.toFixed(4));
  return `places:${req.kind}:${req.query}:${g(req.x)},${g(req.y)}:${req.radius ?? '-'}:${req.size}:${req.categoryCode ?? '-'}`;
}

type PlacesEnv = {
  KAKAO_LOCAL_KEY: string;
  CACHE: { get(k: string): Promise<string | null>; put(k: string, v: string, o?: { expirationTtl: number }): Promise<void> };
};

const JSON_HEADERS = { 'content-type': 'application/json; charset=utf-8' };
const json = (body: unknown, status = 200) => new Response(JSON.stringify(body), { status, headers: JSON_HEADERS });

export async function handlePlaces(
  req: PlacesRequest,
  env: PlacesEnv,
  deps: { fetch: typeof fetch },
): Promise<Response> {
  // 키 없음과 상류 장애를 섞지 않는다 — 섞으면 배포 사고를 장애로 오진한다
  if (!env.KAKAO_LOCAL_KEY) return json({ error: 'places unavailable' }, 501);

  const key = placesCacheKey(req);
  const hit = await env.CACHE.get(key);
  if (hit) return new Response(hit, { headers: JSON_HEADERS });

  const res = await deps.fetch(kakaoLocalUrl(req), {
    headers: { Authorization: `KakaoAK ${env.KAKAO_LOCAL_KEY}` },
  });
  if (!res.ok) return json({ error: 'upstream', status: res.status }, 502);

  let raw: unknown;
  try {
    raw = await res.json();
  } catch {
    return json({ error: 'unparseable' }, 502);
  }
  const documents = (raw as { documents?: unknown[] })?.documents ?? [];
  const body = JSON.stringify({ documents });
  // 성공만 캐시한다. 실패를 캐시하면 일시적 장애가 TTL 내내 굳는다
  await env.CACHE.put(key, body, { expirationTtl: PLACES_TTL_S });
  return new Response(body, { headers: JSON_HEADERS });
}
```

- [ ] **Step 8: 핸들러 테스트가 통과하는지 확인한다**

Run: `npx tsx --test server/src/places.test.ts`
Expected: PASS — 8 tests

- [ ] **Step 9: `guard.ts`에 상한을 넣는다**

**기본값을 쓰면 정상 사용자 한 명도 못 쓴다.** `runPlan.ts:159` 주석대로 후보 하나당 최대 25콜이고 후보는 최대 3개다 — 슬롯 하나가 최악 75콜, 경유지 둘이면 150콜이 계획 한 건에서 나간다. `PER_MIN` 기본값 10으로는 계획 한 건도 못 끝낸다.

`server/src/guard.ts:31`:

```ts
export const PER_MIN: Record<string, number> = { '/extract': 10, '/route': 40, '/enrich': 10, '/transit': 40, '/places': 300 };
```

`:35`:

```ts
export const PER_MIN_IP: Record<string, number> = { '/extract': 30, '/route': 120, '/enrich': 30, '/transit': 120, '/places': 900 };
```

`:56` `PER_DAY`에 한 줄과 근거 주석(`:38-50` 표에 행 추가):

```ts
  '/places': 30000,
```

**값을 정하는 규칙** — 다른 `PER_DAY` 값이 전부 요금표 근거를 주석에 달고 있으므로 여기도 그래야 한다.

1. developers.kakao.com 콘솔에서 로컬 API 일일 쿼터를 확인한다
2. `PER_DAY['/places']` = 그 값의 30%
3. **확인이 안 되면 `10000`으로 두고 배포한다.** 막히면 안 되는 건 이 단계가 아니라 배포다. 계획 한 건이 최악 150콜이므로 10000이면 일 60건 이상을 받는다 — 심사 트래픽에 모자라지 않는다. 확정값은 Task 10 Step 13에서 콘솔을 볼 때 채운다
4. 정한 근거를 `guard.ts:38`의 표에 한 줄로 적는다

- [ ] **Step 10: `index.ts`에 배선한다**

`Env`(19행)에 추가:

```ts
  /** developers.kakao.com 로컬 REST 키. KAKAO_MOBILITY_KEY(길찾기)와 다르다 */
  KAKAO_LOCAL_KEY: string;
```

import에 추가:

```ts
import { handlePlaces } from './places';
import { parsePlacesRequest } from './placesSchema';
```

126행:

```ts
    const known = ['/extract', '/route', '/enrich', '/transit', '/places'];
```

131행 다음 줄에 분기 추가:

```ts
    if (url.pathname === '/places') {
      if (await overDailyCap(env.RATE, '/places', new Date())) return json({ error: 'daily cap' }, 429);
      const parsed = parsePlacesRequest(gated.body);
      if (!parsed) return json({ error: 'bad request' }, 400);
      return handlePlaces(parsed, env, { fetch });
    }
```

- [ ] **Step 11: 전체 테스트를 돌린다**

Run: `npm test`
Expected: PASS

- [ ] **Step 12: secret을 넣고 배포한다**

```bash
cd server
npx wrangler secret put KAKAO_LOCAL_KEY
npx wrangler deploy
cd ..
```

- [ ] **Step 13: 실제로 도는지 확인한다**

```bash
WORKER=$(grep -E '^SERVER_URL=' .env | cut -d= -f2- | tr -d '"'"'"' ')
TOKEN=$(grep -E '^APP_TOKEN=' .env | cut -d= -f2- | tr -d '"'"'"' ')

curl -s -X POST "$WORKER/places" \
  -H "content-type: application/json" \
  -H "x-app-token: $TOKEN" \
  -H "x-device-id: curl-check" \
  -d '{"kind":"keyword","query":"올리브영","x":126.9327,"y":37.5271,"radius":3000,"size":5}' | head -c 400
```

Expected: `{"documents":[{...place_name":"올리브영 ...`

- [ ] **Step 14: 커밋**

```bash
git add server/src/places.ts server/src/places.test.ts server/src/placesSchema.ts server/src/placesSchema.test.ts server/src/index.ts server/src/guard.ts
git commit -m "feat(server): /places — 카카오 로컬 키를 번들에서 걷어낸다"
```

---

## Task 9: 클라이언트를 `/places`로 전환

`getProvider()`가 유일한 진입점이므로 거기 하나만 갈아끼우면 `planSearchFn()`과 `DestinationSheet.tsx:86` 둘 다 따라온다.

**Files:**
- Modify: `src/lib/places.ts` (`kakaoFetch` 155-161행, `getProvider` 287행)

**Interfaces:**
- Consumes: `handlePlaces` 응답 `{ documents: KakaoKeywordDoc[] | KakaoAddressDoc[] }` (Task 8)
- Produces: 변경 없음 — `getProvider`의 반환 타입은 그대로다

- [ ] **Step 1: `kakaoFetch`를 프록시 호출로 바꾼다**

`src/lib/places.ts:155-161`을 교체한다. **호출부(163-231행)는 손대지 않는다** — 응답 모양이 같다.

```ts
/**
 * 카카오 로컬 — Workers /places 프록시를 지난다.
 *
 * 예전엔 dapi.kakao.com 을 직접 불렀다. 카카오 REST 키는 도메인 제한이 안 걸려서
 * 공개 웹 번들에 박히면 누구나 무제한으로 쓴다. 이제 키는 서버에만 있고,
 * 이 호출은 guard 의 분당·일일 상한 안으로 들어온다.
 *
 * 5초에 끊는다 — runPlan 의 전체 예산이 12초라 프록시가 느려지면 계획이 통째로 죽는다.
 * 실패는 그대로 던진다. 받아내는 건 placesFallback 의 몫이다(웹에서만).
 */
const PLACES_TIMEOUT_MS = 5000;

async function kakaoFetch(kind: 'keyword' | 'address', params: URLSearchParams) {
  const extra = (Constants.expoConfig?.extra ?? {}) as { serverUrl?: string; appToken?: string };
  const baseUrl = extra.serverUrl?.trim();
  const appToken = extra.appToken?.trim();
  if (!baseUrl || !appToken) throw new Error('places: 서버 없음');

  const num = (k: string) => (params.get(k) == null ? undefined : Number(params.get(k)));
  const ctrl = new AbortController();
  const timer = setTimeout(() => ctrl.abort(), PLACES_TIMEOUT_MS);
  try {
    const res = await fetch(`${baseUrl.replace(/\/$/, '')}/places`, {
      method: 'POST',
      headers: {
        'content-type': 'application/json',
        'x-app-token': appToken,
        'x-device-id': Constants.sessionId ?? 'unknown',
      },
      body: JSON.stringify({
        kind,
        query: params.get('query') ?? '',
        x: num('x'),
        y: num('y'),
        radius: num('radius'),
        size: num('size'),
        categoryCode: params.get('category_group_code') ?? undefined,
      }),
      signal: ctrl.signal,
    });
    if (!res.ok) throw new Error(`places ${kind} ${res.status}`);
    return res.json();
  } finally {
    clearTimeout(timer);
  }
}
```

- [ ] **Step 2: 공급자 선택이 키가 아니라 서버를 보게 바꾼다**

`kakaoRestKey`는 이제 항상 비어 있으므로 `getProvider`가 카카오를 절대 고르지 않는다. 판정 기준을 서버 유무로 바꾼다.

`src/lib/places.ts:131` 아래에 추가:

```ts
/** 서버가 있으면 카카오 로컬을 쓸 수 있다 — 키는 서버에만 있다 */
const hasServer = (): boolean => {
  const extra = (Constants.expoConfig?.extra ?? {}) as { serverUrl?: string; appToken?: string };
  return !!extra.serverUrl?.trim() && !!extra.appToken?.trim();
};
```

`getProvider`의 `pick()`에서 `kakaoRestKey` 검사를 `hasServer()`로 바꾼다:

```ts
  const pick = (): PlaceSearchProvider => {
    if (wanted === 'kakao' && hasServer()) return kakaoProvider;
    if (wanted === 'google' && googlePlacesKey) return googleProvider;
    if (hasServer()) return kakaoProvider;
    if (googlePlacesKey) return googleProvider;
    return mockProvider;
  };
```

- [ ] **Step 3: 타입 검사와 테스트**

Run: `npx tsc --noEmit && npm test`
Expected: 둘 다 성공

- [ ] **Step 4: 재배포하고 실검색을 확인한다**

```bash
npx expo export --platform web --output-dir dist
npx wrangler pages deploy dist --project-name etavia-demo
```

배포 URL에서 A1 목적지 검색창에 `올리브영`을 넣는다.
Expected: 여의도 일대 실제 매장이 나온다(목 카탈로그가 아니다). DevTools Network에 `/places` 요청이 보이고 `dapi.kakao.com` 직접 호출은 **없어야 한다**

- [ ] **Step 5: 번들에 키가 없는지 다시 확인한다**

Run: `grep -o 'KakaoAK [A-Za-z0-9]\{8\}' dist/_expo/static/js/web/*.js || echo "카카오 키 없음 — OK"`
Expected: `카카오 키 없음 — OK`

- [ ] **Step 6: 커밋**

```bash
git add src/lib/places.ts
git commit -m "feat(web): 장소 검색을 /places 프록시로 옮긴다"
```

---

## Task 10: 완주 검증

localhost는 CORS 조건이 달라 증거가 못 된다. **전부 배포 URL에서 돈다.**

**Files:** 없음 (검증만)

- [ ] **Step 1: 핵심 줄기 완주 — 권한 허용**

Chrome 시크릿 창 → 배포 URL → 위치 **허용**.
A1 목적지 지정 → `계획 만들기` → A2에 `올리브영 들러서 가고 싶어` 입력 → A3 진행 → A5 3안 → CTA → A6 타임라인.
Expected: 막힘 없이 A6까지. A5 판정 카드에 도착 시각이 나온다

- [ ] **Step 2: 핵심 줄기 완주 — 권한 거부**

시크릿 창을 새로 열고 위치 **거부**.
Expected: 출발지가 `데모 출발지 · 여의나루역`. 나머지는 Step 1과 같이 완주. **A6에서 위치 팝업이 다시 뜨지 않아야 한다**(Task 5의 가드)

- [ ] **Step 3: `/route` 강제 실패 → 목 강등**

DevTools → Network → `/route`를 Block request URL로 막고 계획을 새로 만든다.
Expected: A3가 `FAIL`로 끝나지 않는다. A5가 뜨고 판정 카드가 **"서버 없이 추정한 값이에요"** 계열 문구를 보인다

- [ ] **Step 4: `/places` 강제 실패 → 목 카탈로그 강등**

`/places`를 Block하고 목적지 검색과 계획 생성을 한다.
Expected: 검색 결과가 목 카탈로그로 나오고, 계획도 `FAIL(search)` 없이 완주

- [ ] **Step 5: `/extract` 강제 실패 → 로컬 목 강등**

`/extract`를 Block하고 A2에 문장을 넣는다.
Expected: 칩이 여전히 뽑힌다(`src/lib/intent.ts` 로컬 목). 화면이 목으로 떨어졌음을 말한다

- [ ] **Step 6: SPA 라우팅**

주소창에 `<URL>/home`, `<URL>/plan`, `<URL>/timeline`을 직접 입력하고 각각 강력 새로고침(Cmd+Shift+R).
Expected: 404 없이 해당 화면. 실패하면 Pages에 SPA 폴백 설정을 추가한다

- [ ] **Step 7: A6 드래그 재정렬 — 마우스**

경유지 2개 이상인 계획으로 A6에 들어가 카드를 **길게 눌러** 순서를 바꾼다.
Expected: 순서가 실제로 바뀌고 재계산이 돈다

> **여기서 실패하면**: 웹에서만 카드에 위/아래 이동 버튼을 붙인다(`TimelineScreen.web.tsx` 또는 `Platform.OS === 'web'` 분기). 드래그는 핵심 기능이 아니다. 반나절이면 된다

- [ ] **Step 8: A6 드래그 재정렬 — 터치**

실제 모바일 브라우저(iOS Safari / Android Chrome)에서 같은 조작.
Expected: 동작. 실패 시 Step 7의 대체안이 양쪽을 함께 해결한다

- [ ] **Step 9: Sheet 개폐**

A5 경유지 칩 → A4 후보 시트. 스크림 탭 / 드래그 / 닫기 버튼 각각으로 닫는다.
Expected: 최소한 스크림 탭 또는 닫기 버튼 중 하나는 반드시 동작해야 한다

- [ ] **Step 10: 한글 IME**

A2 채팅에 한글을 입력하고 전송한다.
Expected: 조합 중 글자가 깨지거나 중복 입력되지 않는다

- [ ] **Step 11: 모바일 브라우저 레이아웃**

모바일에서 A2 하단 입력창에 포커스를 준다.
Expected: 소프트 키보드가 올라와도 입력창이 안 잘린다(Task 6의 데스크톱 전용 프레임)

- [ ] **Step 12: 다른 네트워크**

LTE로 배포 URL에 접속해 Step 1을 반복한다.
Expected: 완주

- [ ] **Step 13: 공급자 쿼터·결제를 확인한다**

- Google Cloud: 데모 프로젝트의 Routes·Places 일일 하드 쿼터, 결제 예산 알림
- 카카오: 개발자 콘솔 일일 쿼터가 `PER_DAY['/places']`보다 약간 위
- OpenAI: 사용량 상한

- [ ] **Step 14: production을 동결한다**

Cloudflare Pages 프로젝트에서 production 브랜치 자동 배포를 끈다. 9/21~10/17 사이 커밋이 심사 중인 링크를 바꾸면 안 된다.

- [ ] **Step 15: 모니터링을 건다**

`/health` 핑이 아니라 **A1 입력 → 결과까지** 확인하는 외부 합성 모니터. 실패 시 알림.

- [ ] **Step 16: 검증 결과를 기록한다**

`docs/앱스토어-심사-검토.md`에 절 하나를 추가하고 커밋한다: 배포 URL, Step 1-12 결과, 드래그가 됐는지 안 됐는지, 남은 위험.

```bash
git add docs/앱스토어-심사-검토.md
git commit -m "docs: 웹 데모 완주 검증 결과"
```

---

## 남은 위험

| 위험 | 완화 | 어디서 걸리나 |
|---|---|---|
| A6 드래그가 웹에서 안 된다 | 위/아래 이동 버튼 (반나절) | Task 10 Step 7-8 |
| `PER_DAY['/places']` 값이 틀렸다 | 카카오 콘솔 확인 후 확정 | Task 8 Step 9 |
| Pages env 누락으로 조용히 목으로 돎 | A5 판정 카드의 `source` 문구 | Task 7 Step 7 |
| CORS origin 오타 | 시크릿 모드 완주 | Task 7 Step 6-7 |
| 공개 토큰으로 상한 고갈 | 공급자 콘솔 하드 쿼터 + Task 1·2 폴백 | Task 10 Step 13 |
| Sheet 드래그 닫기가 웹에서 안 된다 | 스크림 탭·닫기 버튼만 살면 된다 | Task 10 Step 9 |
