# 되묻기 시트 열림/닫힘 규칙 정리 (N1 + N2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 되묻기 시트가 **언제 열리고 언제 안 열리는지**를 한 규칙으로 정리한다. 지금은 (a) 닫아 둔 시트가 엉뚱한 질문을 들고 다시 열리고, (b) 큐에 있는데 한 번도 못 본 질문이 생기고, (c) 그 질문에 도달할 길이 없다.

**Architecture:** "닫음"을 상태로 기록한다(`dismissedAsks`). 자동으로 열리는 자리(큐 병합·답변 직후)는 **닫지 않은 질문만** 고르고, 사용자가 칩에서 명시적으로 여는 자리는 무엇이든 연다. 접두사(`stop:`/`load:`) 해석은 `narrowAsk.ts` 한 곳으로 모으고, 칩 메뉴가 그 칩에 달린 질문을 **전부** 나열해 도달 경로를 만든다.

**Tech Stack:** React Native (Expo), TypeScript, `node:test` + `tsx`

**Spec:** 없음(회귀 수정 + 그 수정이 드러낸 도달성 결함). 아래 "무엇이 왜 틀렸나"가 근거다.

## 무엇이 왜 틀렸나

`feat/recommend-tab` 의 픽스 웨이브(`ab125dc`)가 만든 `mergeBulkyAsks` 의 마지막 줄:

```ts
const stillOpen = askField != null && asks.some(a => a.field === askField);
return { asks, askField: stillOpen ? askField : asks[0]?.field ?? null };
```

**N1** — `askField === null` 이 "답하던 질문이 잘렸다"와 "사용자가 닫았다" 두 가지인데 구분이 없다. 도보+마트 계획에서 시트를 닫고 이동수단을 자동차로 바꾸면, 물성 질문이 잘리면서 **닫아 둔 `stop:마트` 질문이 저절로 떠오른다.**

**N1-b** — `pickAnswer` 도 같은 실수를 한다(`setAskField(asks[0]?.field ?? null)`). 닫아 둔 질문이 "다른 질문에 답했다"는 이유로 되살아난다.

**N2** — 물성 질문이 둘 생기면(`마트` + `정육점`) 첫 질문을 닫는 순간 두 번째는 **한 번도 안 뜬 채** 큐에 갇힌다. `askForChip` 이 `stop:` 접두사만 보므로 칩 표식(`▾`)도 칩 메뉴 재오픈 버튼도 `load:` 질문에는 안 붙어, 영영 도달할 수 없다.

**부수** — `queryOf` 가 접두사를 확인하지 않고 5글자를 무조건 자른다(`'load:s-1'.slice(5) === 's-1'`). 칩 `queries` 에 `s-1` 같은 말이 들어오면 물성 질문이 좁히기 질문 행세를 한다. 도달하기 어렵지만 불변식이 코드로 지켜지지 않는다.

## 규칙 (이 계획이 세우는 것)

| 자리 | 무엇을 여나 |
|---|---|
| 큐 병합(`mergeBulkyAsks`) | 답하던 질문이 살아 있으면 그대로. 아니면 **닫지 않은** 질문 중 첫 번째 |
| 답변 직후(`pickAnswer`) | **닫지 않은** 질문 중 첫 번째 |
| 칩 메뉴에서 탭 | **그 질문**을 연다(닫아 뒀어도). 닫음 기록에서 뺀다 |
| 시트 닫기 | 지금 뜬 질문을 닫음으로 기록. 큐에서 빼지는 않는다 |

닫음은 **그 질문 하나**에 대한 것이지 큐 전체가 아니다. 그래서 새 질문은 계속 뜬다.

## Global Constraints

- 테스트는 `node:test` + `tsx`. 단일 파일 `npx tsx --test <path>`, 전체 `npm test`.
- 시작 기준선: `feat/recommend-tab` HEAD(`ab125dc`)에서 **873 pass / 0 fail**, `npx tsc --noEmit` 무출력. 절대 개수보다 `fail 0` 과 그 Task 의 신규 테스트만큼 늘었는지로 본다.
- `src/screens/**`, `src/sheets/**` 는 렌더 테스트 인프라가 없다. 판단 로직은 `src/lib/**` · `src/state/**` 의 순수 함수로 빼서 테스트한다.
- `src/state/plan.tsx` 는 `expo-*`·react-native 를 문다. 테스트 있는 모듈에서는 **타입 전용 import** 만 한다.
- `narrowAsk.ts` 는 `bulkyAsk.ts` 를 import 하지 않는다 — `bulkyAsk.ts` 가 `narrowAsk.ts` 를 물고 있어 순환이 된다. 접두사 지식은 `narrowAsk.ts` 가 소유하고 `bulkyAsk.ts` 가 가져다 쓴다.
- `mergeBulkyAsks` 의 **같은 배열 참조 반환 계약**을 깨지 않는다. `PlanScreen` 의 effect 가 `next.asks === narrowAsks` 로 자기 재진입을 막는다.
- 주석은 **왜**를 적는다. 한국어 주석·테스트 이름·UI 문구는 하우스 스타일이다.
- TDD: 실패하는 테스트를 먼저 쓰고 **실패를 눈으로 확인한 뒤** 구현한다.
- 커밋 메시지는 한국어, conventional prefix.

---

### Task 1: 접두사 해석을 `narrowAsk.ts` 한 곳으로

**Files:**
- Modify: `src/state/narrowAsk.ts`
- Modify: `src/state/bulkyAsk.ts` (`bulkyChipId` 가 위임)
- Modify: `src/screens/PlanScreen.tsx` (`askForChip` → `asksForChip` 호출부 3곳)
- Test: `src/state/narrowAsk.test.ts`

**Interfaces:**
- Consumes: `NarrowAsk`, `IntentChip`(타입 전용)
- Produces:
  ```ts
  export const ASK_STOP = 'stop:';
  export const ASK_LOAD = 'load:';
  export type AskTarget =
    | { kind: 'query'; query: string }
    | { kind: 'chip'; chipId: string }
    | null;
  export function askTarget(field: string): AskTarget;
  export function asksForChip(asks: NarrowAsk[], chip: IntentChip): NarrowAsk[];
  ```
  `askForChip`(단수)은 **삭제**한다.

`asksForChip` 매칭 규칙:
- `{kind:'query'}` → `chip.kind === 'stop' && !chip.narrowed && chip.queries.includes(query)`
- `{kind:'chip'}` → `chip.kind === 'stop' && chip.id === chipId` — **`narrowed` 는 보지 않는다.** 좁히기(어떤 마트로)와 물성(무거운가)은 서로 다른 질문이라, 좁힌 칩에도 답 안 한 물성 질문이 남을 수 있다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/state/narrowAsk.test.ts` 맨 아래에 붙인다. 파일 상단의 `stopChip` 헬퍼와 import 를 그대로 쓰고, import 줄에 `askTarget, asksForChip, ASK_LOAD` 를 더한다.

```typescript
/* 접두사는 코드가 확인한다. 전에는 `field.slice('stop:'.length)` 로 5글자를 무조건
   잘라서, `load:s-1` 이 검색어 `s-1` 인 척할 수 있었다 */

test('stop: 은 검색어를, load: 는 칩 id 를 가리킨다', () => {
  assert.deepEqual(askTarget('stop:마트'), { kind: 'query', query: '마트' });
  assert.deepEqual(askTarget('load:s-1'), { kind: 'chip', chipId: 's-1' });
});

test('모르는 접두사는 아무것도 가리키지 않는다 — 5글자를 무조건 자르면 안 된다', () => {
  assert.equal(askTarget('other:x'), null);
  assert.equal(askTarget('마트'), null);
});

test('물성 질문도 칩에 달린 질문으로 셈한다 — 안 그러면 칩에서 도달할 길이 없다', () => {
  const load = { field: 'load:s-1', question: '무거우신가요?', options: ['무거워요', '괜찮아요'] };

  assert.deepEqual(asksForChip([load], stopChip()).map(a => a.field), ['load:s-1']);
});

test('좁힌 칩에도 물성 질문은 남는다 — 어떤 마트인지와 무거운지는 다른 질문이다', () => {
  const load = { field: 'load:s-1', question: '무거우신가요?', options: ['무거워요', '괜찮아요'] };

  assert.deepEqual(asksForChip([load], stopChip({ narrowed: true })).map(a => a.field), ['load:s-1']);
});

test('한 칩에 두 종류가 달리면 둘 다 준다 — 메뉴가 전부 보여줘야 한다', () => {
  const asks = [
    { field: 'stop:마트', question: '어떤 마트로 할까요?', options: ['이마트', '동네 마트', '상관없어요'] },
    { field: 'load:s-1', question: '무거우신가요?', options: ['무거워요', '괜찮아요'] },
  ];

  assert.deepEqual(asksForChip(asks, stopChip()).map(a => a.field), ['stop:마트', 'load:s-1']);
});

test('좁힌 칩은 좁히기 질문을 더 받지 않는다 — 고정한 값을 덮을 길을 막는다', () => {
  const asks = [{ field: 'stop:마트', question: '어떤 마트로 할까요?', options: ['이마트', '상관없어요'] }];

  assert.deepEqual(asksForChip(asks, stopChip({ narrowed: true })), []);
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/state/narrowAsk.test.ts
```

기대: `askTarget`·`asksForChip` 이 없어 import 단계에서 실패.

- [ ] **Step 3: 구현한다**

`src/state/narrowAsk.ts` 에서 `queryOf` 와 `askForChip` 을 지우고 그 자리에 넣는다:

```typescript
/**
 * 되묻기 `field` 의 접두사. 여기 한 곳만 안다 — 접두사를 여러 곳에서 해석하면
 * 한 곳이 5글자를 무조건 자르는 순간(`'load:s-1'.slice(5) === 's-1'`) 물성 질문이
 * 좁히기 질문 행세를 한다.
 */
export const ASK_STOP = 'stop:';
export const ASK_LOAD = 'load:';

/** 이 질문이 무엇을 가리키나. 모르는 접두사면 `null` */
export type AskTarget =
  | { kind: 'query'; query: string }
  | { kind: 'chip'; chipId: string }
  | null;

export function askTarget(field: string): AskTarget {
  if (field.startsWith(ASK_STOP)) return { kind: 'query', query: field.slice(ASK_STOP.length) };
  if (field.startsWith(ASK_LOAD)) return { kind: 'chip', chipId: field.slice(ASK_LOAD.length) };
  return null;
}

/**
 * 이 칩에 달린 되묻기 **전부**. 칩 표식(▾)과 칩 메뉴가 이걸로 그린다.
 *
 * 단수(`askForChip`)였을 때는 칩에 질문이 둘 달리면 하나가 가려졌고, 물성 질문은
 * 접두사가 달라 아예 안 잡혀 **큐에 있는데 도달할 수 없는 질문**이 생겼다.
 */
export function asksForChip(asks: NarrowAsk[], chip: IntentChip): NarrowAsk[] {
  if (chip.kind !== 'stop') return [];
  const { id, queries, narrowed } = chip;
  return asks.filter(a => {
    const t = askTarget(a.field);
    if (t == null) return false;
    // 좁히기는 답한 칩에 다시 달지 않는다 — 고른 값과 같은 말이 검색어로 남으면
    // 사용자가 고정한 값을 되묻기가 덮는다
    if (t.kind === 'query') return !narrowed && queries.includes(t.query);
    // 물성은 좁혔는지와 무관하다 — 어떤 마트인지와 무거운지는 다른 질문이다
    return t.chipId === id;
  });
}
```

`src/state/bulkyAsk.ts` 의 `bulkyChipId` 를 위임으로 바꾸고 `FIELD_PREFIX` 를 지운다. 생성부의 `field` 도 `ASK_LOAD` 를 쓴다:

```typescript
import { askTarget, ASK_LOAD, type NarrowAsk } from './narrowAsk';

/** 물성 되묻기의 field 에서 칩 id 를 꺼낸다. 다른 되묻기면 null */
export function bulkyChipId(field: string): string | null {
  const t = askTarget(field);
  return t?.kind === 'chip' ? t.chipId : null;
}
```
(생성부는 `` field: `${ASK_LOAD}${c.id}` `` 로 바꾼다.)

`src/screens/PlanScreen.tsx` 의 `askForChip` 호출 3곳을 고친다:
- import 를 `asksForChip` 로
- `const menuAsk = menuChip ? askForChip(narrowAsks, menuChip) : undefined;`
  → `const menuAsks = menuChip ? asksForChip(narrowAsks, menuChip) : [];`
  그리고 아래 `{menuAsk && (...)}` 를 `{menuAsks.length > 0 && (...)}` 로 바꾸되 **Task 4 에서 목록으로 다시 쓴다.** 이 Task 에서는 `menuAsks[0]` 을 쓰는 최소 변경으로 컴파일만 통과시킨다.
- 칩 표식: `{askForChip(narrowAsks, chip) ? ' ▾' : ''}` → `{asksForChip(narrowAsks, chip).length > 0 ? ' ▾' : ''}`
- `pickAnswer` 안의 `state.chips.find(c => askForChip([openAsk], c))` → `state.chips.find(c => asksForChip([openAsk], c).length > 0)`

- [ ] **Step 4: 통과를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/state/narrowAsk.test.ts && npx tsc --noEmit && npm test 2>&1 | grep -E '^. (pass|fail) '
```

기대: 신규 6건 포함 전부 통과, `tsc` 무출력, 전체 `fail 0` (879).

- [ ] **Step 5: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/state/narrowAsk.ts src/state/narrowAsk.test.ts src/state/bulkyAsk.ts src/screens/PlanScreen.tsx
git commit -m "refactor(ask): 되묻기 접두사 해석을 한 곳으로 모은다"
```

---

### Task 2: 닫은 질문을 자동으로 열지 않는다

**Files:**
- Modify: `src/state/bulkyAsk.ts` — `nextAskField` 신설, `mergeBulkyAsks` 가 씀
- Test: `src/state/bulkyAsk.test.ts`

**Interfaces:**
- Consumes: Task 1 의 `askTarget`
- Produces:
  ```ts
  /** 자동으로 열 질문. 닫아 둔 것은 고르지 않는다. 없으면 null */
  export function nextAskField(asks: NarrowAsk[], dismissed: readonly string[]): string | null;
  ```
  `mergeBulkyAsks` 시그니처가 바뀐다:
  ```ts
  export function mergeBulkyAsks(
    queue: NarrowAsk[],
    fresh: NarrowAsk[],
    askField: string | null,
    dismissed: readonly string[],
  ): { asks: NarrowAsk[]; askField: string | null };
  ```

**codex 리뷰가 바로잡은 것 — 기대값을 정확히 적는다.** `asks = [...kept, ...add]` 라서 옛 `stop:` 질문이 새 `load:` 질문보다 앞에 온다. 그래서 "닫은 뒤 새 질문이 생기는" 경우도 **현재 코드에서 실패한다**(`asks[0]` 이 옛 질문이다). 아래 Step 2 의 기대값이 그것이다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/state/bulkyAsk.test.ts` 맨 아래에 붙이고, import 줄에 `nextAskField` 를 더한다.

```typescript
const stopAsk = (f = 'stop:마트'): NarrowAsk =>
  ({ field: f, question: '어떤 마트로 할까요?', options: ['이마트', '동네 마트', '상관없어요'] });

test('닫아 둔 질문은 자동으로 열지 않는다', () => {
  const asks = [stopAsk(), ...bulkyAsks([stop()], 'walk')];

  assert.equal(nextAskField(asks, ['stop:마트']), 'load:s-1');
});

test('전부 닫아 뒀으면 아무것도 열지 않는다', () => {
  const asks = [stopAsk()];

  assert.equal(nextAskField(asks, ['stop:마트']), null);
});

test('닫은 게 없으면 첫 질문을 연다', () => {
  assert.equal(nextAskField([stopAsk()], []), 'stop:마트');
});

test('가지치기가 닫아 둔 질문을 다시 열지 않는다 — N1 회귀', () => {
  const queue = [stopAsk(), ...bulkyAsks([stop()], 'walk')];

  // 자동차로 바꿔 물성 질문이 사라진다. 사용자는 이미 시트를 닫아 둔 상태
  const out = mergeBulkyAsks(queue, [], null, ['stop:마트']);

  assert.deepEqual(out.asks.map(a => a.field), ['stop:마트']);
  assert.equal(out.askField, null);
});

test('닫아 둔 뒤에 새 질문이 생기면 그건 띄운다 — 옛 질문이 큐 앞에 있어도', () => {
  const out = mergeBulkyAsks([stopAsk()], bulkyAsks([stop()], 'walk'), null, ['stop:마트']);

  assert.equal(out.askField, 'load:s-1', '큐 앞의 닫은 질문을 건너뛴다');
});

test('답하던 질문은 닫음 목록과 무관하게 그대로 둔다', () => {
  const queue = [stopAsk(), ...bulkyAsks([stop()], 'walk')];

  const out = mergeBulkyAsks(queue, bulkyAsks([stop(), stop({ id: 's-2', label: '정육점', queries: ['정육점'] })], 'walk'), 'load:s-1', []);

  assert.equal(out.askField, 'load:s-1');
});

test('바뀐 게 없으면 같은 배열 참조를 돌려준다 — effect 가 자기 자신을 다시 부르면 안 된다', () => {
  const queue = bulkyAsks([stop()], 'walk');

  const out = mergeBulkyAsks(queue, queue, 'load:s-1', []);

  assert.equal(out.asks, queue, '참조가 같아야 한다');
  assert.equal(out.askField, 'load:s-1');
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/state/bulkyAsk.test.ts
```

기대: `nextAskField` 가 없어 import 단계에서 실패. 구현 뒤에는 `mergeBulkyAsks` 의 인자 수가 늘어 기존 호출부(테스트 포함)가 컴파일에 걸린다 — Step 3 에서 같이 고친다.

- [ ] **Step 3: 구현한다**

`src/state/bulkyAsk.ts` 에 넣는다:

```typescript
/**
 * 자동으로 열 질문. **닫아 둔 것은 고르지 않는다.**
 *
 * 닫기는 그 질문 하나에 대한 것이지 큐 전체가 아니다 — 그래서 새 질문은 계속 뜨고,
 * 닫은 질문은 칩 메뉴에서만 다시 열린다(`asksForChip`).
 */
export function nextAskField(asks: NarrowAsk[], dismissed: readonly string[]): string | null {
  return asks.find(a => !dismissed.includes(a.field))?.field ?? null;
}
```

`mergeBulkyAsks` 의 마지막 두 줄을 바꾸고 시그니처에 `dismissed` 를 더한다. 앞부분(`live`·`kept`·`add`·같은 참조 조기 반환)은 **그대로 둔다**:

```typescript
  /* 답하던 질문이 살아남았으면 그대로 둔다 — 새 질문이 밀어내면 사용자는 자기가
     뭘 답하던 중이었는지 잃는다. 닫음 목록보다 이 판단이 앞선다:
     지금 답하고 있다는 건 닫지 않았다는 뜻이다 */
  if (askField != null && asks.some(a => a.field === askField)) return { asks, askField };

  /* 그 밖에는 닫지 않은 질문 중 첫 번째. `askField` 가 null(닫음)이든 잘렸든 같은
     규칙이다 — 두 경우를 가르려 했더니 "닫았는데 새 질문이 생긴" 경우가 어디에도
     안 맞았다. 닫음 목록이 그 구분을 대신한다 */
  return { asks, askField: nextAskField(asks, dismissed) };
```

기존 호출부를 고친다:
- `src/screens/PlanScreen.tsx` 의 `mergeBulkyAsks(...)` 호출에 네 번째 인자를 넘긴다. Task 3 에서 `dismissedAsks` 상태를 만들기 전까지는 **빈 배열 리터럴이 아니라** 파일 상단에 `const NO_DISMISSED: string[] = [];` 를 두고 그걸 넘긴다 — 매 렌더 새 배열을 만들면 effect 의존성이 매번 바뀐다.
- `src/state/bulkyAsk.test.ts` 의 기존 `mergeBulkyAsks` 호출 전부에 `[]` 를 더한다.

- [ ] **Step 4: 통과를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/state/bulkyAsk.test.ts && npx tsc --noEmit && npm test 2>&1 | grep -E '^. (pass|fail) '
```

기대: 신규 7건 포함 전부 통과, `tsc` 무출력, 전체 `fail 0` (886).

- [ ] **Step 5: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/state/bulkyAsk.ts src/state/bulkyAsk.test.ts src/screens/PlanScreen.tsx
git commit -m "fix(ask): 닫아 둔 되묻기를 자동으로 다시 열지 않는다"
```

---

### Task 3: 화면이 닫음을 기록한다

**Files:**
- Modify: `src/screens/PlanScreen.tsx`

**Interfaces:**
- Consumes: Task 2 의 `nextAskField`, `mergeBulkyAsks(queue, fresh, askField, dismissed)`
- Produces: 없음(화면)

- [ ] **Step 1: 닫음 상태를 만든다**

`const [askField, setAskField] = useState<string | null>(null);` 바로 아래에 넣는다. **훅은 전부 조기 return 위에 있어야 한다** — `PlanScreen` 본문에는 조기 return 이 없지만, 넣는 자리를 기존 useState 들 사이로 유지한다.

```tsx
  /* 사용자가 닫은 질문. 큐에서 빼지는 않는다 — 칩 메뉴에서 다시 열 수 있어야 하고,
     빼면 "그 칩에 답 안 한 질문이 있다"는 표식(▾)도 같이 사라진다.
     닫음은 그 질문 하나에 대한 것이지 큐 전체가 아니다 */
  const [dismissedAsks, setDismissedAsks] = useState<string[]>([]);
```

Task 2 에서 임시로 둔 `NO_DISMISSED` 상수를 지우고, effect 의 호출과 의존성을 바꾼다:

```tsx
  useEffect(() => {
    const next = mergeBulkyAsks(narrowAsks, bulkyAsks(state.chips, state.mode), askField, dismissedAsks);
    if (next.asks === narrowAsks) return;
    setNarrowAsks(next.asks);
    setAskField(next.askField);
  }, [state.chips, state.mode, narrowAsks, askField, dismissedAsks]);
```

- [ ] **Step 2: 닫기가 기록하게 한다**

`<NarrowAskSheet ... onClose={() => setAskField(null)} />` 를 바꾼다:

```tsx
      <NarrowAskSheet
        ask={openAsk}
        onPick={pickAnswer}
        onClose={() => {
          // 닫은 질문을 기억해 둔다 — 안 그러면 큐가 바뀔 때마다 이게 다시 떠오른다
          if (openAsk) setDismissedAsks(prev => (prev.includes(openAsk.field) ? prev : [...prev, openAsk.field]));
          setAskField(null);
        }}
      />
```

- [ ] **Step 3: 답변 직후에도 같은 규칙을 쓴다**

`pickAnswer` 의 `setAskField(asks[0]?.field ?? null)` **두 곳**(물성 분기와 좁히기 분기)을 전부 `setAskField(nextAskField(asks, dismissedAsks));` 로 바꾼다. import 에 `nextAskField` 를 더한다.

주석을 남긴다:

```tsx
    // 답한 뒤 다음 질문으로 넘어가되 닫아 둔 것은 건너뛴다 — 다른 질문에 답했다는
    // 이유로 사용자가 물리친 질문이 되살아나면 안 된다
```

- [ ] **Step 4: 답하면 닫음 기록에서 뺀다**

`pickAnswer` 의 두 분기 모두에서, 답한 질문을 `dismissedAsks` 에서 제거한다(큐에서 빠지므로 남겨 둘 이유가 없고, 같은 `field` 가 나중에 다시 생기면 새 질문이다):

```tsx
    setDismissedAsks(prev => prev.filter(f => f !== openAsk.field));
```

- [ ] **Step 5: 대화를 되돌리면 닫음도 지운다**

`confirmLeave` 의 `setAskField(null);` 옆에 `setDismissedAsks([]);` 를 더한다 — 대화가 통째로 사라지면 그 질문들도 사라진다.

- [ ] **Step 6: 검증**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsc --noEmit && npm test 2>&1 | grep -E '^. (pass|fail) '
```

기대: `tsc` 무출력, 전체 `fail 0` (886 — 이 Task 는 테스트를 더하지 않는다. 화면에는 렌더 테스트 인프라가 없다).

- [ ] **Step 7: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/screens/PlanScreen.tsx
git commit -m "fix(ask): 닫은 되묻기를 기억해 자동 재개방을 막는다"
```

---

### Task 4: 칩 메뉴가 그 칩의 질문을 전부 보여준다 (N2)

**Files:**
- Modify: `src/screens/PlanScreen.tsx` — 칩 메뉴의 질문 버튼

**Interfaces:**
- Consumes: Task 1 의 `asksForChip`, Task 3 의 `dismissedAsks`
- Produces: 없음(화면)

이게 N2 의 해결책이다. 지금은 칩 메뉴가 질문 **하나**만 버튼으로 내는데, 한 칩에 좁히기·물성이 둘 다 달릴 수 있고 물성은 아예 안 잡혔다. 질문 텍스트를 버튼 라벨로 쓰므로 **무엇이 열릴지 이름으로 드러난다** — `▾` 하나가 예측 불가하게 여러 질문을 가리키던 문제가 없어진다.

- [ ] **Step 1: 메뉴를 목록으로 바꾼다**

Task 1 에서 최소 변경해 둔 `{menuAsks.length > 0 && (...)}` 블록을 통째로 바꾼다:

```tsx
          {/* 되묻기를 닫아도 여기서 되살린다. 한 칩에 좁히기·물성이 둘 다 달릴 수
              있으므로 **전부** 낸다 — 하나만 내면 나머지는 큐에 있는데 누를 곳이 없다.
              라벨이 질문 문장 그대로라, 무엇이 열릴지 눌러 보기 전에 안다 */}
          {menuAsks.map(ask => (
            <Pressable
              key={ask.field}
              onPress={() => {
                haptic();
                setChipMenu(null);
                // 명시적으로 여는 자리다 — 닫음 기록에서 빼야 다음 병합이 다시 닫지 않는다
                setDismissedAsks(prev => prev.filter(f => f !== ask.field));
                setAskField(ask.field);
              }}
              style={{ minHeight: 52, borderRadius: 16, backgroundColor: color.primaryTint, alignItems: 'center', justifyContent: 'center' }}
            >
              <Text style={[type.btn, { color: color.primary }]}>{ask.question}</Text>
            </Pressable>
          ))}
```

- [ ] **Step 2: 검증**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsc --noEmit && npm test 2>&1 | grep -E '^. (pass|fail) '
```

기대: `tsc` 무출력, 전체 `fail 0` (886).

- [ ] **Step 3: 시뮬레이터 확인은 컨트롤러 몫**

이 환경에서는 시뮬레이터 한글 입력 경로가 없다. **직접 하지 말고** 리포트에 "미검증"으로 적는다. 확인할 것:
1. 도보 + "마트랑 정육점 들러서 갈게" → 되묻기가 뜬다 → 닫는다 → **다른 질문이 저절로 따라 뜨지 않는다**.
   두 칩 모두에 `▾` 가 있고, 칩을 눌러 메뉴에서 이름으로 고르면 그 질문이 열린다
2. 둘 다 닫으면 칩에 `▾` 가 남고, 칩을 누르면 메뉴에 질문 두 개가 이름으로 보인다
3. 이동수단을 자동차로 바꿔도 닫아 둔 질문이 저절로 뜨지 않는다

- [ ] **Step 4: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/screens/PlanScreen.tsx
git commit -m "fix(ask): 칩 메뉴가 그 칩의 되묻기를 전부 보여준다"
```

## 완료 기준

- `npm test` `fail 0`, 신규 테스트 13건(Task 1: 6, Task 2: 7) → **886**.
- `npx tsc --noEmit` 무출력.
- 큐에 있는 질문은 **전부** 칩 메뉴에서 도달 가능하다.
- 닫은 질문은 사용자가 칩에서 다시 열기 전까지 자동으로 뜨지 않는다.

## 이 계획이 건드리지 않는 것

- `bulkyAsks` 의 판정 규칙과 `loadAsked`.
- `추천 순서 | 최단 시간` 탭과 `/reason`.
- **알려진 방어적 공백(codex #2):** 큐는 그대로인데 `askField` 가 그 큐에 없는 상태. 정상 호출부는 큐와 선택을 함께 갱신하므로 도달하지 않는다. `mergeBulkyAsks` 의 같은 참조 조기 반환이 그때 낡은 `askField` 를 보존하지만, `openAsk` 가 `null` 이 되어 시트가 닫힌 것처럼 보일 뿐 깨지지 않는다.
