# 추천 순서 탭 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 추천 경로 화면에 `추천 순서 | 최단 시간` 탭을 상시 노출하고, 추천 사유 한 줄을 붙이고, 그 추천을 만드는 물성 판정을 사용자에게 직접 받아 신뢰할 수 있게 만든다.

**Architecture:** 두 단계다. 1단계는 **이미 계산된** 두 안(`result.options[0]` = 최단, `result.comfortIdx` = 짐을 덜 드는 안) 사이를 `flow.select(idx)` 로 전환하는 탭이다 — 재계산이 없고, 후보 교체는 `overrides[optionIdx]` 가 옵션별로 들고 있어 자동으로 유지된다. 2단계는 그 `comfortIdx` 를 만들어 내는 `loadAfter` 태그를 LLM 추측이 아니라 **되묻기로 사용자에게 받아** 결정적으로 만든다.

**Tech Stack:** React Native (Expo), TypeScript, `node:test` + `tsx`, Cloudflare Workers(서버)

**Spec:** 별도 스펙 문서 없음 — 2026-09-18 대화에서 확정한 설계다. 핵심 결정은 아래 "설계 배경"에 옮겨 적었다.

## 설계 배경 (스펙 대용)

- **추천순의 정의**: "사용자 편의 기준". 음식 포장·커피처럼 들고 다녀야 하는 것은 목적지 쪽이 낫고, 마트처럼 짐이 생기는 곳은 되도록 마지막이다. 이 축은 이미 코드에 있다 — `Slot.loadBefore/loadAfter/needWhen` → `plan.ts:181` `burdened` → `comfortMin` → `comfortIdx`.
- **최단시간의 정의**: `result.options[0]`. 실측 후 정렬된 배열의 0번이고 `optionTitle` 이 '가장 빠름'으로 부른다.
- **탭은 상시 노출**한다. 짐이 있을 때만 뜨던 기존 조건(`comfortIdx != null && comfortIdx !== 0`)을 없앤다.
- **사유 한 줄**은 `/reason` 이 이미 서버에 있다. 클라이언트가 안 부르고 있을 뿐이다.
- **왜 2단계가 필요한가**: 같은 문장 5회에 `loadAfter` 가 `HARD/none/HARD/none/HARD` 로 갈렸다(2026-09-18 실측). 1단계만 하면 탭은 늘 보이는데 내용이 동전 던지기다.

## Global Constraints

- 테스트는 `node:test` + `tsx`. 실행: `npm test`. 이 브랜치(`feat/recommend-tab`, main 기준)의 시작 기준선은 **846 pass / 0 fail**. 절대 개수는 브랜치마다 다르니 **`fail 0` 과 그 Task 의 신규 테스트 수만큼 늘었는지**로 판단한다.
- `src/screens/**`, `src/sheets/**` 는 렌더 테스트 인프라가 없다. **판단 로직은 `src/lib/**` 또는 `src/state/**` 의 순수 함수로 빼서 테스트한다.**
- `src/lib/places.ts` 처럼 `expo-*` 를 import 하는 모듈은 node 테스트에서 로드되지 않는다. 새 순수 모듈은 **`expo-*` 를 import 하지 않는다**(타입 전용 import 는 괜찮다).
- 서버 응답은 믿지 않는다. 앱에 닿는 값은 스키마를 통과한 것만이다(AGENTS.md).
- `/reason` 은 계획당 최대 1회 호출한다(`server/src/guard.ts:80` 이 이미 상한을 건다).
- 주석은 **왜**를 적는다. 무엇을 하는지는 코드가 말한다.
- 커밋 메시지는 한국어, conventional prefix(`feat(...)`, `fix(...)`).
- TDD: 실패하는 테스트를 먼저 쓰고, 실패를 **눈으로 확인**한 뒤 구현한다.

---

# 1단계 — 탭과 사유 (Task 1~3)

### Task 1: 추천 탭 상태 계산

**Files:**
- Create: `src/lib/routePlan/recommendTab.ts`
- Test: `src/lib/routePlan/recommendTab.test.ts`

**Interfaces:**
- Consumes: 없음 (순수 함수)
- Produces: `recommendTabState(comfortIdx: number | null, selectedIdx: number): { target: number; value: 0 | 1; sameAsFast: boolean }`

`comfortIdx` 는 `PlanResult.comfortIdx` 다. `null` 이면 짐을 안 잰 계획이고, `0` 이면 최단안이 곧 편한 안이다. 둘 다 "가리킬 다른 안이 없다"는 뜻이라 `sameAsFast: true` 로 합친다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/lib/routePlan/recommendTab.test.ts`:

```typescript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { recommendTabState } from './recommendTab';

test('추천안이 따로 있으면 그 인덱스를 가리킨다', () => {
  const s = recommendTabState(2, 0);

  assert.equal(s.target, 2);
  assert.equal(s.sameAsFast, false);
});

test('추천안을 고른 상태면 첫 번째 탭이 켜진다', () => {
  assert.equal(recommendTabState(2, 2).value, 0);
});

test('최단안을 고른 상태면 두 번째 탭이 켜진다', () => {
  assert.equal(recommendTabState(2, 0).value, 1);
});

test('짐을 안 잰 계획이면 가리킬 다른 안이 없다 — 최단안을 그대로 쓴다', () => {
  const s = recommendTabState(null, 0);

  assert.equal(s.sameAsFast, true);
  assert.equal(s.target, 0);
});

test('최단안이 곧 추천안이어도 같은 취급이다 — 원인은 달라도 화면이 할 일은 같다', () => {
  assert.equal(recommendTabState(0, 0).sameAsFast, true);
});

test('두 안이 같으면 추천 탭이 켜진 것으로 본다 — 지금 순서가 추천이기 때문이다', () => {
  assert.equal(recommendTabState(null, 0).value, 0);
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/lib/routePlan/recommendTab.test.ts
```

기대: `Cannot find module './recommendTab'`

- [ ] **Step 3: 최소 구현**

`src/lib/routePlan/recommendTab.ts`:

```typescript
/**
 * `추천 순서 | 최단 시간` 탭의 상태.
 *
 * 탭은 **늘 보인다.** 짐이 있을 때만 띄우던 시절엔, 같은 말을 해도 어떤 날은 탭이
 * 있고 어떤 날은 없었다 — 판정(`loadAfter`)이 흔들리기 때문이다. 화면이 흔들리면
 * 사용자는 자기가 뭘 잘못 눌렀는지 의심한다. 그래서 자리는 고정하고, 가리킬 안이
 * 없을 때는 그 사실을 문구로 말한다.
 */
export function recommendTabState(
  comfortIdx: number | null,
  selectedIdx: number,
): { target: number; value: 0 | 1; sameAsFast: boolean } {
  /* null 은 짐을 안 쟀다는 뜻이고 0 은 최단안이 곧 편한 안이라는 뜻이다. 원인은
     다르지만 화면이 할 일은 같다 — 가리킬 다른 안이 없다 */
  const sameAsFast = comfortIdx == null || comfortIdx === 0;
  if (sameAsFast) return { target: 0, value: 0, sameAsFast: true };
  return { target: comfortIdx, value: selectedIdx === comfortIdx ? 0 : 1, sameAsFast: false };
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/lib/routePlan/recommendTab.test.ts
```

기대: `pass 6 / fail 0`

- [ ] **Step 5: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/lib/routePlan/recommendTab.ts src/lib/routePlan/recommendTab.test.ts
git commit -m "feat(options): 추천 탭 상태를 순수 함수로 뺀다"
```

---

### Task 2: 탭 상시 노출 + 이름 변경

**Files:**
- Modify: `src/screens/OptionsScreen.tsx:197-223` (기존 SegmentControl 블록 전체를 교체)

**Interfaces:**
- Consumes: `recommendTabState` (Task 1)
- Produces: 없음 (화면)

교체 대상은 지금 이렇게 생겼다 — 이 블록을 통째로 지우고 아래 것으로 바꾼다:

```tsx
{comfortIdx != null && comfortIdx !== 0 && (
  <View style={{ gap: 6 }}>
    <SegmentControl
      options={['편한 순서', '최단 시간']}
      ...
```

- [ ] **Step 1: 블록을 교체한다**

`src/screens/OptionsScreen.tsx` 에서 위 블록을 아래로 바꾼다. `comfortIdx` 변수 선언(`const comfortIdx = result.comfortIdx;`)은 그대로 둔다.

```tsx
        {/* 0. 기준 고르기 — **늘 보인다.** 아래 숫자가 전부 이 선택을 따라 바뀌므로
            판정보다 위에 둔다. 가리킬 추천안이 따로 없을 때도 자리를 비우지 않는다:
            탭이 있다 없다 하면 사용자는 자기가 뭘 잘못 눌렀는지 의심한다 */}
        <View style={{ gap: 6 }}>
          <SegmentControl
            options={['추천 순서', '최단 시간']}
            value={tab.value}
            onChange={i => {
              haptic();
              flow.select(i === 0 ? tab.target : 0);
            }}
            fontSize={14}
            padV={9}
          />
          {/* 버튼만으로는 두 기준이 무슨 뜻인지 모른다. 높이를 미리 잡아 둔다 —
              설명이 나중에 와서 줄이 생기면 아래가 밀리고, 그게 곧 '말없이 바뀐다'다 */}
          <Text
            numberOfLines={1}
            style={{ fontFamily: 'Pretendard-Regular', fontSize: 12, lineHeight: 17, color: color.muted, paddingHorizontal: 2 }}
          >
            {tab.sameAsFast
              ? '지금 순서가 가장 편해요'
              : tab.value === 0
                ? '짐을 들고 이동하는 시간을 줄였어요'
                : '총 이동 시간이 가장 짧아요'}
          </Text>
        </View>
```

- [ ] **Step 2: `tab` 을 계산한다**

같은 파일에서 `const comfortIdx = result.comfortIdx;` 바로 아래에 넣는다:

```tsx
  /* 탭이 가리킬 안과 켜진 자리. 판단은 `recommendTab.ts` 한 곳이 한다 */
  const tab = recommendTabState(comfortIdx, state.selectedOptionIdx);
```

import 를 추가한다 (파일 상단 import 블록 끝):

```tsx
import { recommendTabState } from '../lib/routePlan/recommendTab';
```

- [ ] **Step 3: 타입과 테스트를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsc --noEmit && npm test 2>&1 | grep -E '^. (pass|fail) '
```

기대: tsc 출력 없음, `fail 0`. Task 1 의 6건이 더해져 **852** 여야 한다

- [ ] **Step 4: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/screens/OptionsScreen.tsx
git commit -m "feat(options): 추천 순서 탭을 상시 노출한다"
```

---

### Task 3: `/reason` 을 붙여 사유 한 줄을 받는다

**Files:**
- Create: `src/lib/reasonClient.ts`
- Test: `src/lib/reasonClient.test.ts`
- Modify: `src/screens/OptionsScreen.tsx` (Task 2 가 만든 설명 줄에 서버 문구를 얹는다)

**Interfaces:**
- Consumes: `recommendTabState` (Task 1)
- Produces: `makeReasonClient(opts): (req: ReasonRequest) => Promise<string | null>` 과 `type ReasonRequest = { text: string; mode: 'car'|'walk'|'transit'; fast: ReasonOption; comfort: ReasonOption }`, `type ReasonOption = { stops: string[]; totalMin: number }`

서버 계약(`server/src/reason.ts` 에서 확인한 것):
- 요청: `{ text, mode, fast: {stops, totalMin}, comfort: {stops, totalMin} }`. `stops` 가 비면 **422** 다.
- 응답: 항상 `{ why: string | null }`. 서버가 LLM 실패를 삼키고 `null` 로 돌려준다.
- 헤더: `x-app-token`, `x-device-id` (`src/lib/intentClient.ts` 와 같은 모양)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/lib/reasonClient.test.ts`:

```typescript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { makeReasonClient } from './reasonClient';

const REQ = {
  text: '마트 들러서 약국 가고 집에 갈게',
  mode: 'walk' as const,
  fast: { stops: ['이마트', '온누리약국'], totalMin: 40 },
  comfort: { stops: ['온누리약국', '이마트'], totalMin: 44 },
};

const jsonRes = (body: unknown, status = 200) =>
  new Response(JSON.stringify(body), { status, headers: { 'content-type': 'application/json' } });

test('서버가 준 한 줄을 그대로 돌려준다', async () => {
  const client = makeReasonClient({
    baseUrl: 'https://x.test',
    appToken: 't',
    deviceId: 'd',
    fetchFn: async () => jsonRes({ why: '장 본 걸 들고 약국에 들어가지 않아도 돼요' }),
  });

  assert.equal(await client(REQ), '장 본 걸 들고 약국에 들어가지 않아도 돼요');
});

test('why 가 null 이면 null 이다 — 설명은 장식이라 없어도 경로는 나온다', async () => {
  const client = makeReasonClient({
    baseUrl: 'https://x.test', appToken: 't', deviceId: 'd',
    fetchFn: async () => jsonRes({ why: null }),
  });

  assert.equal(await client(REQ), null);
});

test('서버가 실패해도 던지지 않는다 — 설명 하나 때문에 화면이 시끄러우면 안 된다', async () => {
  const client = makeReasonClient({
    baseUrl: 'https://x.test', appToken: 't', deviceId: 'd',
    fetchFn: async () => jsonRes({ error: 'bad request' }, 422),
  });

  assert.equal(await client(REQ), null);
});

test('네트워크가 끊겨도 던지지 않는다', async () => {
  const client = makeReasonClient({
    baseUrl: 'https://x.test', appToken: 't', deviceId: 'd',
    fetchFn: async () => { throw new Error('offline'); },
  });

  assert.equal(await client(REQ), null);
});

test('모양이 다른 응답은 버린다 — 서버 응답도 믿지 않는다', async () => {
  const client = makeReasonClient({
    baseUrl: 'https://x.test', appToken: 't', deviceId: 'd',
    fetchFn: async () => jsonRes({ why: 42 }),
  });

  assert.equal(await client(REQ), null);
});

test('토큰과 기기 id 를 헤더에 싣는다', async () => {
  let seen: Record<string, string> = {};
  const client = makeReasonClient({
    baseUrl: 'https://x.test/', appToken: 'tok', deviceId: 'dev',
    fetchFn: async (_url, init) => {
      seen = (init?.headers ?? {}) as Record<string, string>;
      return jsonRes({ why: '한 줄' });
    },
  });
  await client(REQ);

  assert.equal(seen['x-app-token'], 'tok');
  assert.equal(seen['x-device-id'], 'dev');
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/lib/reasonClient.test.ts
```

기대: `Cannot find module './reasonClient'`

- [ ] **Step 3: 최소 구현**

`src/lib/reasonClient.ts`:

```typescript
/**
 * `/reason` 호출 — 계산된 두 안을 보내고 설명 한 줄을 받는다.
 *
 * **실패해도 던지지 않는다.** 설명은 장식이지 기능이 아니다 — 없으면 탭만 뜨면 되고,
 * 이것 하나 때문에 경로 화면이 에러를 띄우면 안 된다. 서버도 같은 약속을 지킨다
 * (`server/src/reason.ts` 가 LLM 실패를 삼키고 `{why:null}` 을 200 으로 돌려준다).
 *
 * `expo-constants` 를 여기서 읽지 않는다 — 읽으면 node 테스트에서 이 파일이 로드되지
 * 않는다. 값은 호출부가 넘긴다.
 */
export type ReasonOption = { stops: string[]; totalMin: number };
export type ReasonRequest = {
  text: string;
  mode: 'car' | 'walk' | 'transit';
  fast: ReasonOption;
  comfort: ReasonOption;
};

const DEFAULT_TIMEOUT_MS = 6000;

export function makeReasonClient(opts: {
  baseUrl: string;
  appToken: string;
  deviceId: string;
  fetchFn?: typeof fetch;
  timeoutMs?: number;
}): (req: ReasonRequest) => Promise<string | null> {
  const fetchFn = opts.fetchFn ?? fetch;
  const timeoutMs = opts.timeoutMs ?? DEFAULT_TIMEOUT_MS;

  return async req => {
    const ctrl = new AbortController();
    const timer = setTimeout(() => ctrl.abort(), timeoutMs);
    try {
      const res = await fetchFn(`${opts.baseUrl.replace(/\/$/, '')}/reason`, {
        method: 'POST',
        headers: {
          'content-type': 'application/json',
          'x-app-token': opts.appToken,
          'x-device-id': opts.deviceId,
        },
        body: JSON.stringify(req),
        signal: ctrl.signal,
      });
      if (!res.ok) return null;
      const body = (await res.json()) as unknown;
      // 서버 응답도 믿지 않는다 — 화면에 그대로 나가는 문자열이다
      const why = (body as { why?: unknown } | null)?.why;
      return typeof why === 'string' && why.trim() ? why : null;
    } catch {
      return null;
    } finally {
      clearTimeout(timer);
    }
  };
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/lib/reasonClient.test.ts
```

기대: `pass 6 / fail 0`

- [ ] **Step 5: 화면에 붙인다**

`src/screens/OptionsScreen.tsx` 에 import 를 추가한다:

```tsx
import Constants from 'expo-constants';
import { makeReasonClient } from '../lib/reasonClient';
```

`OptionsScreen` 의 React import 는 지금 `import React, { useMemo, useState } from 'react';` 다. 아래 코드가 `useEffect` 와 `useRef` 를 쓰므로 **같이 추가한다**:

```tsx
import React, { useEffect, useMemo, useRef, useState } from 'react';
```

`const tab = recommendTabState(...)` 아래에 넣는다:

```tsx
  /* 서버가 준 설명 한 줄. 없으면 고정 문구로 떨어진다 — 설명은 장식이다 */
  const [whyLine, setWhyLine] = useState<string | null>(null);
  /* 계획당 한 번만 부른다(`server/src/guard.ts:80` 도 같은 상한을 건다).
     추천안이 최단안과 같으면 보낼 두 번째 안이 없어 아예 부르지 않는다 */
  const askedRef = useRef(false);
  useEffect(() => {
    if (tab.sameAsFast || askedRef.current || !result || !state.request) return;
    const comfort = result.options[tab.target];
    const fast = result.options[0];
    if (!comfort || !fast) return;
    askedRef.current = true;
    const extra = (Constants.expoConfig?.extra ?? {}) as { serverUrl?: string; appToken?: string };
    const baseUrl = extra.serverUrl?.trim();
    const appToken = extra.appToken?.trim();
    if (!baseUrl || !appToken) return;
    const client = makeReasonClient({ baseUrl, appToken, deviceId: Constants.sessionId ?? 'unknown' });
    void client({
      /* `PlanRequest` 에 사용자 문장은 없다 — origin·destination·mode·arriveByMin·
         departAtMin·stops·order 뿐이다. 서버도 `text` 를 선택값으로 받으므로 빈
         문자열을 보낸다. 설명은 두 안의 **순서 차이**만 보고도 쓸 수 있다 */
      text: '',
      mode: req.mode,
      fast: { stops: fast.visits.map(v => v.candidate.name), totalMin: Math.round(result.rescore(fast.visits).totalMin) },
      comfort: { stops: comfort.visits.map(v => v.candidate.name), totalMin: Math.round(result.rescore(comfort.visits).totalMin) },
    }).then(setWhyLine);
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [tab.sameAsFast, tab.target, result]);
```

설명 줄의 추천 탭 가지에 서버 문구를 우선한다 — Task 2 가 만든 `<Text>` 의 내용을 이렇게 바꾼다:

```tsx
            {tab.sameAsFast
              ? '지금 순서가 가장 편해요'
              : tab.value === 0
                ? whyLine ?? '짐을 들고 이동하는 시간을 줄였어요'
                : '총 이동 시간이 가장 짧아요'}
```

- [ ] **Step 6: 타입과 전체 테스트**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsc --noEmit && npm test 2>&1 | grep -E '^. (pass|fail) '
```

기대: tsc 출력 없음, `fail 0`. Task 3 의 6건이 더해져 **858** 이어야 한다

- [ ] **Step 7: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/lib/reasonClient.ts src/lib/reasonClient.test.ts src/screens/OptionsScreen.tsx
git commit -m "feat(options): 추천 사유 한 줄을 /reason 에서 받는다"
```

---

# 2단계 — 물성을 사용자에게 받는다 (Task 4~6)

1단계가 끝나면 탭은 늘 보이지만, `comfortIdx` 가 대부분 `null` 이라 두 탭이 같은 경로를 가리킨다. 원인은 `loadAfter` 판정이 비결정적이라는 것이다. 2단계는 그 값을 **사용자에게 직접 받는다.**

### Task 4: 물성 되묻기를 만드는 규칙

**Files:**
- Create: `src/state/bulkyAsk.ts`
- Test: `src/state/bulkyAsk.test.ts`

**Interfaces:**
- Consumes: `NarrowAsk` from `src/state/narrowAsk.ts` (`{ field: string; question: string; options: string[] }`), `IntentChip` from `src/state/plan.tsx`
- Produces: `bulkyAsks(chips: IntentChip[], mode: 'car'|'walk'|'transit'): NarrowAsk[]`, `BULKY_YES = '무거워요'`, `BULKY_NO = '괜찮아요'`, `bulkyChipId(field: string): string | null`

규칙:
- `mode === 'car'` 면 묻지 않는다. 트렁크에 실으므로 답이 계획을 안 바꾼다(`plan.ts:181` 이 자동차에서 짐 계산을 통째로 건너뛴다).
- 이미 `loadAfter` 가 정해진 칩은 묻지 않는다.
- 부피가 생기는 업종어가 `queries` 에 있을 때만 묻는다.
- `field` 는 `load:<chipId>` 다. 기존 좁히기 되묻기(`stop:<검색어>`)와 접두사로 구분한다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/state/bulkyAsk.test.ts`:

```typescript
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { bulkyAsks, bulkyChipId, BULKY_YES, BULKY_NO } from './bulkyAsk';
import type { IntentChip } from './plan';

const stop = (over: Partial<Extract<IntentChip, { kind: 'stop' }>> = {}): IntentChip => ({
  id: 's-1', kind: 'stop', label: '마트', queries: ['마트'],
  stopKind: 'category', openNow: false, flexible: true, near: 'any', ...over,
} as IntentChip);

test('짐이 생기는 업종이면 묻는다', () => {
  const asks = bulkyAsks([stop()], 'walk');

  assert.equal(asks.length, 1);
  assert.equal(asks[0].field, 'load:s-1');
  assert.deepEqual(asks[0].options, [BULKY_YES, BULKY_NO]);
});

test('자동차면 묻지 않는다 — 트렁크에 실으니 답이 계획을 안 바꾼다', () => {
  assert.deepEqual(bulkyAsks([stop()], 'car'), []);
});

test('이미 답한 칩은 다시 묻지 않는다', () => {
  assert.deepEqual(bulkyAsks([stop({ loadAfter: 'hard' })], 'walk'), []);
  assert.deepEqual(bulkyAsks([stop({ loadAfter: 'none' })], 'walk'), []);
});

test('짐이 안 생기는 업종은 묻지 않는다 — 안 물어도 되는 걸 물으면 소음이다', () => {
  assert.deepEqual(bulkyAsks([stop({ id: 's-2', queries: ['올리브영'], label: '올리브영' })], 'walk'), []);
});

test('여러 칩이면 각각 묻는다', () => {
  const asks = bulkyAsks([stop(), stop({ id: 's-9', queries: ['정육점'], label: '정육점' })], 'transit');

  assert.deepEqual(asks.map(a => a.field), ['load:s-1', 'load:s-9']);
});

test('field 에서 칩 id 를 되꺼낸다', () => {
  assert.equal(bulkyChipId('load:s-1'), 's-1');
  assert.equal(bulkyChipId('stop:마트'), null);
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/state/bulkyAsk.test.ts
```

기대: `Cannot find module './bulkyAsk'`

- [ ] **Step 3: 최소 구현**

`src/state/bulkyAsk.ts`:

```typescript
/**
 * "들고 다니기 무거운가"를 **사용자에게 묻는다.**
 *
 * 왜 LLM 에게 안 맡기나: 같은 문장 5회에 `loadAfter` 가 `HARD/none/HARD/none/HARD` 로
 * 갈렸다(2026-09-18 실측). 이 태그 하나가 `추천 순서` 탭이 가리킬 안을 정하는데,
 * 그게 동전 던지기면 같은 말을 해도 추천이 있었다 없었다 한다.
 *
 * 규칙은 코드가 고정한다 — 프롬프트를 더 세게 써도 비결정성은 안 없어진다.
 * 묻는 비용은 탭 한 번이고, 답은 사용자가 제일 잘 안다.
 */
import type { IntentChip } from './plan';
import type { NarrowAsk } from './narrowAsk';

export const BULKY_YES = '무거워요';
export const BULKY_NO = '괜찮아요';

const FIELD_PREFIX = 'load:';

/**
 * 부피·무게가 생기는 업종어. 브랜드가 아니라 **업종**으로 둔다 —
 * 브랜드를 나열하면 새 브랜드가 나올 때마다 빠진다.
 */
const BULKY = ['마트', '슈퍼', '정육점', '생수', '쌀', '가구', '창고형', '시장', '농협', '하나로'];

export function bulkyAsks(chips: IntentChip[], mode: 'car' | 'walk' | 'transit'): NarrowAsk[] {
  // 자동차는 짐 계산 자체를 건너뛴다(`routePlan/plan.ts` 의 `burdened`) — 물어도 안 쓴다
  if (mode === 'car') return [];
  return chips.flatMap(c => {
    if (c.kind !== 'stop' || c.loadAfter != null) return [];
    if (!c.queries.some(q => BULKY.some(b => q.includes(b)))) return [];
    return [{
      field: `${FIELD_PREFIX}${c.id}`,
      question: `${c.label}에서 산 건 들고 다니기 무거우신가요?`,
      options: [BULKY_YES, BULKY_NO],
    }];
  });
}

/** 물성 되묻기의 field 에서 칩 id 를 꺼낸다. 다른 되묻기면 null */
export function bulkyChipId(field: string): string | null {
  return field.startsWith(FIELD_PREFIX) ? field.slice(FIELD_PREFIX.length) : null;
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/state/bulkyAsk.test.ts
```

기대: `pass 6 / fail 0`

- [ ] **Step 5: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/state/bulkyAsk.ts src/state/bulkyAsk.test.ts
git commit -m "feat(ask): 짐이 생기는 경유지를 사용자에게 묻는 규칙"
```

---

### Task 5: 답을 칩에 기록하는 리듀서

**Files:**
- Modify: `src/state/plan.tsx` (액션 타입, 리듀서 case, 컨텍스트 API)
- Test: `src/state/plan.test.ts` (기존 파일에 추가)

**Interfaces:**
- Consumes: 없음
- Produces: 액션 `{ type: 'SET_CHIP_LOAD'; chipId: string; loadAfter: 'none' | 'hard' }`, 컨텍스트 메서드 `setChipLoad(chipId: string, loadAfter: 'none' | 'hard'): void`

`IntentChip(stop).loadAfter` 는 `planRequest.ts:40` 에서 `Slot.loadAfter` 로 그대로 내려가고, 거기서 `routePlan/plan.ts` 의 `burdened` 판정에 쓰인다. 그래서 칩에만 쓰면 나머지는 이미 연결돼 있다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`src/state/plan.test.ts` 맨 아래에 추가한다. 이 파일은 `require('./plan')` 로 `planReducer` 를 가져오고(값 import 는 스텁 hoisting 때문에 못 쓴다), `fresh()` 로 초기 상태를 만든다. 둘 다 파일 상단에 이미 있으니 그대로 쓴다.

```typescript
/** 마트 칩 하나를 얹은 상태 — 물성 질문이 향할 자리 */
const withMart = (): PlanState => ({
  ...fresh(),
  chips: [{
    id: 's-1', kind: 'stop', label: '마트', queries: ['마트'],
    stopKind: 'category', openNow: false, flexible: true, near: 'any',
  }],
});

test('물성 답을 칩에 기록한다 — 이 값이 슬롯을 지나 추천 순서를 정한다', () => {
  const after = planReducer(withMart(), { type: 'SET_CHIP_LOAD', chipId: 's-1', loadAfter: 'hard' });

  const chip = after.chips.find(c => c.id === 's-1');
  assert.equal(chip?.kind === 'stop' && chip.loadAfter, 'hard');
});

test('없는 칩 id 면 상태를 바꾸지 않는다 — 같은 참조를 돌려준다', () => {
  const before = withMart();

  assert.equal(planReducer(before, { type: 'SET_CHIP_LOAD', chipId: 'nope', loadAfter: 'hard' }), before);
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsx --test src/state/plan.test.ts 2>&1 | tail -20
```

기대: `SET_CHIP_LOAD` 가 처리되지 않아 `loadAfter` 가 `undefined`

- [ ] **Step 3: 최소 구현**

액션 타입에 추가한다 (`PlanAction` 유니온, `| { type: 'NARROW_STOP'; ... }` 근처):

```typescript
  /** 물성 되묻기의 답. `planRequest.ts` 가 이 값을 슬롯으로 내리고 거기서 추천 순서가 갈린다 */
  | { type: 'SET_CHIP_LOAD'; chipId: string; loadAfter: 'none' | 'hard' }
```

리듀서에 case 를 추가한다 (`case 'NARROW_STOP':` 근처):

```typescript
    case 'SET_CHIP_LOAD': {
      const hit = state.chips.find(c => c.kind === 'stop' && c.id === action.chipId);
      // 바뀐 게 없으면 같은 참조를 돌려준다 — 호출부가 그걸로 재계산을 건너뛴다
      if (!hit) return state;
      return {
        ...state,
        chips: state.chips.map(c =>
          c.kind === 'stop' && c.id === action.chipId ? { ...c, loadAfter: action.loadAfter } : c,
        ),
      };
    }
```

컨텍스트 API 에 추가한다 (타입 선언과 구현 둘 다):

```typescript
  /** 물성 되묻기의 답을 칩에 남긴다 */
  setChipLoad: (chipId: string, loadAfter: 'none' | 'hard') => void;
```

```typescript
      setChipLoad: (chipId, loadAfter) => dispatch({ type: 'SET_CHIP_LOAD', chipId, loadAfter }),
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsc --noEmit && npm test 2>&1 | grep -E '^. (pass|fail) '
```

기대: tsc 출력 없음, `fail 0`

- [ ] **Step 5: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/state/plan.tsx src/state/plan.test.ts
git commit -m "feat(ask): 물성 답을 경유지 칩에 기록한다"
```

---

### Task 6: 물성 되묻기를 기존 시트 큐에 합류시킨다

**Files:**
- Modify: `src/screens/PlanScreen.tsx` (되묻기 큐를 만드는 자리와 `pickAnswer`)

**Interfaces:**
- Consumes: `bulkyAsks`, `bulkyChipId`, `BULKY_YES` (Task 4), `setChipLoad` (Task 5), 기존 `asksForSheet`/`answerAsk`/`NarrowAskSheet`
- Produces: 없음 (화면)

`PlanScreen` 에는 이미 되묻기 시트가 있다(`narrowAsks` 큐 + `askField` + `NarrowAskSheet`). 물성 되묻기를 **같은 큐에 얹는다** — 사용자에게는 질문이 이어서 뜰 뿐이다.

- [ ] **Step 1: import 를 추가한다**

```tsx
import { bulkyAsks, bulkyChipId, BULKY_YES } from '../state/bulkyAsk';
```

`usePlan()` 구조분해에 `setChipLoad` 를 추가한다.

- [ ] **Step 2: 추출 결과에 물성 질문을 이어 붙인다**

`applyChat` 의 `.then(...)` 안, `const forSheet = asksForSheet(narrow);` 아래를 이렇게 바꾼다:

`applyChat` 안에서는 칩이 아직 갱신되기 **전**이다(`applyIntent` 가 방금 dispatch 됐다). `PlanScreen` 에 `stateRef` 는 없으므로, 갱신된 칩을 보는 `useEffect` 로 큐에 더한다. `pickAnswer` 선언 위에 넣는다:

```tsx
  /* 물성 질문은 칩이 갱신된 **뒤**에 판단한다 — `applyChat` 안에서는 `applyIntent` 가
     방금 dispatch 된 참이라 아직 옛 칩이다. 이미 큐에 있는 질문은 다시 넣지 않는다:
     칩이 바뀔 때마다 이 effect 가 다시 도는데, 그때 같은 질문이 쌓이면 시트가
     답한 질문을 또 묻는다 */
  useEffect(() => {
    const asks = bulkyAsks(state.chips, state.mode);
    if (asks.length === 0) return;
    /* 이미 큐에 있는 질문은 다시 넣지 않는다 — 칩이 바뀔 때마다 이 effect 가 다시
       도는데, 그때 같은 질문이 쌓이면 시트가 답한 질문을 또 묻는다.
       `add` 가 비면 여기서 끝나므로 `narrowAsks` 를 의존성에 둬도 돌지 않는다 */
    const add = asks.filter(a => !narrowAsks.some(q => q.field === a.field));
    if (add.length === 0) return;
    setNarrowAsks(prev => [...prev, ...add]);
    // 열려 있는 질문이 없을 때만 첫 물성 질문을 띄운다 — 답하던 질문을 밀어내지 않는다
    setAskField(cur => cur ?? add[0].field);
  }, [state.chips, state.mode, narrowAsks]);
```

- [ ] **Step 3: 답 처리에 물성 가지를 더한다**

`pickAnswer` 를 이렇게 바꾼다:

```tsx
  const pickAnswer = (option: string) => {
    if (!openAsk) return;
    const chipId = bulkyChipId(openAsk.field);
    if (chipId) {
      // 물성 질문 — 칩에 답을 남긴다. 좁히기(narrowStop)와 다른 값이라 다른 자리에 쓴다
      setChipLoad(chipId, option === BULKY_YES ? 'hard' : 'none');
      flow.reset(); // 추천 순서가 바뀔 수 있다 — 계산은 사용자가 다시 들어갈 때
      const asks = narrowAsks.filter(a => a.field !== openAsk.field);
      setNarrowAsks(asks);
      setAskField(asks[0]?.field ?? null);
      return;
    }
    const { narrowTo, asks } = answerAsk(narrowAsks, openAsk, option);
    if (narrowTo) {
      const target = state.chips.find(c => askForChip([openAsk], c));
      if (target) {
        narrowStop(target.id, narrowTo);
        flow.reset();
      }
    }
    setNarrowAsks(asks);
    setAskField(asks[0]?.field ?? null);
  };
```

- [ ] **Step 4: 칩 표식이 물성 질문에도 붙는지 확인한다**

`askForChip(narrowAsks, chip)` 은 `stop:` 접두사만 본다. 물성 질문은 `load:<chipId>` 라 표식(▾)이 안 붙는다. **이번 작업에서는 그대로 둔다** — 물성 질문은 좁히기와 달리 닫아도 계획이 정상 진행되고, 표식까지 붙이면 칩 하나에 두 종류 질문이 달려 무엇을 묻는지 흐려진다. 이 결정을 코드 주석으로 남긴다.

- [ ] **Step 5: 타입과 전체 테스트**

```bash
cd /Users/picpal/Desktop/workspace/duler && npx tsc --noEmit && npm test 2>&1 | grep -E '^. (pass|fail) '
```

기대: tsc 출력 없음, `fail 0`

- [ ] **Step 6: 시뮬레이터에서 확인한다**

앱에서 도보 모드로 "마트 들러서 갈게" 를 보내고 다음을 본다:
1. 좁히기 질문(어떤 마트로) 뒤에 **"…에서 산 건 들고 다니기 무거우신가요?"** 가 시트로 뜬다
2. `무거워요` 를 고르고 경로를 찾으면 `추천 순서` 탭이 **최단 시간과 다른 순서**를 가리킨다
3. 추천 탭 아래 설명 한 줄이 뜬다(서버가 주면 그 문장, 아니면 고정 문구)

- [ ] **Step 7: 커밋**

```bash
cd /Users/picpal/Desktop/workspace/duler
git add src/screens/PlanScreen.tsx
git commit -m "feat(ask): 물성 되묻기를 되묻기 시트에 잇는다"
```

---

## 완료 기준

- `npm test` 가 `fail 0`. 신규 테스트 20건(Task 1: 6, Task 3: 6, Task 4: 6, Task 5: 2) → **866**.
- `npx tsc --noEmit` 출력 없음.
- 시뮬레이터에서 Task 6 Step 6 의 세 가지가 모두 보인다.
- 자동차 모드에서는 물성 질문이 **뜨지 않는다**.
