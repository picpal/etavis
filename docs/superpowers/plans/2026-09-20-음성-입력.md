# 음성 입력(온디바이스 STT) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 계획 화면 입력 바의 마이크 버튼을 누르면 말한 문장이 입력창에 글자로 차오르고, 끝나면 입력창에 채워져 사용자가 전송한다.

**Architecture:** 플랫폼 네이티브 STT(`expo-speech-recognition`, iOS `SFSpeechRecognizer`)를 `src/lib/speech.ts` 의 `useSpeechInput()` 훅 하나로 감싼다. 상태 전이는 순수 리듀서(`speechSession.ts`)로 떼어 node 테스트로 고정하고, 화면(`BottomInputBar`)은 훅이 주는 `listening · interim · level · error` 만 본다. 인식 결과는 자동 전송하지 않고 입력창에 채운다. 오인식 교정은 기존 `/extract` LLM 의 몫이다.

**Tech Stack:** Expo SDK 57 · `expo-speech-recognition@57.1.0` · React Native 0.86 · node:test(`tsx --test`) · maestro

**Spec:** 이 문서의 「설계 요약」 절. 별도 스펙 파일 없음(2026-09-20 채팅에서 결정).

## Global Constraints

- Expo 는 바뀌었다. 코드를 쓰기 전에 https://docs.expo.dev/versions/v57.0.0/ 를 읽는다.
- 작업은 워크트리 `/Users/picpal/Desktop/workspace/duler/.claude/worktrees/wip+0919` 에서만. `node_modules` 는 메인으로 가는 심링크다. **절대 스테이징하지 않는다** (`git add -A` 금지, 파일을 지목해서 add).
- Metro: 8081 은 메인 코드다. 워크트리는 **8082** 에 띄운다. `CI=1` 금지(파일 감시가 꺼진다). 기기 `932E3441-F52C-4563-8D30-CA5C481D6DA5`(iPhone 17 Pro) 의 `RCT_jsLocation` 은 이미 `localhost:8082` 다.
- 시뮬레이터는 이 세션 기기 하나만 쓴다. 조작은 `maestro --device <UDID>`. 돌리기 전에 `lsof -nP -iTCP:7001 -sTCP:LISTEN` 이 비어 있어야 한다. `pkill -f maestro` 금지.
- maestro 선택자는 접근성 라벨 전체 일치 정규식이다. `.*…*.` 로 감싼다. `inputText` 전에 `tapOn` 으로 포커스. 첫 명령 앞에 `extendedWaitUntil`.
- 앱은 상태를 영속하지 않는다. 시나리오는 한 플로우 파일에 담는다. 처음부터 가는 플로우는 `launchApp`(`clearState` 없이).
- 네이티브 빌드·`npm install` 전에 `vm_stat | grep -E 'free|compressor'` 를 본다. 24GB 기계, 스왑 상시 포화.
- 웹 데모 타깃: shim 된 모듈에 export 를 추가하면 `.web.ts` 에도 같은 export 를 넣는다. 원본과 shim 의 export 목록을 `diff` 로 대조한다.
- 카피 규칙: 듣는 중 상태에 "듣는 중" 같은 텍스트 라벨을 두지 않는다(버튼 초록 + 음파로 충분, 2026-09-19 결정). 안내 문구는 존댓말 한 줄.
- LLM 은 '무엇을' 만 뽑는다. STT 결과를 이 계획에서 가공하거나 해석하지 않는다. 문장 그대로 입력창에 넣는다.
- 기능은 한 단계에 하나씩. 이 계획은 **계획 화면 입력 바 하나** 만 다룬다. 홈 검색 시트의 마이크는 범위 밖.

---

## 설계 요약

| 항목 | 결정 |
|---|---|
| 엔진 | 플랫폼 네이티브(`expo-speech-recognition`). Whisper 는 나중에 서버 재인식 옵션으로만 |
| 결과 처리 | 입력창에 채운다. 전송은 사용자가. 이미 글이 있으면 공백 하나로 이어 붙인다 |
| 듣는 동안 | 마이크 버튼 초록 + 흰 아이콘. 입력창: 부분 결과가 오기 전엔 음파, 오면 회색 글자가 차오른다 |
| 종료 | `continuous: false` 로 무음 자동 종료 + 마이크 재탭 수동 종료(`stop()`, 최종 결과 처리) |
| 언어 | `ko-KR` 고정 |
| 오프라인 | `network` 오류면 `supportsOnDeviceRecognition()` 일 때 `requiresOnDeviceRecognition: true` 로 한 번 재시도 |
| 권한 거부 | 자리표시 "설정에서 마이크·음성 인식을 켜 주세요". 그 상태에서 마이크 재탭 → `Linking.openSettings()` |
| 인식 0건 | 자리표시 "잘 못 들었어요. 다시 말해 주세요" |
| 키보드 | 인식 시작 시 `Keyboard.dismiss()`. 끝나도 자동 포커스하지 않는다 |
| 음파 | `volumechange`(-2~10) 를 0~1 로 정규화해 `VoiceBars` 에 넘긴다. 가짜 타이머 제거 |
| 웹 | `speech.web.ts` shim 이 `available: false` 를 돌려주고 마이크 버튼이 숨는다 |
| 검증 | 시뮬레이터: 가짜 드라이버(`EXPO_PUBLIC_SPEECH_FAKE=1`)로 UI 전이·이어 붙이기·전송을 maestro 로 고정. 실기기: TestFlight 로 인식 품질·자동 종료 |

### 파일 구조

| 파일 | 역할 |
|---|---|
| `src/lib/speechSession.ts` (신규) | 순수 리듀서·문자열 유틸. RN 의존 없음. 테스트 대상 |
| `src/lib/speechSession.test.ts` (신규) | node:test |
| `src/lib/speech.ts` (신규) | `useSpeechInput()` 훅. 네이티브 모듈·권한·재시도·가짜 드라이버 |
| `src/lib/speech.web.ts` (신규) | 웹 shim. 같은 export, `available: false` |
| `src/components/VoiceBars.tsx` | `level` prop 을 받는다. `useFakeLevel` 삭제 |
| `src/components/BottomInputBar.tsx` | 훅 연결. 부분 결과 표시·이어 붙이기·오류 자리표시 |
| `app.json` | 플러그인 + 한국어 권한 문구 |
| `package.json` | 의존성 |
| `docs/NEXT.md` | 실기기 검증 체크리스트 |

---

### Task 1: 세션 리듀서와 문자열 유틸 (순수 로직)

**Files:**
- Create: `src/lib/speechSession.ts`
- Test: `src/lib/speechSession.test.ts`

**Interfaces:**
- Produces:
  ```ts
  export type SpeechPhase = 'idle' | 'listening' | 'done' | 'error';
  export type SpeechErrorCode = 'not-allowed' | 'no-speech' | 'network' | 'unavailable' | 'other';
  export type SpeechState = { phase: SpeechPhase; interim: string; finalText: string | null; error: SpeechErrorCode | null };
  export type SpeechEvent =
    | { type: 'start' }
    | { type: 'result'; transcript: string; isFinal: boolean }
    | { type: 'end' }
    | { type: 'error'; code: string }
    | { type: 'reset' };
  export const initialSpeech: SpeechState;
  export function reduceSpeech(state: SpeechState, event: SpeechEvent): SpeechState;
  export function mapErrorCode(code: string): SpeechErrorCode;
  export function errorCopy(code: SpeechErrorCode): string;
  export function mergeTranscript(existing: string, transcript: string): string;
  export function volumeToLevel(volume: number): number;
  ```

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/speechSession.test.ts`:

```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import {
  errorCopy,
  initialSpeech,
  mapErrorCode,
  mergeTranscript,
  reduceSpeech,
  volumeToLevel,
} from './speechSession';

test('start → listening, 이전 결과·오류는 지운다', () => {
  const s = reduceSpeech({ ...initialSpeech, finalText: '옛말', error: 'no-speech' }, { type: 'start' });
  assert.deepEqual(s, { phase: 'listening', interim: '', finalText: null, error: null });
});

test('부분 결과는 interim 에 쌓이고 phase 는 listening 그대로', () => {
  let s = reduceSpeech(initialSpeech, { type: 'start' });
  s = reduceSpeech(s, { type: 'result', transcript: '스타벅스', isFinal: false });
  s = reduceSpeech(s, { type: 'result', transcript: '스타벅스 들러서', isFinal: false });
  assert.equal(s.phase, 'listening');
  assert.equal(s.interim, '스타벅스 들러서');
  assert.equal(s.finalText, null);
});

test('최종 결과 → done, finalText 에 담긴다', () => {
  let s = reduceSpeech(initialSpeech, { type: 'start' });
  s = reduceSpeech(s, { type: 'result', transcript: '스타벅스 들러서 가자', isFinal: true });
  assert.deepEqual(s, { phase: 'done', interim: '', finalText: '스타벅스 들러서 가자', error: null });
});

test('최종 없이 end 가 오면 interim 을 최종으로 승격한다 — iOS 가 isFinal 없이 끝내는 경우', () => {
  let s = reduceSpeech(initialSpeech, { type: 'start' });
  s = reduceSpeech(s, { type: 'result', transcript: '약국도', isFinal: false });
  s = reduceSpeech(s, { type: 'end' });
  assert.equal(s.phase, 'done');
  assert.equal(s.finalText, '약국도');
});

test('아무 말도 없이 end 면 no-speech 오류', () => {
  let s = reduceSpeech(initialSpeech, { type: 'start' });
  s = reduceSpeech(s, { type: 'end' });
  assert.deepEqual(s, { phase: 'error', interim: '', finalText: null, error: 'no-speech' });
});

test('done 뒤에 오는 end 는 무시한다 — 결과를 두 번 내보내지 않는다', () => {
  let s = reduceSpeech(initialSpeech, { type: 'start' });
  s = reduceSpeech(s, { type: 'result', transcript: '끝', isFinal: true });
  const after = reduceSpeech(s, { type: 'end' });
  assert.deepEqual(after, s);
});

test('no-speech 오류라도 interim 이 있으면 결과로 살린다', () => {
  let s = reduceSpeech(initialSpeech, { type: 'start' });
  s = reduceSpeech(s, { type: 'result', transcript: '편의점', isFinal: false });
  s = reduceSpeech(s, { type: 'error', code: 'no-speech' });
  assert.equal(s.phase, 'done');
  assert.equal(s.finalText, '편의점');
});

test('오류 코드 매핑 — 웹 스피치 이름을 앱의 다섯 가지로', () => {
  assert.equal(mapErrorCode('not-allowed'), 'not-allowed');
  assert.equal(mapErrorCode('no-speech'), 'no-speech');
  assert.equal(mapErrorCode('network'), 'network');
  assert.equal(mapErrorCode('service-not-allowed'), 'unavailable');
  assert.equal(mapErrorCode('language-not-supported'), 'unavailable');
  assert.equal(mapErrorCode('audio-capture'), 'other');
  assert.equal(mapErrorCode('aborted'), 'other');
});

test('오류 문구 — 한 줄 존댓말, 권한은 설정 안내', () => {
  assert.equal(errorCopy('not-allowed'), '설정에서 마이크·음성 인식을 켜 주세요');
  assert.equal(errorCopy('no-speech'), '잘 못 들었어요. 다시 말해 주세요');
  assert.equal(errorCopy('network'), '인터넷이 없어 음성 인식을 못 했어요');
  assert.equal(errorCopy('unavailable'), '이 기기에서는 음성 인식을 쓸 수 없어요');
  assert.equal(errorCopy('other'), '음성 인식이 중단됐어요. 다시 눌러 주세요');
});

test('이어 붙이기 — 공백 하나, 양끝 정리, 빈 쪽은 무시', () => {
  assert.equal(mergeTranscript('', '스타벅스 들러서'), '스타벅스 들러서');
  assert.equal(mergeTranscript('약국도 ', ' 스타벅스 들러서 '), '약국도 스타벅스 들러서');
  assert.equal(mergeTranscript('약국도', ''), '약국도');
  assert.equal(mergeTranscript('   ', '  '), '');
});

test('음량 정규화 — -2~10 을 0~1 로, 0 이하는 안 들리는 것', () => {
  assert.equal(volumeToLevel(-2), 0);
  assert.equal(volumeToLevel(0), 0);
  assert.equal(volumeToLevel(5), 0.5);
  assert.equal(volumeToLevel(10), 1);
  assert.equal(volumeToLevel(14), 1);
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/picpal/Desktop/workspace/duler/.claude/worktrees/wip+0919 && npx tsx --test src/lib/speechSession.test.ts`
Expected: FAIL — `Cannot find module './speechSession'`

- [ ] **Step 3: 구현**

`src/lib/speechSession.ts`:

```ts
/** 음성 입력 세션 — 순수 상태 전이. RN·네이티브 모듈 의존 없음.
 *  훅(speech.ts)은 네이티브 이벤트를 여기 SpeechEvent 로 바꿔 넣기만 한다.
 *
 *  iOS 는 continuous:false 일 때 무음이면 isFinal 없이 end 만 보내기도 한다.
 *  그래서 end·no-speech 가 와도 interim 이 있으면 그걸 결과로 승격한다 —
 *  사용자가 분명히 말했는데 빈손으로 끝나는 게 최악이다. */

export type SpeechPhase = 'idle' | 'listening' | 'done' | 'error';
export type SpeechErrorCode = 'not-allowed' | 'no-speech' | 'network' | 'unavailable' | 'other';

export type SpeechState = {
  phase: SpeechPhase;
  /** 듣는 동안 차오르는 부분 결과 */
  interim: string;
  /** phase 가 done 일 때만 값이 있다. 훅이 한 번 소비하고 reset 한다 */
  finalText: string | null;
  error: SpeechErrorCode | null;
};

export type SpeechEvent =
  | { type: 'start' }
  | { type: 'result'; transcript: string; isFinal: boolean }
  | { type: 'end' }
  | { type: 'error'; code: string }
  | { type: 'reset' };

export const initialSpeech: SpeechState = { phase: 'idle', interim: '', finalText: null, error: null };

export function reduceSpeech(state: SpeechState, event: SpeechEvent): SpeechState {
  switch (event.type) {
    case 'start':
      return { phase: 'listening', interim: '', finalText: null, error: null };
    case 'reset':
      return initialSpeech;
    case 'result': {
      if (state.phase !== 'listening') return state;
      const text = event.transcript.trim();
      if (event.isFinal) return { phase: 'done', interim: '', finalText: text, error: null };
      return { ...state, interim: text };
    }
    case 'end': {
      if (state.phase !== 'listening') return state;
      if (state.interim) return { phase: 'done', interim: '', finalText: state.interim, error: null };
      return { phase: 'error', interim: '', finalText: null, error: 'no-speech' };
    }
    case 'error': {
      if (state.phase !== 'listening') return state;
      const code = mapErrorCode(event.code);
      if (code === 'no-speech' && state.interim) {
        return { phase: 'done', interim: '', finalText: state.interim, error: null };
      }
      return { phase: 'error', interim: '', finalText: null, error: code };
    }
  }
}

/** expo-speech-recognition 의 error 이름(웹 스피치 API 기준)을 앱의 다섯 가지로 */
export function mapErrorCode(code: string): SpeechErrorCode {
  switch (code) {
    case 'not-allowed':
    case 'no-speech':
    case 'network':
      return code;
    case 'service-not-allowed':
    case 'language-not-supported':
      return 'unavailable';
    default:
      return 'other';
  }
}

export function errorCopy(code: SpeechErrorCode): string {
  switch (code) {
    case 'not-allowed':
      return '설정에서 마이크·음성 인식을 켜 주세요';
    case 'no-speech':
      return '잘 못 들었어요. 다시 말해 주세요';
    case 'network':
      return '인터넷이 없어 음성 인식을 못 했어요';
    case 'unavailable':
      return '이 기기에서는 음성 인식을 쓸 수 없어요';
    case 'other':
      return '음성 인식이 중단됐어요. 다시 눌러 주세요';
  }
}

/** 입력창에 이미 글이 있으면 공백 하나로 이어 붙인다 */
export function mergeTranscript(existing: string, transcript: string): string {
  return [existing.trim(), transcript.trim()].filter(Boolean).join(' ');
}

/** volumechange 는 -2~10. 0 이하는 안 들리는 것으로 본다 */
export function volumeToLevel(volume: number): number {
  return Math.min(1, Math.max(0, volume / 10));
}
```

- [ ] **Step 4: 통과 확인**

Run: `npx tsx --test src/lib/speechSession.test.ts`
Expected: 11 tests, 0 fail

- [ ] **Step 5: 커밋**

```bash
git add src/lib/speechSession.ts src/lib/speechSession.test.ts
git commit -m "feat(speech): 음성 세션 리듀서 — 부분·최종·오류 전이를 순수 함수로"
```

---

### Task 2: 라이브러리 설치, 권한 문구, `useSpeechInput` 훅과 웹 shim

**Files:**
- Modify: `package.json`, `app.json`
- Create: `src/lib/speech.ts`, `src/lib/speech.web.ts`

**Interfaces:**
- Consumes: Task 1 의 `reduceSpeech · initialSpeech · mapErrorCode · volumeToLevel · SpeechErrorCode`
- Produces:
  ```ts
  export type SpeechInput = {
    /** 이 플랫폼·기기에서 쓸 수 있나. false 면 화면은 마이크 버튼을 숨긴다 */
    available: boolean;
    listening: boolean;
    /** 듣는 동안 차오르는 글자 */
    interim: string;
    /** 0~1 음량 */
    level: number;
    error: SpeechErrorCode | null;
    start(): Promise<void>;
    stop(): void;
  };
  export function useSpeechInput(opts: { onFinal: (text: string) => void }): SpeechInput;
  ```

- [ ] **Step 1: 메모리 확인 후 설치**

```bash
vm_stat | grep -E 'free|compressor'
cd /Users/picpal/Desktop/workspace/duler/.claude/worktrees/wip+0919
npx expo install expo-speech-recognition
```

Expected: `package.json` 에 `"expo-speech-recognition": "~57.1.0"` 이 생긴다(정확한 캐럿·틸드는 expo install 이 정한다). `node_modules` 는 심링크라 메인에 설치된다 — 정상이다.

확인: `git status --short` 에 `package.json`, `package-lock.json` 만 보여야 한다. `node_modules` 가 보이면 절대 add 하지 말고 멈춘다.

- [ ] **Step 2: app.json 플러그인과 권한 문구**

`app.json` 의 `"plugins": ["expo-font"]` 를 다음으로 바꾼다:

```json
    "plugins": [
      "expo-font",
      [
        "expo-speech-recognition",
        {
          "microphonePermission": "말로 경유 조건을 넣을 때 마이크로 목소리를 들어요.",
          "speechRecognitionPermission": "들은 목소리를 글자로 바꿔 입력창에 채워요."
        }
      ]
    ],
```

- [ ] **Step 3: 네이티브 훅**

`src/lib/speech.ts`:

```ts
/** 음성 입력 훅 — expo-speech-recognition 을 SpeechInput 하나로 감싼다.
 *  화면은 이 훅만 본다. 상태 전이는 speechSession.ts(순수)에 있다.
 *
 *  - ko-KR 고정, continuous:false 라 무음이면 알아서 끝난다. 재탭은 stop().
 *  - network 오류면 온디바이스 인식을 지원할 때 한 번 재시도한다.
 *  - EXPO_PUBLIC_SPEECH_FAKE=1 이면 네이티브 대신 가짜 드라이버가 돈다 —
 *    시뮬레이터에는 마이크가 없어 UI 전이를 maestro 로 고정하려면 이게 필요하다.
 *    Metro 번들 시점에 값이 박히므로 Metro 를 그 환경변수로 띄워야 한다. */
import { useCallback, useEffect, useReducer, useRef, useState } from 'react';
import { ExpoSpeechRecognitionModule, useSpeechRecognitionEvent } from 'expo-speech-recognition';
import {
  initialSpeech,
  reduceSpeech,
  volumeToLevel,
  type SpeechErrorCode,
  type SpeechEvent,
} from './speechSession';

export type SpeechInput = {
  available: boolean;
  listening: boolean;
  interim: string;
  level: number;
  error: SpeechErrorCode | null;
  start(): Promise<void>;
  stop(): void;
};

const FAKE = process.env.EXPO_PUBLIC_SPEECH_FAKE === '1';

/** 가짜 드라이버 — 부분 결과 두 번, 최종 한 번, 음량은 사인파. 타이머 핸들을 돌려준다 */
function runFake(dispatch: (e: SpeechEvent) => void, setLevel: (v: number) => void) {
  const timers: ReturnType<typeof setTimeout>[] = [];
  const at = (ms: number, fn: () => void) => timers.push(setTimeout(fn, ms));
  let tick = 0;
  const meter = setInterval(() => setLevel(0.3 + 0.7 * Math.abs(Math.sin(tick++ / 3))), 100);
  at(600, () => dispatch({ type: 'result', transcript: '스타벅스', isFinal: false }));
  at(1200, () => dispatch({ type: 'result', transcript: '스타벅스 들러서', isFinal: false }));
  at(2000, () => dispatch({ type: 'result', transcript: '스타벅스 들러서 가자', isFinal: true }));
  at(2100, () => dispatch({ type: 'end' }));
  return () => {
    timers.forEach(clearTimeout);
    clearInterval(meter);
  };
}

export function useSpeechInput({ onFinal }: { onFinal: (text: string) => void }): SpeechInput {
  const [state, dispatch] = useReducer(reduceSpeech, initialSpeech);
  const [level, setLevel] = useState(0);
  const retriedOnDevice = useRef(false);
  const fakeStop = useRef<(() => void) | null>(null);
  const onFinalRef = useRef(onFinal);
  onFinalRef.current = onFinal;

  const available = FAKE || ExpoSpeechRecognitionModule.isRecognitionAvailable();

  const startNative = (onDevice: boolean) => {
    ExpoSpeechRecognitionModule.start({
      lang: 'ko-KR',
      interimResults: true,
      continuous: false,
      maxAlternatives: 1,
      requiresOnDeviceRecognition: onDevice,
      volumeChangeEventOptions: { enabled: true, intervalMillis: 100 },
    });
  };

  useSpeechRecognitionEvent('result', e => {
    const transcript = e.results[0]?.transcript ?? '';
    dispatch({ type: 'result', transcript, isFinal: e.isFinal });
  });
  useSpeechRecognitionEvent('end', () => dispatch({ type: 'end' }));
  useSpeechRecognitionEvent('volumechange', e => setLevel(volumeToLevel(e.value)));
  useSpeechRecognitionEvent('error', e => {
    // 인터넷이 없으면 온디바이스로 한 번만 다시 시도한다
    if (e.error === 'network' && !retriedOnDevice.current && ExpoSpeechRecognitionModule.supportsOnDeviceRecognition()) {
      retriedOnDevice.current = true;
      dispatch({ type: 'start' });
      startNative(true);
      return;
    }
    dispatch({ type: 'error', code: e.error });
  });

  // 최종 결과는 한 번만 밖으로 내보내고 idle 로 돌아간다
  useEffect(() => {
    if (state.phase === 'done' && state.finalText != null) {
      onFinalRef.current(state.finalText);
      setLevel(0);
      dispatch({ type: 'reset' });
    }
    if (state.phase === 'error') setLevel(0);
  }, [state.phase, state.finalText]);

  const start = useCallback(async () => {
    if (state.phase === 'listening') return;
    retriedOnDevice.current = false;
    if (FAKE) {
      dispatch({ type: 'start' });
      fakeStop.current = runFake(dispatch, setLevel);
      return;
    }
    const perm = await ExpoSpeechRecognitionModule.requestPermissionsAsync();
    if (!perm.granted) {
      dispatch({ type: 'start' });
      dispatch({ type: 'error', code: 'not-allowed' });
      return;
    }
    dispatch({ type: 'start' });
    startNative(false);
  }, [state.phase]);

  const stop = useCallback(() => {
    if (FAKE) {
      // 가짜는 지금까지의 부분 결과를 최종으로 승격시킨다(리듀서의 end 규칙)
      fakeStop.current?.();
      fakeStop.current = null;
      dispatch({ type: 'end' });
      return;
    }
    ExpoSpeechRecognitionModule.stop();
  }, []);

  useEffect(() => () => fakeStop.current?.(), []);

  return {
    available,
    listening: state.phase === 'listening',
    interim: state.interim,
    level,
    error: state.error,
    start,
    stop,
  };
}
```

- [ ] **Step 4: 웹 shim**

`src/lib/speech.web.ts`:

```ts
/** 웹 데모 — 음성 입력 없음. export 목록은 speech.ts 와 같아야 한다(확장자 없는 import 라 tsc 가 못 잡는다) */
import type { SpeechErrorCode } from './speechSession';

export type SpeechInput = {
  available: boolean;
  listening: boolean;
  interim: string;
  level: number;
  error: SpeechErrorCode | null;
  start(): Promise<void>;
  stop(): void;
};

export function useSpeechInput(_opts: { onFinal: (text: string) => void }): SpeechInput {
  return { available: false, listening: false, interim: '', level: 0, error: null, start: async () => {}, stop: () => {} };
}
```

- [ ] **Step 5: 타입·export 대조**

```bash
npx tsc --noEmit -p .
grep -o '^export [a-z]* [A-Za-z]*' src/lib/speech.ts | sort > /tmp/a.txt
grep -o '^export [a-z]* [A-Za-z]*' src/lib/speech.web.ts | sort > /tmp/b.txt
diff /tmp/a.txt /tmp/b.txt && echo SAME
```

Expected: tsc 오류 0, `SAME`.

- [ ] **Step 6: 커밋**

```bash
git add package.json package-lock.json app.json src/lib/speech.ts src/lib/speech.web.ts
git commit -m "feat(speech): expo-speech-recognition 과 useSpeechInput 훅 — ko-KR, 온디바이스 폴백, 웹 shim"
```

---

### Task 3: VoiceBars 가 실제 음량을 받는다

**Files:**
- Modify: `src/components/VoiceBars.tsx`

**Interfaces:**
- Produces: `export function VoiceBars({ level, tint }: { level: number; tint?: string })` — `level` 은 0~1

- [ ] **Step 1: 가짜 신호 제거, prop 으로 교체**

`src/components/VoiceBars.tsx` 전체를 다음으로 바꾼다:

```tsx
/** 음량 파형 막대 — 듣는 중일 때 입력 바 안에서 움직인다.
 *  level 은 0~1(useSpeechInput 이 volumechange 를 정규화해 준다). */
import React, { useEffect, useRef } from 'react';
import { Animated, Easing, View } from 'react-native';
import { color } from '../theme/tokens';

const BAR_COUNT = 5;
const BAR_MIN = 4;
const BAR_MAX = 22;
// 가운데가 가장 크고 바깥으로 갈수록 작다 — 한 값으로 다섯 막대를 움직인다
const WEIGHTS = [0.45, 0.8, 1, 0.8, 0.45];

export function VoiceBars({ level, tint = color.green }: { level: number; tint?: string }) {
  const anims = useRef(Array.from({ length: BAR_COUNT }, () => new Animated.Value(BAR_MIN))).current;

  useEffect(() => {
    Animated.parallel(
      anims.map((a, i) =>
        Animated.timing(a, {
          toValue: BAR_MIN + (BAR_MAX - BAR_MIN) * level * WEIGHTS[i],
          duration: 110,
          easing: Easing.out(Easing.quad),
          useNativeDriver: false,
        }),
      ),
    ).start();
  }, [level, anims]);

  return (
    <View style={{ flexDirection: 'row', alignItems: 'center', gap: 4, height: BAR_MAX }}>
      {anims.map((h, i) => (
        <Animated.View key={i} style={{ width: 4, height: h, borderRadius: 2, backgroundColor: tint }} />
      ))}
    </View>
  );
}
```

- [ ] **Step 2: 타입 확인**

Run: `npx tsc --noEmit -p .`
Expected: `BottomInputBar.tsx` 에서 `Property 'level' is missing` 오류 1건. Task 4 에서 고친다. 다른 오류는 없어야 한다.

- [ ] **Step 3: 커밋은 Task 4 와 함께** (이 단계 단독으로는 tsc 가 깨져 있으므로 커밋하지 않는다)

---

### Task 4: BottomInputBar 에 훅 연결 — 글자 차오름, 이어 붙이기, 오류 자리표시

**Files:**
- Modify: `src/components/BottomInputBar.tsx`

**Interfaces:**
- Consumes: `useSpeechInput` (Task 2), `VoiceBars({ level })` (Task 3), `errorCopy · mergeTranscript` (Task 1)
- Produces: 외부 props 는 그대로. `voice` 가 true 여도 `available` 이 false 면 버튼이 숨는다

- [ ] **Step 1: 구현**

`src/components/BottomInputBar.tsx` 에서 import 와 컴포넌트 본문을 다음으로 바꾼다. `SendGlyph` 는 그대로 둔다.

```tsx
/** 하단 입력 바 — 입력창(minHeight 52, R18) + (선택) 52×52 마이크 + 52×52 전송 버튼
 *  마이크: 누르면 듣기 시작(버튼 초록·아이콘 흰색). 부분 결과가 오기 전엔 입력창에 음파,
 *  오면 회색 글자가 차오른다. 끝나면 입력창에 채워지고 전송은 사용자가 한다. */
import React, { useState } from 'react';
import { Keyboard, Linking, Pressable, Text, TextInput, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { color, radius, shadow, type } from '../theme/tokens';
import { haptic } from './common';
import { MicIcon } from './primitives';
import { VoiceBars } from './VoiceBars';
import { useSpeechInput } from '../lib/speech';
import { errorCopy, mergeTranscript } from '../lib/speechSession';
```

컴포넌트 안: 기존 `const [listening, setListening] = useState(false);` 를 지우고 다음을 넣는다.

```tsx
  const speech = useSpeechInput({
    onFinal: t => setText(prev => mergeTranscript(prev, t)),
  });
  const listening = speech.listening;
  const showMic = voice && speech.available;

  const onPressMic = () => {
    haptic();
    if (listening) {
      speech.stop();
      return;
    }
    if (speech.error === 'not-allowed') {
      // iOS 는 한 번 거부하면 다시 묻지 않는다. 설정으로 보낸다
      Linking.openSettings();
      return;
    }
    Keyboard.dismiss();
    void speech.start();
  };
```

입력창 분기(`{listening ? (...) : (<TextInput .../>)}`)를 다음으로 바꾼다:

```tsx
          {listening && !speech.interim ? (
            <View style={{ alignItems: 'center' }}>
              <VoiceBars level={speech.level} />
            </View>
          ) : listening ? (
            <Text style={[type.bodyL, { color: color.muted }]} numberOfLines={2}>
              {speech.interim}
            </Text>
          ) : (
            <TextInput
              editable={editable}
              autoFocus={autoFocus}
              value={text}
              onChangeText={setText}
              onSubmitEditing={submit}
              placeholder={speech.error ? errorCopy(speech.error) : placeholder}
              placeholderTextColor={speech.error ? color.amber : color.placeholder}
              selectionColor={color.primary}
              returnKeyType="send"
              style={[type.bodyL, { color: color.ink, paddingVertical: 0 }]}
            />
          )}
```

마이크 버튼 블록(`{voice && (<Pressable ...>)}`)을 다음으로 바꾼다:

```tsx
      {showMic && (
        <Pressable
          onPress={onPressMic}
          accessibilityLabel={listening ? '음성 인식 중, 누르면 멈춤' : '음성으로 말하기'}
          style={({ pressed }) => ({
            width: 52,
            height: 52,
            borderRadius: radius.input,
            backgroundColor: listening ? color.green : color.surface,
            alignItems: 'center',
            justifyContent: 'center',
            ...shadow.input,
            opacity: pressed ? 0.85 : 1,
          })}
        >
          <MicIcon size={24} tint={listening ? '#fff' : color.primary} />
        </Pressable>
      )}
```

`voice` prop 의 주석은 `/** 마이크 버튼을 보인다. 기기가 음성 인식을 지원하지 않으면 숨는다 */` 로 고친다.

- [ ] **Step 2: 타입·테스트**

```bash
npx tsc --noEmit -p .
npm test 2>&1 | tail -3
```

Expected: tsc 0 오류. `npm test` 의 fail 0.

- [ ] **Step 3: 커밋**

```bash
git add src/components/VoiceBars.tsx src/components/BottomInputBar.tsx
git commit -m "feat(input): 마이크가 실제로 듣는다 — 부분 결과가 입력창에 차오르고 끝나면 채워진다"
```

---

### Task 5: 시뮬레이터 네이티브 재빌드와 maestro 검증

네이티브 모듈이 추가됐으므로 시뮬레이터에 깔린 앱(메인 `ios/build-sim` 산출물)으로는 `ExpoSpeechRecognitionModule` 이 없어 JS 가 죽는다. 워크트리에서 다시 빌드한다.

**Files:**
- (gitignored) `ios/` 생성
- Create (스크래치): `<scratchpad>/voice-fake.yaml`, `<scratchpad>/voice-perm.yaml`

- [ ] **Step 1: 사전 점검**

```bash
cd /Users/picpal/Desktop/workspace/duler/.claude/worktrees/wip+0919
git check-ignore ios && echo IGNORED          # 2026-09-20 확인: 이미 무시된다. 아니면 멈추고 보고
vm_stat | grep -E 'free|compressor'            # free 가 수만 페이지 이하면 유휴 claude 채널 세션부터 정리
grep -c SWIFT_RETURNS_RETAINED node_modules/expo-modules-jsi/apple/Sources/ExpoModulesJSI-Cxx/include/RuntimeScheduler.h
```

마지막 줄이 `0` 이 아니면 Xcode 26 패치가 필요하다(메모리 `etavia-ios-build` 3번):

```bash
sed -i '' 's/SWIFT_RETURNS_RETAINED //g' node_modules/expo-modules-jsi/apple/Sources/ExpoModulesJSI-Cxx/include/RuntimeScheduler.h
```

- [ ] **Step 2: 기존 Metro 8082 를 가짜 드라이버 환경변수로 다시 띄운다**

```bash
lsof -nP -iTCP:8082 -sTCP:LISTEN            # PID 확인
kill <PID>
EXPO_PUBLIC_SPEECH_FAKE=1 npx expo start --port 8082 </dev/null &
```

`CI=1` 을 붙이지 않는다.

- [ ] **Step 3: 시뮬레이터 앱 빌드·설치**

```bash
LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 npx expo run:ios --device 932E3441-F52C-4563-8D30-CA5C481D6DA5 --port 8082 --no-bundler
```

Expected: `ios/` 가 생기고 Pods 설치 후 시뮬레이터에 설치·실행. 10분 안팎. `--no-bundler` 라 Step 2 의 Metro 를 그대로 쓴다. 실패하면 로그의 첫 `error:` 줄을 그대로 보고하고 멈춘다 — 코드에서 원인을 찾기 전에 `vm_stat` 을 먼저 본다(OOM 이 흔하다).

설치 후 `RCT_jsLocation` 이 살아 있는지:

```bash
xcrun simctl spawn 932E3441-F52C-4563-8D30-CA5C481D6DA5 defaults read com.etavia.app RCT_jsLocation
```

Expected: `localhost:8082`. 아니면 `defaults write com.etavia.app RCT_jsLocation "localhost:8082"` 후 앱 재실행.

- [ ] **Step 4: 가짜 드라이버로 UI 전이 검증 — 플로우 파일**

`<scratchpad>/voice-fake.yaml` (스크래치 경로는 세션의 scratchpad 디렉터리로 바꿔 쓴다):

```yaml
appId: com.etavia.app
---
- launchApp
- extendedWaitUntil:
    visible: ".*최종 목적지를 입력하세요.*"
    timeout: 20000
- tapOn: ".*최종 목적지를 입력하세요.*"
- extendedWaitUntil:
    visible: ".*코엑스.*영동대로.*"
    timeout: 20000
- tapOn: ".*코엑스.*영동대로.*"
- extendedWaitUntil:
    visible: ".*계획 만들기.*"
    timeout: 10000
- tapOn: ".*계획 만들기.*"
- extendedWaitUntil:
    visible: ".*조건을 더 말해보세요.*"
    timeout: 90000
# 1) 시작 → 버튼 라벨이 '음성 인식 중' 으로 바뀐다
- tapOn: ".*음성으로 말하기.*"
- extendedWaitUntil:
    visible: ".*음성 인식 중.*"
    timeout: 3000
- takeScreenshot: <scratchpad>/v1-listening
# 2) 부분 결과가 글자로 차오른다
- extendedWaitUntil:
    visible: "스타벅스 들러서"
    timeout: 3000
- takeScreenshot: <scratchpad>/v2-interim
# 3) 최종 결과가 입력창에 채워지고 버튼은 평소로 돌아온다
- extendedWaitUntil:
    visible: ".*음성으로 말하기.*"
    timeout: 5000
- assertVisible: "스타벅스 들러서 가자"
- takeScreenshot: <scratchpad>/v3-filled
# 4) 다시 말하면 이어 붙는다
- tapOn: ".*음성으로 말하기.*"
- extendedWaitUntil:
    visible: ".*음성으로 말하기.*"
    timeout: 6000
- assertVisible: "스타벅스 들러서 가자 스타벅스 들러서 가자"
- takeScreenshot: <scratchpad>/v4-merged
# 5) 듣는 중 재탭은 부분 결과를 최종으로 승격한다 — 1.5초 뒤 멈추면 '스타벅스 들러서' 까지
- tapOn: ".*음성으로 말하기.*"
- extendedWaitUntil:
    visible: "스타벅스 들러서"
    timeout: 3000
- tapOn: ".*음성 인식 중.*"
- extendedWaitUntil:
    visible: ".*음성으로 말하기.*"
    timeout: 3000
- takeScreenshot: <scratchpad>/v5-stopped
```

실행 전 `lsof -nP -iTCP:7001 -sTCP:LISTEN` 이 비어 있어야 한다.

```bash
maestro --device 932E3441-F52C-4563-8D30-CA5C481D6DA5 test <scratchpad>/voice-fake.yaml
```

**검증 조건(전부 충족해야 통과):**
- 플로우 전 명령 COMPLETED.
- `v1-listening.png`: 마이크 버튼이 초록, 입력창 가운데에 초록 막대 5개, "듣는 중" 같은 텍스트 라벨 없음.
- `v2-interim.png`: 입력창에 회색 글자 `스타벅스 들러서`, 버튼은 아직 초록.
- `v3-filled.png`: 입력창에 검정 글자 `스타벅스 들러서 가자`, 버튼은 흰 배경·파란 아이콘, 키보드 안 올라옴.
- `v4-merged.png`: 두 문장이 공백 하나로 이어져 있다.
- `v5-stopped.png`: 재탭으로 멈춘 뒤 입력창에 이어 붙은 텍스트가 `… 스타벅스 들러서` 로 끝난다(`가자` 없음).
- 스크린샷의 루트 `bounds` 가 `[402,874]`(iPhone 17 Pro) — 남의 기기가 아니다.

스크린샷은 PIL 로 하단 200px 을 잘라 본다(`sips` 크롭은 결과가 틀린 적이 있다).

- [ ] **Step 5: 채운 문장이 기존 채팅 파이프라인으로 흘러가는지**

같은 파일 끝에 붙인다(별도 실행 금지 — 상태가 안 남는다):

```yaml
# 6) 전송 → 채팅으로 처리된다. 서버가 죽어 있어도 intent.ts 목이 받는다
- tapOn:
    point: "93%,95%"
- extendedWaitUntil:
    visible: ".*스타벅스.*"
    timeout: 30000
- takeScreenshot: <scratchpad>/v6-sent
```

전송 버튼은 접근성 라벨이 없어 좌표로 누른다. 화면 하단 우측 52×52. 좌표가 빗나가면 `maestro --device <UDID> hierarchy` 로 위치를 잡는다.

**검증 조건:** `v6-sent.png` 에서 사용자 버블에 보낸 문장이 보이고, 어시스턴트 버블이나 칩에 `스타벅스` 가 나타난다. 입력창은 비어 있다.

- [ ] **Step 6: 실제 드라이버 — 권한 흐름과 오류 자리표시**

Metro 를 환경변수 없이 다시 띄운다:

```bash
kill <8082 PID>
npx expo start --port 8082 </dev/null &
```

시뮬레이터에서 마이크 권한을 지운 뒤 앱을 다시 띄운다:

```bash
xcrun simctl privacy 932E3441-F52C-4563-8D30-CA5C481D6DA5 reset microphone com.etavia.app
xcrun simctl terminate 932E3441-F52C-4563-8D30-CA5C481D6DA5 com.etavia.app
xcrun simctl launch 932E3441-F52C-4563-8D30-CA5C481D6DA5 com.etavia.app
```

`<scratchpad>/voice-perm.yaml`:

```yaml
appId: com.etavia.app
---
- extendedWaitUntil:
    visible: ".*최종 목적지를 입력하세요.*"
    timeout: 20000
- tapOn: ".*최종 목적지를 입력하세요.*"
- extendedWaitUntil:
    visible: ".*코엑스.*영동대로.*"
    timeout: 20000
- tapOn: ".*코엑스.*영동대로.*"
- extendedWaitUntil:
    visible: ".*계획 만들기.*"
    timeout: 10000
- tapOn: ".*계획 만들기.*"
- extendedWaitUntil:
    visible: ".*조건을 더 말해보세요.*"
    timeout: 90000
- tapOn: ".*음성으로 말하기.*"
# 마이크 권한 → 음성 인식 권한, 두 번 뜬다. 문구는 app.json 의 것이어야 한다
- extendedWaitUntil:
    visible: ".*마이크로 목소리를 들어요.*"
    timeout: 5000
- takeScreenshot: <scratchpad>/p1-mic-dialog
- tapOn: "허용|OK|Allow"
- extendedWaitUntil:
    visible: ".*글자로 바꿔 입력창에 채워요.*"
    timeout: 5000
- takeScreenshot: <scratchpad>/p2-speech-dialog
- tapOn: "허용|OK|Allow"
# 시뮬레이터엔 목소리가 없다 → 무음 자동 종료 → no-speech 자리표시
- extendedWaitUntil:
    visible: ".*잘 못 들었어요.*"
    timeout: 20000
- takeScreenshot: <scratchpad>/p3-no-speech
```

**검증 조건:**
- 권한 대화상자 두 개의 문구가 `app.json` 에 적은 한국어 그대로다(영어 기본 문구가 보이면 플러그인 설정이 빌드에 안 들어간 것 — `ios/Etavia/Info.plist` 의 `NSMicrophoneUsageDescription` 을 확인).
- `p3-no-speech.png`: 입력창 자리표시가 `잘 못 들었어요. 다시 말해 주세요`, 버튼은 평소 상태, 앱이 죽지 않았다.
- 시뮬레이터가 호스트 마이크를 잡아 실제로 인식하면(`Simulator > I/O > Audio Input` 이 내장 마이크일 때) no-speech 대신 글자가 찰 수 있다. 그 경우 이 단계는 "시뮬레이터가 실제 인식함" 으로 기록하고, 자리표시 검증은 기내 모드 대신 **권한 거부 경로** 로 대체한다: `xcrun simctl privacy <UDID> revoke microphone com.etavia.app` 후 마이크 탭 → `설정에서 마이크·음성 인식을 켜 주세요` 가 보이는지.

- [ ] **Step 7: (선택) 호스트 마이크로 실인식 시도**

시뮬레이터 메뉴 `I/O > Audio Input > 내장 마이크` 를 켜고, 앱에서 마이크를 탭한 직후 맥이 말하게 한다:

```bash
say -v Yuna "스타벅스 들러서 가자"
```

입력창에 문장이 차면 기록한다. 안 차도 실패로 치지 않는다 — 실기기가 정답이다.

- [ ] **Step 8: 커밋**

이 Task 는 소스를 바꾸지 않는다. `.gitignore` 를 손댔으면 그것만 커밋한다. 스크린샷 6장(v1~v6, p1~p3)의 경로를 최종 보고에 적는다.

---

### Task 6: 실기기 검증 체크리스트와 문서

**Files:**
- Modify: `docs/NEXT.md`

- [ ] **Step 1: NEXT.md 에 실기기 항목 추가**

`docs/NEXT.md` 끝에 붙인다:

```markdown
## 음성 입력 — 실기기에서만 확인되는 것

시뮬레이터는 마이크가 없다. TestFlight 빌드(`npm run release -- <버전>`, 태그가 트리거)로 확인한다.
EAS 가 prebuild 하므로 서명·Xcode 패치는 손댈 게 없다.

- [ ] 계획 화면 → 마이크 탭 → 권한 두 번(마이크·음성 인식) 한국어 문구
- [ ] "스타벅스 들러서 가자" → 글자가 차오르고, 말을 멈춘 뒤 2~3초 안에 입력창에 채워진다
- [ ] 이어서 "약국도" → 공백 하나로 붙는다
- [ ] 듣는 중 재탭 → 그때까지 글자로 끝난다
- [ ] 기내 모드 → 온디바이스 재시도가 되거나(iOS 17+ ko-KR), 안 되면 `인터넷이 없어…` 자리표시
- [ ] 설정에서 마이크 끄기 → 마이크 탭 → `설정에서…` 자리표시 → 재탭 → 설정 앱이 열린다
- [ ] 브랜드명 오인식(올리브용 등)은 전송 후 LLM 이 교정하는지 — STT 쪽 수정 금지
```

- [ ] **Step 2: 커밋·병합**

```bash
git add docs/NEXT.md
git commit -m "docs(next): 음성 입력 실기기 체크리스트"
```

병합은 직접 한다(메모리 `merge-from-worktree-myself`): `git -C /Users/picpal/Desktop/workspace/duler merge --no-ff wip+0919` 전에 메인의 `git status --short` 가 깨끗한지 본다. 메인 8081 Metro 는 병합 후 자동으로 새 코드를 잡지만, **메인 시뮬레이터 앱들도 네이티브 재빌드가 필요하다** — 메인에서 `npm run ios` 를 돌리기 전에 Xcode 패치 여부를 다시 확인한다.

---

## 최종 보고 형식

1. 커밋 해시 목록(Task 1·2·4·6).
2. `npm test` 결과 한 줄(pass/fail 수).
3. 시뮬레이터 스크린샷 경로 v1~v6, p1~p3 와 각 검증 조건의 통과 여부 표.
4. Step 7(호스트 마이크 실인식) 결과: 됐다/안 됐다.
5. 실기기 체크리스트는 미검증으로 명시한다. 시뮬레이터 결과를 실기기 검증으로 부풀리지 않는다.
6. 막힌 것이 있으면 어느 Step 에서, 어떤 오류 첫 줄로 막혔는지.
