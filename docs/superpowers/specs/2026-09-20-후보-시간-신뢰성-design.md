# 후보 시간 신뢰성 — 교체 시트의 숫자를 같은 자로 잰다

작성 2026-09-20. 서교동 → 코엑스 대중교통, 이마트(무거움)·CU 두 곳. 계획은 `provider_legs`("구간마다 시간표 조회")로 직행 45분·들르기 +39분·도착 23:47 을 냈다. CU 교체 시트를 열자 **30곳 전부 "약 −32분 · 경로에서 0m · 추정"** 이었고 전부 홍대였다. 하나를 고르자 +39분이 +7분, 23:47 이 23:15 로 바뀌었는데 배너는 여전히 "구간마다 시간표 조회", '약'은 사라졌고, 버튼은 "23:15 도착 경로로 계속"이 됐다. 지어낸 32분이 실측인 척 확정 가능한 계획까지 흘러갔다.

이 문서는 설계다. 코드는 없다. `src/`·`server/`는 건드리지 않았다.

---

## 0. 지시문과 내 읽기가 다른 곳 — 먼저 적는다

설계의 전제가 되므로 맨 앞에 둔다. D1·D2·D3 는 지시문 그대로 확인했다. 아래는 다르거나 더 있는 것이다.

| # | 지시문 | 코드가 말하는 것 | 설계에 미치는 영향 |
|---|---|---|---|
| F1 | D4 는 `corridorSearch.ts:104-148` (`searchAlong`) | **이 실행은 그 경로를 타지 않았다.** 대중교통에 itinerary 가 있으면 `runPlan.ts:204-211` 이 `searchAtAnchors` 를 먼저 부르고, `searchAlong` 은 앵커가 0건일 때만 폴백이다. 30곳은 `attach`(`corridorSearch.ts:237-251`)가 잘랐다 — `anchorWalkM` 오름차순, 30 컷. 앵커 5개 중 **출발지(서교동)와 승차역(홍대입구) 둘이 홍대 안에 있고**, 카카오가 앵커당 한 페이지 15건을 주니 2 × 15 = 30 = `max` 로 딱 찬다. 삼성역·코엑스 앵커의 CU 는 걷는 거리가 조금 더 멀어 전부 밀렸다. 반지름 사다리도 첫 회차에 `target=8` 을 넘겨 멈췄다 | 컷 규칙은 `byCorridor` 와 `attach` **둘 다** 바꿔야 한다. 이미 `nearFirst` 를 공유하니 그 자리에 하나로 둔다(§3 D4) |
| F2 | D2 는 `planFlowBridge.ts:181` 한 곳 | 쌍둥이가 둘 더 있다. `src/state/plan.tsx:351` `applyCandidate` 는 **전체 여행 차이**를 `replaceDeltaMin` 에 더하고, `computeChain`(`plan.tsx:202`)은 그걸 **들어오는 구간 하나**에 붙인다 — 총합은 맞지만 그 경유지 도착 시각과 다음 구간이 틀린다. `TimelineScreen.tsx:52` 도 경유지 도착에 전체 차이를 더한다 | D2 는 A5 만 고치면 끝이 아니다. 확정 뒤 A6 경로(3단계)를 따로 잡는다 |
| F3 | 계획 1건 = `/transit` 10회 | 서버가 **일일 상한을 캐시보다 먼저 센다** (`server/src/transit.ts:31` → `:35`). `/places` 는 반대다(`index.ts` 의 주석이 이유를 적어 뒀다). 10분 안에 같은 구간을 다시 물으면 구글엔 안 나가는데 300 에서 1 이 빠진다 | "고른 매장을 다시 잰다" 류의 설계는 이 순서를 먼저 뒤집어야 이중 지출이 없다(§5) |
| F4 | `timingCopy.ts` 만 '약'을 정한다 | `CandidateSheet.tsx:51` `addedLabel` 이 `note.includes('추정')` 으로 **따로** 정한다. 문자열 냄새 맡기라 note 가 trend 근거로 바뀌면(`planFlowBridge.ts:213`) '추정' 글자가 사라지고 '약'도 같이 사라진다 — 관측된 "약이 사라짐"의 한 원인이다 | 시트가 `timingSource` 를 받고 `timingCopy` 로만 정한다(1단계) |
| F5 | — | `timingCopy('provider', mode, true)` 는 `showVerdict: true` 를 유지하고 테스트가 그걸 "자동차 교체 시트의 기존 동작"으로 굳혀 뒀다. 파일 머리말("추정치 위에서 판정하면 … 늦게 만든다")과 정면으로 어긋난다 | 1단계에서 뒤집는다. 그 테스트는 반대 문장으로 바꾼다 |
| F6 | — | 영업시간이 없는 후보는 `openStateOf` → `'open'` → **"영업 중"** 으로 나간다(`planFlowBridge.ts:112-119`). `Slot.fixed` 주석은 "화면은 영업 여부를 모른다고 말하고"라고 적었는데 그렇게 안 한다 | 같은 종류의 거짓말이지만 시간이 아니다. **범위 밖**으로 두고 여기 기록만 한다 |
| F7 | `computeRouteMatrix` 를 정직하게 | 요소당 과금이고 단가는 `computeRoutes` 와 같은 급이다(Essentials $5/1,000). **돈은 한 푼도 안 준다.** 얻는 건 왕복 한 번(지연)과, SKU 별로 월 무료분이 따로면 예산이 사실상 두 배가 되는 것 — 이건 **확인 못 했다** | 지금 안 한다. 7단계로 미루고 무료분 확인을 관문으로 건다(§5). **확인 끝 — §11: 관문은 열렸는데 안 한다** |

---

## 1. 근본 원인 — "자 섞기"

네 결함은 하나다. **등급이 다른 두 수를 산술로 섞고, 결과를 등급 없이 내보낸다.** 이름을 붙이면 **자 섞기(mixed-ruler arithmetic)** 다. D1 은 실측 합계에서 추정 합계를 뺐다 — 대중교통 실측 구간엔 접근·대기 8분씩이 들어 있고 추정(`km × ρ`, ρ = 45/13 ≈ 3.5분/km)엔 없으니, 홍대 안 300m 차이가 −32분으로 찍힌다. D2 는 **여행 전체** 차이를 **경유지 한 곳** 도착에 더했다 — 단위가 다른 두 수를 더한 것이라 출발(22:23)보다 이른 도착(21:58)이 나온다. D3 은 등급 표시가 **계획 단위**(`timingSource`)에만 있고 **변형 단위**(교체한 뒤의 `Rescored.estimated`)를 `provider_legs` 가지가 버린다 — 값은 추정으로 떨어졌는데 문구는 계획의 등급을 그대로 읽는다. D4 는 시간 축을 무시하고 후보를 잘라 **시간이 같은 30곳**을 **선택지 30개**로 늘어놓았고, 그 위에 D1 이 같은 −32 를 30번 찍었다. 각각을 따로 고치면 다음 화면에서 다시 난다. 깨진 건 불변식 하나다.

## 2. 지킬 불변식

> **사용자가 견주는 두 수는 같은 자로 잰 값이다. 화면에 나가는 모든 시간 값은 자기 등급을 들고 다니고, 등급을 문구로 바꾸는 곳은 `timingCopy` 하나다.**

검증 가능한 형태로 셋으로 쪼갠다.

1. **차이는 같은 자에서만 난다.** `Δ = a − b` 를 만들 때 a 와 b 는 같은 추정기로 만들었거나 둘 다 실측이다. 실측 − 추정은 존재하지 않는다.
2. **차이는 난 자리에만 붙는다.** 경유지 i 를 바꾸면 바뀌는 구간은 정확히 둘(`prev→i`, `i→next`)이다. i 의 도착엔 앞 구간 차이만, 총합엔 둘의 합이, 뒤 경유지 도착엔 둘의 합이 붙는다.
3. **등급은 값에 붙어 다닌다.** `Rescored`·`Alternative`·`Candidate` 가 `cls: 'measured' | 'estimated'` 를 들고, 화면은 그 값을 `timingCopy` 에 넘겨 '약'을 받는다. 문자열이나 계획 단위 등급으로 추측하지 않는다.

### 어디서 막나 — 재료를 없앤다

"조심하자"가 아니라 **뺄 재료를 손에 쥐어 주지 않는다.**

| 층 | 장치 | 효과 |
|---|---|---|
| 타입 | `src/lib/routePlan/types.ts` 에 `TimingClass = 'measured' \| 'estimated'`, `Timed = { min: number; cls: TimingClass }`. `Rescored` 에 `cls`·`legCls: TimingClass[]`·`delta?: { totalMin: Timed; arrivals: Timed[] }` 추가. `Alternative.addedMin`·`arriveMin` 은 `Timed`. `Candidate`(`mockData.ts`)에 `cls: TimingClass` | 등급 없는 시간 값이 화면 타입에 없다 |
| 관문 | `PlanResult.rescoreFrom(base: Visit[], visits: Visit[]): Rescored` 하나가 **기준 안 대비 변형의 시간과 차이**를 낸다(§3 D1). `rescore(visits)` 는 남되 `delta` 를 채우지 않는다 | 차이는 관문 안에서만 만들어지고, 관문은 같은 자를 쓴다 |
| 호출부 | `slotCandidates` 는 `timing.totalMin` 을 **받지 않는다**(시그니처에서 뺀다). `addedMin` 은 `rescoreFrom(...).delta` 에서만 온다. `runPlan.ts:443-450` 트렌드 스왑의 `addedMin` 도 같다 | 빼기를 쓸 수 있는 자리가 브리지에 없다 |
| 문구 | `timingCopy(source, mode, legEstimated)` 가 모든 가지에서 `legEstimated` 를 본다. `deltaCopy(t: Timed)` 가 추가시간 문구를 낸다. `CandidateSheet` 는 `timingSource` 를 prop 으로 받고 note 를 읽지 않는다 | '약'을 정하는 곳이 하나로 돌아온다 |
| 테스트 | 아래 §8. 핵심은 "실측 기준 안에서 300m 옆 후보를 골라도 −16분이 안 나온다" 한 줄 | 회귀하면 그 줄이 빨갛다 |

---

## 3. 결함별 설계

### D1 — 실측에서 추정을 뺀다 (`planFlowBridge.ts:177`)

**선택: 같은 자 차이(leg-differential).** WAPE 보정과 "숫자 안 보이기"는 버린다. 이유는 아래.

`rescoreFrom(base, visits)` 는 `scorePlan` 을 두 번 도는 게 아니라 **구간마다** 등급을 정한다. 기준 안 `base` 의 구간 k 와 변형 `visits` 의 구간 k 를 짝지어(`from>to` id 로 비교):

```
구간 k 가 두 안에서 같다        → 기준 값 그대로, 등급 그대로
구간 k 가 다르다:
  둘 다 lookup 적중            → M_new, cls measured, Δk = M_new − M_old
  아니면                        → E_new = km(new)×ρ, E_old = km(old)×ρ  (같은 추정기)
                                  값 = max(E_new, M_old + (E_new − E_old))   (M_old 없으면 E_new)
                                  cls estimated, Δk = E_new − E_old
```

`delta.totalMin = ΣΔk` (등급은 바뀐 구간들의 최저), `delta.arrivals[i]` 는 i 까지의 ΣΔk. 우회는 `detourM = |corridorOf(cand).y|` — 회랑 수직거리다. 기하값이라 등급이 없고, 추정 km 에서 실측 km 를 뺀 "0m" 이 사라진다.

**왜 같은 자인가.** `docs/transit-추정-오차.md` 가 답을 이미 적어 뒀다 — 구간 추정 오차는 bias −1.1분, MAE 5.1분이다. 한쪽으로 쏠린 게 아니라 흩어져 있어서 **상수를 어떻게 보정해도 RMSE 는 9% 준다.** 그러니 WAPE 로 추정을 끌어당겨도 화면 숫자는 그만큼밖에 안 좋아지고, 대신 "보정된 추정"이라는 등급 없는 숫자가 하나 더 생긴다. 반면 **같은 자로 잰 두 값의 차**는 계통 오차(접근·대기 8분 등)가 서로 지워진다. 홍대 안 300m 옆 후보의 Δ 는 `0.3km × 3.5 ≈ 1분` — "비슷해요"가 나온다. 정직하다. `M_old + (E_new − E_old)` 는 실측 기준 위에 그 차이를 얹은 것이라 절대 시각도 같은 논리로 선다. `E_new` 하한은 음수 방지용이지 보정이 아니다.

**왜 숫자를 아예 안 보이는 건 아닌가.** `provider_direct_only` 는 이미 '약'을 붙여 경유 추정치를 보여 주고 확정까지 허용한다. 그 목소리와 맞춘다. 다만 **묶음 안**(§4)에서는 숫자를 안 낸다 — 거기서 1분 차이는 추정기 잡음이라 숫자가 정보가 아니다.

**바꿀 파일**

- `src/lib/routePlan/plan.ts` — `rescoreFrom` 추가. `alternatives` 계산(`:196-213`)도 이걸로. `rescore` 의 `Rescored` 에 `cls`·`legCls` 채움.
- `src/lib/routePlan/score.ts` — `Scored` 에 `legCls` 추가(`hit` 이면 measured). `estimateLegKm` 은 그대로.
- `src/state/planFlowBridge.ts` — `slotCandidates(result, slots, visits, idx)` 로 시그니처 축소. `effectiveVisits` 는 `rescoreFrom(option.visits, visits)`. `alternativeToCandidate` 는 `Timed` 를 받고 `note` 에 ' · 추정' 을 붙이지 않는다(등급은 `cls` 가 든다).
- `src/state/runPlan.ts:443-472` — 트렌드 스왑의 `addedMin`·`arriveOk` 판정을 `rescoreFrom` 으로.
- `src/data/mockData.ts` — `Candidate.cls`.

**사용자가 보는 것**: 홍대 30곳이 "−32분" 대신 "비슷해요"(묶음이면 숫자 없음). 삼성역 CU 는 "약 +6분". 시드로 실측된 후보는 "+4분"(약 없음).

### D2 — 전체 차이를 한 지점에 더한다 (`planFlowBridge.ts:181` + F2 쌍둥이)

A5: `alternativeToCandidate(alt, query, alt.arriveMin, ...)` — `arriveMin = base.arrivals[idx] + delta.arrivals[idx]` 는 `rescoreFrom` 이 이미 냈다. 브리지는 더하지 않는다. 21:58 은 구조적으로 못 나온다(앞 구간 Δ 만 붙고, 그 Δ 는 `max(E_new, …)` 로 하한이 있다).

A6(확정 뒤, 3단계): `StopState.replaceDeltaMin` 을 `replaceDeltaInMin`·`replaceDeltaOutMin` 으로 나눈다. `computeChain` 은 들어오는 구간에 `In`, 나가는 구간(다음 스톱의 `legBetween(prev, …)` 또는 `dest`)에 `Out` 을 붙인다. `Candidate` 가 `addedMin` 외에 `arriveDeltaMin`(= 앞 구간 Δ)을 들고 나오면 `applyCandidate` 는 `In = arriveDeltaMin 차이`, `Out = addedMin 차이 − In` 으로 나눈다. `TimelineScreen.tsx:52` 는 후보의 `arriveAt` 을 그대로 쓴다 — 브리지가 낸 값이라 다시 더할 이유가 없다. `dataset.legs` 에 `cls` 를 실어 `toLegacyPlan` 이 `dataset.legEstimated` 를 세우고, `TimelineScreen.tsx:34`·`TabStubScreens.tsx:314`·`tracker.tsx:261,302` 가 세 번째 인자로 넘긴다 — 지금은 두 인자만 넘겨 확정 뒤 화면이 계획 등급만 읽는다.

### D3 — `provider_legs` 가 `legEstimated` 를 버린다 (`timingCopy.ts:38`)

`timingCopy` 를 (source, legEstimated) 표 하나로 다시 쓴다. `legEstimated` 가 참이면 **어느 source 든** '약'이 붙고 판정이 꺼진다. F5 의 `provider + true → showVerdict: true` 도 함께 뒤집는다 — 파일 머리말이 이미 그렇게 말하고 있고, `runPlan.ts:465-469` 가 같은 이유로 추정 위 스왑을 거부한다. 문구는 §6.

`deltaCopy(t: Timed): string` 을 같은 파일에 둔다. `CandidateSheet.addedLabel` 은 이걸 부른다. `signedMin` 의 '−' 처리는 그대로 옮긴다.

### D4 — 한 동네가 목록을 독점한다 (`corridorSearch.ts` `byCorridor` **와** `attach`)

**규칙: 진행률 버킷 라운드로빈.** 후보를 경로 위 위치로 묶고, 묶음마다 한 개씩 돌아가며 뽑아 `max` 에서 자른다.

- 버킷 키: 회랑 검색은 `floor(s × samples)` (s = `crossTrack(...).progressM / L`, 샘플 5 → 버킷 5). 앵커 검색은 `anchorId` (이미 가장 가까운 앵커로 붙여 놓는다).
- 버킷 안 순서: 지금 기준 그대로(회랑 수직거리 / `anchorWalkM`).
- 뽑기: 버킷을 진행 순으로 놓고 라운드로빈. 버킷이 비면 건너뛴다. `max` 에서 멈춘다.
- `side` 가 있으면 두 패스 — near 쪽 버킷들 먼저 라운드로빈, 그다음 반대쪽. `nearFirst` 의 "그쪽 먼저, 반대쪽 안 버림" 계약을 그대로 지킨다.
- 이름은 `spreadCut(list, bucketOf, rankOf, max, near?)`. `nearFirst` 자리를 대체하고 `byCorridor`·`attach` 둘 다 부른다. 회랑 컷과 앵커 컷이 같은 함수를 쓴다는 주석이 이미 있으니 거기 얹는다.
- 보장: 공급이 있는 버킷 B 개면 각 버킷은 최소 `⌊max / B⌋` 자리를 얻는다(5개면 6곳). 서교동·홍대입구가 30을 다 먹지 못한다.

**비용: `/places` 0회 추가.** 같은 검색 결과를 다르게 자를 뿐이다. 반지름 사다리의 `target` 판정(전체 8곳이면 멈춤)은 손대지 않는다 — 버킷별 목표로 바꾸면 회차마다 +5콜이고, 이 실행에서 삼성역 앵커는 첫 회차에 이미 후보를 줬다(밀린 것이지 없던 게 아니다). 필요해지면 그때 별도 단계.

**주의**: `runPlan.ts:275` 의 `sided.candidates.slice(0, MAX_CANDIDATES)` 가 한 번 더 자른다. `applyNear` 는 입력 순서를 보존하므로 `spreadCut` 의 라운드로빈 순서가 그대로 살아 통과한다. 뒤 단계 `enumerate` 는 `estC` 로 정렬하므로 순서에 안 민감하다.

---

## 4. 후보 묶기 — 같은 자리는 선택지 하나다

**정의**: 두 후보가 (같은 `anchorId` 이고) 서로 250m 안이면 한 묶음. `SAME_TIME_M = 250` 은 제품 문장이다 — **"250m 안은 같은 시간 선택지다."** 도보 3분이고, 구간 추정 MAE 5분·같은 OD 의 출발 시각 차이 20% 흔들림(`docs/transit-추정-오차.md`)보다 작다. 그 안의 시간 차이는 정보가 아니라 잡음이다. 묶기는 탐욕 — 후보를 Δ 오름차순으로 훑으며 기존 묶음 중심 250m 안이면 합치고 아니면 새 묶음.

**대표**: 묶음에 실측된 후보(두 구간 다 `measured`)가 있으면 그것. 없으면 `scoreTrend` 1위. 묶음의 시간은 대표의 시간이고 등급도 대표의 등급이다.

**시트가 말하는 것**:

- 묶음 머리: `같은 자리 4곳 · 시간 차이 없어요` — 대표의 추가시간·도착만 한 번 보인다.
- 묶음 안 순서: 시간이 아니다. `scoreTrend`(`signals.google` 평점·`signals.blog` 언급) → 영업(`closing_soon` 은 뒤로, `closed` 는 이미 `rankCandidates` 가 뺀다) → 이름. 이유 문구는 지금처럼 `reasons.join(' · ')`.
- 묶음 사이 순서: `추가시간` 탭은 대표 Δ, `추천` 탭은 대표 trend 점수.
- 묶음이 1곳이면 머리 없이 지금 카드 그대로.

**어디에**: `src/lib/routePlan/cluster.ts` 순수 함수 `clusterCandidates(cands, distanceM)`. 브리지의 `slotCandidates` 가 `Candidate` 에 `clusterId`·`clusterSize` 를 붙이고, `CandidateSheet` 가 그걸로 머리를 그린다. `rankCandidates` 는 묶음 단위로 정렬하도록 키를 대표에서 읽는다.

**실제로 재야 할 자리 수**: 30곳이 5 버킷에 퍼지면 묶음은 5~10개다. 시드가 이미 재 둔 것(V=1 은 8안, V=2 는 4안 안팎)이 묶음 몇 개를 공짜로 실측으로 만든다 — D4 로 후보가 퍼지면 `pickSeeds` 의 `estC` 상위가 서로 다른 묶음에 걸릴 확률도 오른다. 나머지 묶음은 **시트를 열 때 재지 않는다**(0회). 고를 때 그 하나만 잰다(6단계, 2회). 열 때마다 5~10 묶음 × 2구간 = 10~20회를 쓰는 안은 300/일 아래에서 성립하지 않는다.

**지시문에 대한 반론 하나**: "묶음 = 재야 할 위치 수"는 맞지만 그걸 **시트 열기 비용**으로 삼으면 안 된다. 사용자는 30곳 중 한 곳을 고르지 열 곳을 고르지 않는다. 재는 건 고른 것 하나면 된다.

---

## 5. 비용 모델

단위: `/transit` 호출 수(= 일일 상한 300 이 세는 것). 매트릭스는 요소 수.

| 항목 | 오늘 | 제안 | 비고 |
|---|---|---|---|
| 계획 1건 (transit V=1) | 9 (직행 1 + 4안 × 2) | 9 | D4·묶기는 0 |
| 계획 1건 (transit V=2) | 8~10 | 8~10 | 동일 |
| 교체 시트 열기 | 0 | 0 | `rescoreFrom` 은 순수 계산 |
| 후보 고르기 (A5 `setOverride`) | 0 — 그래서 −32 가 공짜였다 | **2** (`prev→c`, `c→next`). 시드였으면 0 (LegStore 적중) | 6단계. 계획당 `TRANSIT_SWAP_BUDGET = 4`(고르기 2번). 넘으면 '약'으로 남는다 |
| 계획 1건 상한 | 10 | **14** (10 + 4) | `transitBudget.ts` 한 곳 |
| 300/일이 받는 계획 | 30 | 30 (안 고름) · 25 (1번 고름) · **21** (2번 고름) | 실사용 대부분은 안 고르거나 1번이다 |
| 다시 계산 (10분 안) | 10 (구글엔 ~0 나감) | **~0** — F3 을 뒤집으면 | 캐시 적중을 상한에서 빼면 고르기 예산보다 더 돌려받는다 |
| `/places` 계획 1건 | 슬롯당 5 × 회차(1~4) + far 5 | 동일 | D4 는 컷만 바꾼다 |
| 매트릭스 요소 (묶음 대표 C 개 전부, V=1) | — | 2C (1×C + C×1). C=8 → 16 요소 | 단가 = 호출 단가. **돈은 같다** |

**결론**: 상한 300 은 그대로 둔다. 대신 두 가지를 같이 한다 — (1) `TRANSIT_SWAP_BUDGET = 4` 를 `transitBudget.ts` 에 두고 플래너·공급자·가드 주석이 같은 숫자를 본다, (2) `server/src/transit.ts` 의 캐시 확인을 `overDailyCap` 앞으로 옮긴다(`/places` 와 같은 순서·같은 주석). (2) 없이 (1) 만 하면 고른 매장을 재는 2회 중 시드 구간이 캐시에 있어도 상한을 먹는다.

**`computeRouteMatrix` — 지금 안 한다.** 요소당 과금이라 `1×C + C×1` 은 `computeRoutes` 2C 회와 **같은 돈**이다. 얻는 것은 왕복 한 번(지연 1.7~2.5초 → 한 번)과, **SKU 별 월 무료분이 따로라면** `/transit` 예산이 사실상 두 배가 되는 것인데, 후자는 요금표에서 확인하지 못했다. 응답에 `legs.steps`·정류장이 없어 직행(앵커 재료)엔 못 쓰고 구간 실측에만 쓴다. 서버 엔드포인트·스키마·가드 버킷·앱 공급자까지 한 단계 분량이다. 6단계(고른 것만 2회)가 확정을 실측으로 만드는 데 이미 충분하므로, 7단계로 두고 **무료분 확인을 관문**으로 건다. 확인되면 그때 "시트를 열 때 묶음 대표 전부"가 가능해진다.

---

## 6. 신뢰 표면 — 상태별로 사용자가 읽는 것

`timingCopy(source, mode, legEstimated)` 의 새 표. 기존 문구는 한 글자도 안 바꾸고 `legEstimated` 행만 는다.

| source | legEstimated | approx | banner | showVerdict |
|---|---|---|---|---|
| `provider` | false | `''` | `null` | true |
| `provider` | **true** | `'약 '` | `'바꾼 매장 구간은 추정이에요'` | **false** (F5 뒤집음) |
| `provider_legs` | false | `''` | `'구간마다 시간표 조회 · 체류 뒤 배차는 미반영'` | false |
| `provider_legs` | **true** | `'약 '` | `'구간마다 시간표 조회 · 바꾼 매장 구간은 추정'` | false |
| `provider_direct_only` | * | `'약 '` | 기존 | false |
| `estimate` / undefined | * | `'약 '` | 기존 | false |

6단계가 들어오면 `true` 행의 배너 뒤에 재는 동안 ` · 시간표 조회 중` 을 붙이고, 응답이 오면 `legEstimated` 가 거짓이 되어 위 행으로 올라간다. 6단계 전에는 "조회해요" 같은 약속을 문구에 넣지 않는다 — 없는 기능을 말하면 거짓말이다.

`deltaCopy(t: Timed)`:

| cls | 값 | 문구 |
|---|---|---|
| measured | +7 | `+7분` |
| measured | −3 | `−3분` |
| measured | 0 | `같아요` |
| estimated | \|Δ\| < 3 | `비슷해요` |
| estimated | +6 | `약 +6분` |

카드의 다른 칸: 도착은 `cls` 가 estimated 면 `약 23:15`, 아니면 `23:15`. note 는 `CU · 경로에서 120m` — ' · 추정' 접미를 뗀다(등급은 '약'이 말한다, 두 군데서 말하면 한쪽이 틀린다). 묶음 머리는 §4.

**상태 넷을 화면 하나로**(A5 헤드라인 · 배너 · 버튼):

| 상태 | 헤드라인 | 배너 | 버튼 |
|---|---|---|---|
| 전부 실측 | `23:47 도착` (+판정) | 없음 / 구간 배너 | `23:47 도착 경로로 계속` |
| 일부 추정 (바꾼 뒤) | `약 23:41 도착` | `구간마다 시간표 조회 · 바꾼 매장 구간은 추정` | `약 23:41 도착 경로로 계속` |
| 전부 추정 | `약 …` | 기존 모드별 배너 | `약 … 경로로 계속` |
| 모름 | 시간 칸 대신 `시간을 알 수 없어요` | `이 후보는 경로에 붙일 수 없어요` | 고르기 비활성 | 

"모름"은 `rescoreFrom` 이 후보를 회랑에 투영 못 할 때(직행 폴리라인이 2점 미만·ρ 가 추정 출처)만이다. 지금 코드로는 직행이 실패하면 계획 자체가 FAIL 이라 A5 까지 안 오므로, 이 행은 타입에 자리만 만들고(`cls` 에 `'unknown'` 을 넣지 않는다 — `Timed | null`) 화면은 null 이면 고르기를 막는다.

**바꾸면 dirty 로 만들고 다시 계산을 강제할까 — 아니다.** 근거 셋.

1. 다시 계산은 `/transit` 10 + `/places` 10~20 + 12초다. 그런데 **고른 후보를 재 준다는 보장이 없다** — 시드는 `estC` 로 뽑히고 사용자의 선택은 시드가 아닐 수 있다. 비싸면서 진실을 안 산다.
2. `provider_direct_only` 는 이미 '약'을 붙인 채 확정을 허용한다. 같은 목소리다. 다르게 하면 왜 어떤 '약'은 되고 어떤 '약'은 안 되는지 사용자가 설명받아야 한다.
3. 진짜로 원하는 건 "고른 것을 잰다"이고 그건 2회면 된다(6단계). 강제 재계산은 그 자리에 놓인 비싼 대체품이다.

그래서: 바꾸면 **'약' + 배너 + 판정 꺼짐**으로 확정을 허용하고, 6단계가 들어오면 고르는 순간 2회를 재서 '약'을 지운다. dirty 배너("조건이 바뀌었어요")는 지금처럼 `isStale`·경유지 빼기에만 쓴다.

---

## 7. 단계 — 하나씩, 싼 것부터

| 단계 | 결함 | 바꾸는 것 | API | 혼자서 죽이는 거짓말 |
|---|---|---|---|---|
| **1** | D3 · F4 · F5 | `timingCopy` 표 + `deltaCopy`. `CandidateSheet` 가 `timingSource` prop 을 받고 note 를 안 읽는다. `toLegacyPlan` 이 `dataset.legEstimated` 를 세우고 A6·트래커 호출부 셋이 3번째 인자를 넘긴다 | 0 | "실측인 척" — 바꾼 뒤 '약'과 배너가 붙고 판정이 꺼진다. 숫자(−32)는 아직 남는다 |
| **2** | D1 · D2(A5) | `Timed`·`cls`·`rescoreFrom`. `slotCandidates` 시그니처 축소. `alternatives`·트렌드 스왑도 관문으로. 우회 = 회랑 수직거리 | 0 | **"고르면 32분 줄어든다" — 이 단계가 죽인다.** 21:58 도 함께 |
| **3** | D2(A6, F2) | `replaceDeltaInMin`/`OutMin`, `computeChain`, `applyCandidate`, `TimelineScreen:52` | 0 | 확정 뒤 타임라인의 경유지 도착이 맞는다 |
| **4** | D4 · F1 | `spreadCut` — `byCorridor`·`attach` 공용 | 0 | 홍대 30곳 독점. 삼성역 CU 가 목록에 산다 |
| **5** | 묶기 | `cluster.ts`, 시트 묶음 머리, 묶음 안 순서 | 0 | "30개 선택지" — 5~8개 자리로 읽힌다 |
| **6** | 확정 실측 | `PlanResult.measureSwap(visits, idx)` — 두 구간만 공급자에 묻고 `LegStore` 에 넣는다. `TRANSIT_SWAP_BUDGET`. 서버 캐시-앞 순서(F3) | +2/고르기 | 바꾼 뒤에도 '약' 없이 확정할 수 있다 |
| 7 (관문) | 매트릭스 | `/transit-matrix` + 스키마 + 가드 버킷 + 앱 어댑터 | 요소 = 호출 | 무료분 별도 확인 뒤에만 |

1 과 2 의 순서: 1 이 더 싸고(순수 함수 한 파일 + prop 하나) 그 자체로 출시 가치가 있다 — 숫자가 틀려도 '약'이 붙으면 AGENTS.md 의 선은 안 넘는다. 2 는 관문을 세우는 일이라 타입이 여러 파일에 닿는다. 둘을 한 커밋에 섞지 않는다.

6단계 세부: `measureSwap` 은 `plan()` 클로저 안 `legs`·`provider` 를 닫아 둔 함수로 `PlanResult` 에 싣는다(`rescore` 가 이미 그런 모양이다). 두 구간을 `Promise.all` 로 한 파도에 보내 공급자 코얼레싱을 받고, 오면 `learnLegs` 뒤 리듀서에 `LEGS_LEARNED` 를 보내 `effectiveVisits` 가 다시 돈다. 실패는 삼킨다 — '약'으로 남을 뿐이다. 예산은 계획당이고 `apiCalls` 에 더한다(로그가 사용량을 축소해 말하면 안 된다).

---

## 8. 테스트 — 단계마다 원래 결함을 잡았을 한 줄

러너는 `node:test` + `node:assert/strict`, `npm test`. 파일과 문장.

**1단계 — `src/lib/timingCopy.test.ts`**
- `구간 실측(provider_legs)이라도 바꾼 구간이 추정이면 약이 붙고 배너가 그걸 말하고 판정은 없다` — `timingCopy('provider_legs', 'transit', true)` deepEqual. **이 한 줄이 D3 를 잡는다.**
- `실측(provider)이라도 구간 하나가 추정이면 판정하지 않는다 — 추정 위의 여유는 거짓 정밀도다` — 기존 "약은 붙고 판정은 한다" 테스트를 이 문장으로 바꾼다.
- `deltaCopy — 실측은 부호 그대로, 추정은 약, 3분 미만 추정은 비슷해요, 실측 0은 같아요`
- `src/state/planFlowBridge.test.ts`: `toLegacyPlan — 오버라이드로 미실측 후보가 들어오면 dataset.legEstimated 가 true 다`

**2단계 — `src/lib/routePlan/plan.test.ts`**
- `rescoreFrom — 기준 안이 실측이고 바꾼 구간만 미실측이면 차이는 같은 추정기로 잰 두 값의 차다 — 실측에서 추정을 빼지 않는다`: 대중교통 목(구간마다 접근·대기 8분)으로 V=1 계획, 시드 밖 후보를 현재 후보 300m 옆에 둔다. `delta.totalMin.cls === 'estimated'`, `|delta.totalMin.min| < 3`. 오늘 코드면 ≈ −16 이 나와 빨갛다. **이 한 줄이 D1 을 잡는다.**
- `rescoreFrom — 바뀐 두 구간이 다 실측이면 차이도 실측이다` — 시드 후보끼리 바꿔 `cls === 'measured'`.
- `rescoreFrom — 바꾼 경유지의 도착 차이는 들어오는 구간만이다 — 여행 전체 차이를 한 지점에 더하지 않는다`: `delta.arrivals[idx].min === Δ_in`, `arrivals[idx] >= departAtMin`. **D2.**
- `rescoreFrom — 안 바뀐 구간은 값도 등급도 그대로다`
- `src/state/planFlowBridge.test.ts`: `slotCandidates — 같은 자리 후보들이 전부 같은 큰 음수로 나오지 않고 도착은 출발 뒤다`; `경로에서 Nm 는 회랑 수직거리다 — 추정 km 에서 실측 km 를 뺀 0m 가 아니다`.
- `src/state/runPlan.test.ts`: `트렌드 스왑의 addedMin 은 rescoreFrom 의 delta 다 — 실측 기준 안에서 추정 후보로 바꿔도 마감 판정을 뒤집지 않는다`.

**3단계 — `src/state/plan.test.ts`**
- `replaceStop — 교체 차이는 들어오는 구간과 나가는 구간에 나뉘어 붙고, 그 경유지 도착은 앞 구간 차이만 움직인다`: 스톱 2개, 두 번째 후보로 바꾸고 첫 스톱·두 번째 스톱·목적지 시각을 각각 단언. 오늘 코드는 두 번째 스톱 도착에 전체 차이가 붙어 빨갛다.

**4단계 — `src/lib/corridorSearch.test.ts`**
- `앵커 둘이 15곳씩 가까이 채워도 세 번째 앵커의 후보가 목록에 산다 — 진행률 버킷마다 자리를 남긴다`: 앵커 3개, 앞 둘은 100m 에 15곳씩, 셋째는 300m 에 5곳. 결과 30 안에 셋째 앵커 후보 ≥ 5. 오늘 코드는 0 이라 빨갛다. **F1 재현.**
- `회랑 5점 중 한 점에 40곳이 몰려도 다른 점의 후보가 산다` — `searchAlong` 판.
- `side=end 면 그쪽 버킷을 먼저 채우되 반대쪽을 버리지 않는다` — 기존 `nearFirst` 계약 유지.

**5단계 — `src/lib/routePlan/cluster.test.ts`**
- `250m 안 후보는 한 묶음이고, 묶음 대표는 실측된 후보가 있으면 그것이다`
- `묶음 안 순서는 시간이 아니라 평점·언급·마감이 정한다 — 추가시간이 같으니 시간으로 줄 세우면 순서가 임의다`
- `src/lib/candidateRank.test.ts`: `추가시간 탭은 묶음 대표 Δ 로 묶음을 세우고 묶음 안은 흩지 않는다`

**6단계**
- `src/lib/routePlan/plan.test.ts`: `measureSwap — 고른 후보의 두 구간만 /transit 2회로 재고 시드였던 구간은 다시 묻지 않는다`; `measureSwap — 계획당 TRANSIT_SWAP_BUDGET 을 넘는 고르기는 추정으로 남고 apiCalls 는 실제 나간 수다`; `measureSwap — 재고 나면 같은 후보의 rescoreFrom 이 measured 로 올라간다`
- `server/src/transit.test.ts`: `/transit 캐시 적중은 일일 상한을 먹지 않는다 — /places 와 같은 순서다`
- `src/lib/timingCopy.test.ts`: `재는 동안은 배너에 조회 중이 붙고 끝나면 실측 행으로 올라간다`

---

## 9. 범위 밖으로 남기는 것

- F6 영업시간 모름 → "영업 중". 시간이 아닌 다른 거짓말이라 따로 한 단계.
- 반지름 사다리의 버킷별 `target`. D4 컷만으로 이 실행은 고쳐진다. 회차마다 +5콜이라 관측된 뒤에.
- `pickSeeds` 가 묶음을 보고 시드를 고르는 것. 묶음(5단계)이 들어온 뒤 플래너 쪽 별도 단계.
- 9단계(확정 뒤 실제 출발 시각 재조회)는 `docs/대중교통-경로-단계계획.md` 그대로. 6단계의 `measureSwap` 은 그 재료(구간 재조회)를 먼저 만드는 셈이라 겹치지 않고 이어진다.

---

## 10. 구현하며 정정한 것 (2026-09-20, 1~4단계 착지 후)

위 설계는 코드를 읽고 썼지만 구현이 **여섯 군데를 뒤집었다.** 5~7단계는 이 절을
먼저 읽는다 — 위 본문에 남은 원문보다 여기가 우선이다.

**D4 · 앵커 버킷 키는 `anchorId` 가 아니라 `used` 안의 인덱스다.** 문자열로는
경로 순서가 안 나온다. `extractAnchors` 가 출발→승차→…→도착 순으로 내므로
그 인덱스가 곧 진행 순서다. (4단계)

**D4 · "샘플 5개 → 버킷 5개"는 `side='any'` 일 때만이다.** `side` 가 있으면
샘플 구간이 절반이라 공급 버킷은 2~4개다. `⌊max/B⌋` 보장은 그대로지만
'6곳씩'을 기대하면 안 된다. (4단계)

**D4 · "near 쪽 *버킷* 먼저"가 아니라 "near 쪽 *후보* 먼저"다.** 버킷 하나가
1500m 경계를 걸칠 수 있다. `nearFirst` 가 하던 것과 같은 계약, 다른 단위다. (4단계)

**D4 · 순서 보존 논증에 `applyParkingPolicy` 가 빠져 있었다.** `applyNear`
앞에 있고 자동차 모드에서 재정렬한다. `a.i - b.i` 타이브레이크라 안정 정렬이고
자르지 않고 거르기만 해서 결론은 살았다 — 운이 좋았던 것이지 논증이 맞았던
게 아니다. (4단계)

**D1 · 우회 거리는 `rescoreFrom` 이 못 낸다.** 후보별 회랑 투영이 필요하고
브리지엔 그게 없다. 그래서 `alternativeAt(base, idx, cand)` 이 실제 관문이고,
`rescoreFrom` 만 두는 것보다 강하다 — 호출자가 총합 두 개를 손에 쥘 일이
아예 없어진다. (2단계)

**D2 · `TimelineScreen:52` 에서 후보의 `arriveAt` 을 그대로 쓰면 안 된다.**
그 값은 계산 시점 시계(`request.departAtMin`) 위에 있어서, 재시계화·순서변경·
앞선 교체가 끼면 실제 도착과 다르다. 맞는 형태는 "현재 도착 + 후보 간 차이".
설계대로 했으면 시계가 어긋난 계획에서 조용히 틀렸을 자리다. (3단계)

### 설계에 없던 결함 둘 (구현 중 발견, 이미 고침)

**`legEstimated` 가 확정 시점에 눌러앉는다.** `toLegacyPlan` 이 한 번만
세워서, 확정 뒤 A6 에서 매장을 바꾸면 진행중·타임라인이 계속 "실측"이라고
말했다. 1단계가 앞 화면에서 없앤 거짓말이 한 화면 뒤에 살아 있었다.
`legEstimatedOf`/`syncLegEstimated` 가 유일한 파생점이다. (3단계)

**부호를 박아 둔 자리 둘.** `OptionsScreen` EtaBar 의 `들르기 +N분` 과
`TabStubScreens` 의 `경유 +N분`. 음수면 `+-5분` 이 나오고, 추정 구간이 섞이면
음수가 된다. 후자는 `copy.approx` 도 빠져 있어 '약 94분' 옆에 맨몸으로 붙어
있었다 — 한 카드의 두 숫자가 다른 등급을 말하면 하나는 거짓이다.
둘 다 `signedMin` 한 곳에서 낸다.

**묶기는 브리지가 아니라 시트 쪽(`rankClusters`)에 있다.** 설계는
`slotCandidates` 가 `clusterId`·`clusterSize` 를 후보에 붙이라고 했는데, 시트는
그걸 받아도 다시 묶어야 그린다. `Candidate` 타입을 늘리지 않고 `rankClusters`
한 곳에서 묶으면 목 데이터셋·A6 교체까지 같은 규칙을 공짜로 받는다. (5단계)

**묶음의 기준점은 중심이 아니라 첫 멤버다.** 중심은 멤버가 붙을 때마다 움직여서
250m 씩 이어 붙으면 묶음이 얼마든지 길어진다 — "같은 자리"가 아니게 된다.
첫 멤버로 고정하면 지름이 2×250m 로 묶인다. `anchorId` 조건도 뺐다: 같은 250m
안이 앵커가 달라 두 묶음이 되면 화면에 "같은 자리"가 두 번 뜬다. (5단계)

**대표 규칙과 묶음 안 순서를 따로 두면 안 된다.** 설계는 대표를 실측→trend,
안쪽을 trend→영업→이름으로 나눠 적었다. 그대로 짜니 곧 마감하는 곳이 이름순에
걸려 대표가 되어 맨 위에 서고, 바로 아래 목록에서는 뒤로 밀렸다 — 한 곳이 같은
화면에서 1등이자 꼴등이다. `preferenceOrder` 하나를 `leadOrder` 가 꼬리로 쓴다.

**설계가 빠뜨린 것 — 지금 경로에 든 곳이 접힌 안쪽으로 사라진다.** 묶기 전에는
추가시간 0 이라 늘 맨 위였는데, 대표가 아니면 "이 자리 N곳 더" 안으로 들어간다.
기기에서 실제로 그랬다(2026-09-20, CU 홍대한벗점이 목록에서 안 보였다).
`leadOrderWith(currentId)` 가 현재 후보를 무조건 대표로 세운다. (5단계)

### 유지된 판단

`Candidate.cls` 는 설계상 2단계였지만 **1단계에 필요했다.** 같은 실측 계획
안에서도 시드 후보는 실측이고 나머지는 추정이라, 계획 단위 `timingSource`
만으로는 카드 문구를 못 정한다 — F4 가 걷어낸 note 냄새 맡기가 나르던
비트가 바로 이것이다.

§8 의 `runPlan` 트렌드 스왑 테스트는 **추가하지 않았다.** 바깥에서 관측되지
않는다(추정 후보는 애초에 스왑되지 않고, 신호 없는 후보가 평점 5.0 을 이길 수
없다). 기존 `추정치면 스왑하지 않는다` 가 그 관문을 덮는다. 빈 테스트를
넣으면 숫자만 부푼다.

---

## 11. 7단계 — 관문은 열렸고, 그래서 안 한다 (2026-09-21)

§5·§7 은 `computeRouteMatrix` 를 **무료분 확인**이라는 관문에 걸어 뒀다. 확인했다. 관문은
열린다. 그런데 열고 보니 사려던 물건이 우리 모양에 안 맞는다.

### 확인한 것

| 물음 | 답 | 근거 |
|---|---|---|
| 월 무료분이 SKU 별로 따로인가 | **그렇다.** `Compute Routes Essentials` 10,000 과 `Compute Route Matrix Essentials` 10,000 이 각자다 | 요금표의 SKU별 Free Usage Cap |
| 매트릭스 과금 단위 | 요소(origins × destinations)당. 요청당이 아니다 | Routes API Usage and Billing |
| TRANSIT 은 Essentials 인가 Pro 인가 | **Essentials.** Pro 유발 조건은 중간 경유지 11~25개·`optimizeWaypointOrder`·`TRAFFIC_AWARE(_OPTIMAL)`·위치 수정자이고, Enterprise 는 이륜차·통행료·폴리라인 교통정보다. **travelMode 는 어느 목록에도 없다** | SKU details 의 트리거 조건 |

세 번째는 덤으로 얻은 것인데 7단계보다 값지다. `transitBudget.ts` 의 "월 무료분 10,000 ÷ 30
= 일 300" 이 **가정이 아니라 확인된 사실**이 됐다. TRANSIT 이 Pro 였다면 5,000 이라 지금
예산이 두 배로 헐거웠을 것이다.

### 그런데 — `departureTime` 은 요청당 하나다

`ComputeRouteMatrixRequest` 의 최상위 필드다. `RouteMatrixOrigin` 안이 아니다
(REST 레퍼런스의 필드 목록: `origins[] · destinations[] · travelMode · routingPreference ·
departureTime · arrivalTime · …`). 즉 **한 매트릭스의 모든 요소가 같은 출발 시각으로 조회된다.**

우리가 재려는 두 묶음의 성질이 다르다.

- **들어오는 구간**(`prev → 후보ᵢ`) — 출발 시각이 전부 같다(앞 경유지 도착 + 체류).
  `1 × C` 매트릭스가 정확히 맞는다.
- **나가는 구간**(`후보ᵢ → next`) — **후보마다 출발 시각이 다르다.** 후보ᵢ 에 언제 닿느냐가
  다르기 때문이다(`measureSwap` 이 `outClock = inClock + inMin + dwell` 로 내는 그 값).
  `C × 1` 매트릭스는 그 C 개를 **한 시각으로 묶어 버린다.**

나가는 절반을 한 시각으로 재는 건 이 문서가 §1~§2 에서 없애기로 한 **자 섞기**의 변종이다.
대중교통은 특히 나쁘다 — 같은 OD 가 출발 시각만 달라져 20% 흔들린 기록이 있고
(`docs/transit-추정-오차.md`), `LegStore` 의 창은 15분이다.

"묶음 안에서는 시각 차이가 1~3분이니 창 안 아니냐"는 반론이 가능하지만, 그건 **7단계가
사려던 물건이 아니다.** §5 가 적은 상품은 "시트를 열 때 **묶음 대표 전부**"이고, 4단계
(`spreadCut`) 이후 묶음 대표들은 홍대와 삼성역처럼 멀리 흩어진다 — 도착 시각이 수십 분씩
벌어진다. 한 시각으로 묶으면 틀리는 바로 그 경우가 상품의 내용이다.

### 결론 — 7단계를 닫는다

- **6단계로 충분하다.** 고른 것 하나를 두 구간 다 제 시각에 재서 '약'을 지운다(호출 2회).
  매트릭스로 살 수 있는 건 "열 때 여러 개를 대충", 6단계가 파는 건 "고를 때 하나를 제대로"다.
  후자가 사용자가 실제로 필요로 하는 것이다 — 사용자는 30곳 중 한 곳을 고르지 열 곳을 고르지 않는다.
- **별도 무료분은 쓸 데가 없다.** 두 번째 주머니가 있어도 우리 모양이 매트릭스에 안 들어간다.
  들어오는 구간만 매트릭스로 재는 절충안도 값이 없다 — 카드의 등급은 추가시간과 도착 중
  **낮은 쪽**이라(`alternativeToCandidate`), 나가는 구간이 추정으로 남으면 '약'이 그대로다.
- **다시 열어야 할 조건**: Google 이 요소별 `departureTime` 을 주거나, 우리가 "열 때 여러 개"를
  정말로 원하게 되는 제품 변화가 있을 때. 그 전에는 이 절이 답이다.
